node_modules/
.pnpm-store/
.turbo/
.next/
coverage/
playwright-report/
test-results/
.env
.env.*
!.env.example
apps/consumer/public/qa-checkin.png
*.tsbuildinfo

# Secretos locales de Wallet (SA de Google + firma Apple) — NO commitear
.secrets-apple/

# Material de firma / secretos — NUNCA se commitea (defensa en profundidad).
# La carpeta tests/ NO se ignora entera (tiene los e2e reales); solo secretos.
*.p12
*.key
*.pem
*.cer
*.der
*.mobileprovision
Wallet Issuer.json

# macOS junk (mistake→rule 2026-08-15: tests/.DS_Store aparecía como untracked)
.DS_Store
**/.DS_Store
