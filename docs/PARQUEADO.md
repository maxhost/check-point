# PARQUEADO

**Todo lo diferido, parado o pospuesto de este proyecto vive aca.** Creado por la spec 0066
(ADR 0069): `docs/TASKS.md` pasa a contener **solo el arco en ejecucion**, y este es el unico
lugar donde buscar pendientes.

Cada fila dice **de donde salio** y **por que se paro**. El detalle largo de casi todas esta en
`docs/archivo/TASKS-historico-2026-09-16.md` (buscar con `grep -n` por el numero de tarea, que se
conserva).

Regla al sacar algo de aca: **nada toca codigo sin su spec cerrada.** Varias de estas filas dicen
explicitamente «necesita spec» — eso es literal.

## Decisiones tomadas que esperan spec

| # | Que | Origen | Por que se paro |
|---|---|---|---|
| 54 | **El impago bloquea el acceso** (backoffice + cuenta + mostrador): 3 reintentos en la semana y despues un modal que pide pagar para rehabilitar. El plan NO baja y los locales no se tocan. Disparador = estado de Stripe (`unpaid`, o `past_due` con `next_payment_attempt: null`), no un contador propio | ADR **0059** | Decision del owner del 2026-09-10, nacida de una pregunta abierta de la 0063. **Se saco de la 0063 a proposito:** el bloqueo vive en `requireBackofficeSession`, el guard compartido de 8 paginas **y del mostrador** — un bug ahi deja a todos afuera |
| 56 | **El gate de email verificado en las otras 9 superficies de API del owner**, unificado en un `requireApiOwner`, **con los `code` `unauthorized`/`not_owner`/`email_not_verified` en los 401/403** y con `memberships.status='active'` sumado a los tres `ownerBusiness` | spec **0068** (medido el 2026-09-17) | **Recorte del owner del 2026-09-17**, con su motivo: «como estamos en proceso de reconversion todo a API diferentes, mas seguras, yo tocaria solo lo que faltaba de staff». **Las dos decisiones de contenido YA estan tomadas** por el owner ese mismo dia (si al `status='active'`, si a los `code`): lo unico que falta es cuando. **El agujero sigue abierto y esta medido:** el gate del ADR 0070 §11 vive en un solo archivo (`app/api/staff/_auth.ts:50`) y cubre 2 superficies de 11, asi que **un owner con el email sin verificar crea sucursales, sube marca, arma campañas, carga catalogo, edita el programa de fidelizacion y abre un checkout de Stripe**. Las 9 sin gate: `api/billing/*`, `api/locations/*`, `api/marketing/*`, `api/catalog/*`, `api/brand`, `api/brand/logo-upload` (que **ni siquiera resuelve owner**: solo sesion), `api/loyalty-program`, `api/loyalty-program/stamp-upload`, `api/loyalty-terms/templates`. **Dos trampas escritas de antemano para quien lo tome:** (1) el gate va **despues** de resolver al owner, o un integrante recibe `email_not_verified` en vez de `not_owner` —lo cazo un test de la 0067—; (2) `billing-routes-auth.neon` **NO falsifica esto**: su seed pone `emailVerified: true` y nunca ejercita el caso. **Hallazgo adicional medido:** los tres `ownerBusiness` (`brand.ts:32`, `catalog/core.ts:73`, `loyalty-program.ts:51`) **no filtran `status='active'`** —que `ownerContext` si filtra— y loyalty ordena `desc` donde los otros `asc`. **GATILLO, definido el 2026-09-17 tras preguntar el owner si esto espera a «que armemos las API de esas superficies» (medido: NO hay tal reescritura agendada — el ADR 0070 no menciona ninguna de esas 9 rutas, 0 matches; el arco reconstruye la UI, no esos endpoints, asi que el gate NO llega gratis con nada). Sigue parqueada por dos razones medidas: (1) la 3a spec del arco (entitlements) va a editar `billing/`, `marketing/` y `locations/` —3 de las 9— para migrar los call-sites divergidos, asi que el `requireApiOwner` sale natural ahi y hacerlo ahora es tocar esos archivos dos veces; (2) la exposicion HOY es CERO: produccion tiene 0 negocios, 0 usuarios merchant y 0 suscripciones (por SQL, 2026-09-17). **El vencimiento real no es «cuando existan las API» sino ANTES DEL PRIMER COMERCIO REAL** — desde que haya un owner de verdad, el agujero es explotable por su propia cuenta sin verificar.** **→ ABSORBIDA POR LA SPEC 0072** (`specs/0072-entitlements-y-estado-del-negocio.md`, escrita el 2026-09-17): se implementa ahi, no se retoma por separado. **→ RESUELTO: la spec 0072 esta `implementada`; su resumen cubre exactamente esto.** |
| 57 | **`core.business.status`** — estado propio del negocio | pedido del **owner** el 2026-09-17 | **LA COLUMNA YA ESTA, EN PRODUCCION (migracion `0036`, 2026-09-17). LA FEATURE NO.** El owner dicto los tres estados y su semantica: `active` opera normal; **`suspended`** → el **staff NO puede loguearse**, el owner SI entra pero solo ve un mensaje con el motivo y un boton de contacto, y **toda la plataforma queda inusable** (no se escanea, no se acreditan puntos ni sellos, no se edita programa ni marca); **`closed`** → **no admite NI LOGIN**, ni owner ni staff. Se migro `status` + `suspension_reason` (el popup tiene que decir por que) + `status_changed_at`, con `CHECK` de los tres valores, **a pedido explicito del owner**, para que la migracion no vuelva a bloquear a la 3ª spec. **⚠️ NO HAY UN SOLO GUARD QUE LA LEA: hoy un negocio `suspended` esta suspendido en la BASE y plenamente operativo en la APP.** Lo que FALTA, y es todo el trabajo: los guards en **el login** (el estado hay que leerlo en `auth/start`, en el consumo del link magico y en el PIN del staff — o sea que toca superficie de la spec 0067, ya implementada), en las 11 superficies de API y en el mostrador. **Quien ESCRIBE el estado queda diferido por decision del owner** a «las API de admin de CheckPass.club», que no existen (`apps/platform` tiene una sola ruta, `/api/health`): por ahora es un `UPDATE` a mano. **Y lo que el owner NO dijo, que NO se escribe como decision suya:** que pasa del lado del CONSUMIDOR de un negocio `closed` —pases de Wallet ya emitidos, sellos acumulados, el QR que sigue circulando—. Su respuesta describio la superficie del comercio. Decision pendiente de la 3ª spec **→ ABSORBIDA POR LA SPEC 0072** (`specs/0072-entitlements-y-estado-del-negocio.md`, escrita el 2026-09-17): se implementa ahi, no se retoma por separado. **→ RESUELTO: la spec 0072 esta `implementada`; su resumen cubre exactamente esto.** |
| 58 | **`seedConsumer` genera `phone_e164` con `Math.random()` sobre una columna UNIQUE** | hallazgo del **revisor de la 0075** (2026-09-18), reproducido | `counter-integration-support.ts:209` hace `` `+593${Math.floor(100000000 + Math.random() * 800000000)}` `` e inserta en `core.consumer_account`, que tiene **`consumer_account_phone_unique`**. Contra una rama Neon **compartida que acumula filas**, es una colision de cumpleaños del seed. **Evidencia ejecutada:** `NeonDbError: duplicate key value violates unique constraint "consumer_account_phone_unique" — Key (phone_e164)=(+593278146317) already exists` en `marketing-merit.neon.integration.test.ts`, que **aislado da 3/3 verde**. **Afecta a 25 archivos de test** (`rg -l seedConsumer`). **Por que importa y no es cosmetico:** produce rojos **indistinguibles de una regresion**, y en esta misma sesion un rojo intermitente de esa familia costo una hora y casi entra a un reporte al owner como bug del producto. **El arreglo es de una linea** y el patron correcto ya esta en el mismo archivo: `qrToken` usa `randomUUID()`. **NO se toco**: es codigo, y en este repo el codigo no se toca sin su spec. **Decision del owner pendiente:** spec chica de una linea, o se parquea. **OJO, no confundir con el otro flake:** `consumer-recovery.neon > enforces 3/hour persistently` es **otro**, con mecanismo **SIN identificar** — verificado que **no usa `seedConsumer`**. |
| 55 | **Cambio de intervalo anual → mensual** | ADR **0058** §10 | Recorte explicito del owner el 2026-09-10: «entregamos mensual a anual, no anual a mensual». El sentido inverso no tiene forma barata — exige devolver ~11 meses en credito (el owner excluyo los reembolsos), o `proration_behavior: 'none'` que **cobra de inmediato y pierde lo pagado**, o `subscription_schedules` (superficie nueva) |
| 48 | **`/wallet` no se actualiza en vivo**: el consumidor recibe el push, abre el icono y ve el saldo viejo hasta recargar | spec **0060** (en `borrador`) | 4 decisiones abiertas del owner. **No es regresion ni olvido:** la spec 0031 saco la «landing en vivo» de su alcance explicitamente y sin reemplazo. Confirmado por el owner en el QA B1.4 |
| 49 | **La clave publica de Geoapify quedo sin restriccion de origen** | QA / gotcha de CORS | Fix operativo del owner para destrabar el CORS (ACAO fijo = un solo dominio). El DoD «tokens publicos restringidos por origen» de la 0023 esta **falso en produccion, a proposito**. Fix durable ya identificado (Opcion B): proxear el autocomplete por el server con `GEOAPIFY_API_KEY`, same-origin |
| 29 | **Arte final de los pases de Wallet** (Google `heroImage` + logo; Apple `strip` + logo/icon + colores), servir assets desde dominio estable y actualizar la Loyalty Class real antes del publishing access | rebrand CheckPass Club (cierre de 0032) | El rebrand se hizo app-wide; el arte quedo fuera. Necesita spec |
| 15 | **Catalogo unico de beneficios** (cupones/premios, no productos) | spec **0021** | Diferido con las campañas (ADR 0034): sus consumidores —wizard de campañas + Incentive Engine— no existen. Revive con esa fase |

## Hallazgos a decidir (nadie los acordo todavia)

| # | Que | Origen | Estado |
|---|---|---|---|
| 43 | El probe de decode **no muestra ninguna señal en pantalla hasta 8 s**, y el fallback real paga un base64 de ~6,7 MB en el camino que **siempre** falla | revisor de la spec 0052 | **Hallazgo a decidir, no acordado.** 4 opciones escritas, ninguna elegida. Si el owner reporta «tarda y no pasa nada», **es esto, no un bug nuevo** |
| 45 | En **Android** el selector de imagen ofrece solo galeria, nunca la camara (Android 13+ rutea `image/*` al Photo Picker). No existe el atributo `capture` en ningun archivo del repo | QA del owner 2026-09-05, bloque A3 | Necesita decision + spec chica. Efecto colateral ya visible: el texto de ayuda del catalogo promete «podes tomar una foto» |
| 41 | Re-enroll de un telefono existente **abre la sesion de esa cuenta** | ADR **0057** | **Aceptado temporalmente.** Antes de ampliar ese flujo se suma OTP para probar posesion del telefono |
| 59 | **El barrido de las rutas del mostrador hardcodea los 4 nombres.** `apps/merchant/src/server/counter-email-gate.test.ts` itera `["resolve","grant","redeem","coupon-redeem"]` y exige que cada `route.ts` contenga `await requireOperator(request)`. **Muerde para las 4 que conoce** (medido: el revisor de la 0082 mutó `coupon-redeem` para que resolviera la sesión por su cuenta y el barrido se puso rojo), pero **no puede descubrir una quinta**: una ruta nueva en `app/api/counter/` que resuelva la sesión sola quedaría fuera del gate de email **sin poner rojo a nadie**. | spec **0082** (revisor, 2026-09-19) | **Severidad BAJA y arreglo conocido, no aplicado**: enumerar el directorio con `readdirSync` y asertar un piso de 4 archivos, en vez de la lista literal. No se aplicó en la 0082 porque llegó DESPUÉS del PASS y tocar el test habría invalidado la revisión. **Gatillo: la próxima vez que se agregue una ruta a `app/api/counter/`** — ahí el barrido tiene que descubrirla o el gate se pierde en silencio. |
| 60 | **El PIN del integrante tiene que llegarle SIN que lo vea quien lo dio de alta** (SMS u otro canal fuera de banda). | decision del **owner** el 2026-09-20, al elegir conservar el perfil administrador (ADR 0079 §3) | **Es la unica cosa que cierra la escalada residual del perfil ADMINISTRADOR, y esta aceptada a conciencia mientras tanto.** Hoy `POST /api/staff` devuelve `{staff, pin}` con el PIN **en claro**, y `pin/regenerate` lo vuelve a emitir: por eso **crear a un tercero con el permiso X equivale a tener X** — quien lo crea se queda con la credencial y entra como el. Con la regla «un no-owner SI otorga permisos que el no tiene» (ADR 0079 §3.2, pedida por el owner con su caso del admin que necesita dar de alta a alguien de marketing), un administrador puede obtener por cuenta interpuesta **los seis permisos no-`staff`**. Queda acotado —nada del ADR 0079 §8 se alcanza asi— y **registrado** en `core.activity_log`. Palabras del owner: *«a futuro pensare como hacemos llegar ese pin al staff sin que el administrador lo pueda ver, por ejemplo por SMS, pero esto sera a futuro»*. **Necesita spec** y trae proveedor de SMS, costo por mensaje y un dato que hoy no pedimos (el telefono del integrante): el alta escribe **solo el nombre** (spec 0067 §4). |

## Deuda de verificacion declarada (no perseguida, a proposito)

| Que | Origen | Nota |
|---|---|---|
| **0058 y 0059 no tienen oraculo de comportamiento**: su unica cobertura es un barrido estatico de strings | tarea 50, demostrado **por mutacion** el 2026-09-09 | Borrar `setIsAnalyzing(true)` de `use-brand-logo.ts` apaga «Preparando imagen…» —el **DoD #1** de la 0059— con los **5 gates verdes**. La tecnica que lo cierra ya existe en el repo: `login-form-retry.test.ts` stubea `useState` con `vi.mock("react")`, ~45 lineas y cero paquetes |
| Deuda declarada de la **spec 0065** | handoff de la 0065 | `stages.tsx` en 299/300 · la precedencia de `cancel_reason` · el cableado de los botones (solo lo cierra el QA humano) · los limites de la fase A: los 400 m sin caso de integracion, el orden de los seis motivos de exclusion, y «sin el advisory lock dos corridas se pasan de cuota» (racear dos ticks reales es un volado, declarado por la propia spec) |
| **`no-mutations-left.sh` solo ve `.ts`/`.tsx` bajo los `src/`** | medido el 2026-09-16 al implementar la 0066 | Una mutacion sobre un `.md`, un `.sh` o un hook **no la caza nadie**: las dos mutaciones de esta spec (una en una skill, otra en `CLAUDE.md`) habrian sobrevivido al Stop. Se **declara** en vez de arreglarse porque tocar los hooks existentes esta fuera del alcance de la 0066 |
| Las GitHub Actions tiran **warning de deprecacion de Node 20** (`actions/checkout@v4`, `actions/setup-node@v4`, `pnpm/action-setup@v4` forzadas a correr en Node 24) | fuera de alcance de 0047 y 0048 | No bloquea nada |

## Pendientes del owner (no son codigo)

| Que | Origen |
|---|---|
| Alta del remitente en **Resend** + envs en Vercel + QA en vivo del recovery | spec **0046** |

## Legado de la era demo/scaffold

Las tareas **3, 5, 6, 11, 12, 13 y 14** (specs 0010–0020) quedaron marcadas «en revision» —
implementadas y con gates locales verdes, **sin PASS independiente** y con E2E/QA manual
pendientes. En los hechos las superó el producto real (backoffice, marca, programa, wizard). Se
dejan registradas aca para que nadie las reviva por error creyendo que son trabajo pendiente:
**si algo de eso hace falta, sale como spec nueva contra el producto de hoy**, no retomando una
fila de agosto. Detalle en `docs/archivo/TASKS-historico-2026-09-16.md`.
