# INDEX

Mapa de ADRs y specs. **Empeza aca.** Cada fila tiene lo suficiente para decidir si
abrir el archivo o no — no leas todo: lee la fila y abri lo que corresponda.

**Al crear un ADR o una spec, agregar la fila aca en el mismo commit.** Un indice
desactualizado es peor que no tenerlo.

## Arquitectura

[ARCHITECTURE.md](ARCHITECTURE.md) reúne las decisiones transversales y enlaza sus ADRs.
[SCAFFOLD-PLAN.md](SCAFFOLD-PLAN.md) define el recorrido y DoD para preparar el repositorio antes de implementar una feature.
[REPOSITORY.md](REPOSITORY.md) identifica el remoto canónico y la política de publicación.
[LECCIONES.md](LECCIONES.md) es el registro historico de `mistake→rule`: cada error observado del agente con su caso, su fecha y la regla que salio de ahi. Salio de `CLAUDE.md` en la spec 0066; **no se referencia con `@`** (un import `@` se carga como si estuviera pegado y anularia la poda).
[PARQUEADO.md](PARQUEADO.md) es el **unico lugar donde buscar pendientes**: todo lo diferido, parado o pospuesto, con su origen y por que se paro. `TASKS.md` contiene solo el arco en ejecucion (hoy: el arco del wizard, ADR 0070 / spec 0067).
[archivo/](archivo/) guarda lo cerrado que ya no se lee seguido: los encargos, revisiones y barridos de fases terminadas, el `TASKS.md` historico (7.185 lineas, hasta 2026-09-16), el `CLAUDE.md` de 696 lineas previo a la poda, `spec-0066-implementacion.md` (los tres pasos, la bitacora de mutaciones y el PASS del revisor de la spec 0066, movido aca al cerrar esa spec) y `spec-0067-implementacion.md` (**los 3 pasos del arco del wizard**: las bitacoras de mutaciones de implementadores y revisores, la evidencia ejecutada y los tres `PASS` — incluidas la sonda que prueba que el `pin_hash` centinela del backfill es **fail-closed** y la que prueba que una cookie de sesion forjada sin el secreto devuelve `null`). Nada se borro; se movio con `git mv`.
[.claude/skills/](../.claude/skills/) son las skills que se cargan on demand: `protocolo-de-verificacion` (mutaciones, presupuesto, oraculos, limites) y `gotchas-del-repo` (drizzle, Stripe, Neon, Vercel, pnpm, wallet…). `.claude/agents/` define `implementador` y `revisor` con el protocolo y el presupuesto adentro.
[wallet-go-live.md](wallet-go-live.md) es el checklist de go-live de los pases de Wallet (Google demo→prod gratis; Apple $99); el código ya está listo, es trámite de cuenta + arte.

## ADR — decisiones

| # | Fecha | Decision | Estado | Archivos |
|---|---|---|---|---|
| 0001 | 2026-08-09 | V1 centrada en campañas rentables para bares | aceptada | `adr/0001-v1-campanas-rentables-para-bares.md` |
| 0002 | 2026-08-09 | Guardrail económico y saldos aislados por local | aceptada | `adr/0002-guardrail-economico-y-no-puntos-globales.md` |
| 0003 | 2026-08-09 | V1 con consumidor, comercio y red curada por administrador | aceptada | `adr/0003-v1-tres-actores-y-red-curada.md` |
| 0004 | 2026-08-09 | Wallet con activos separados por emisor y alcance | aceptada | `adr/0004-wallet-con-emisor-y-alcance-explicitos.md` |
| 0005 | 2026-08-10 | Vigencia por emisor y ciclo de eliminación guest | aceptada | `adr/0005-vigencia-de-activos-y-retencion-guest.md` |
| 0006 | 2026-08-10 | Programa y campañas a nivel negocio; evento como campaña | aceptada | `adr/0006-programas-y-campanas-a-nivel-negocio.md` |
| 0007 | 2026-08-10 | Permisos globales, operación local y auditoría con snapshots | aceptada | `adr/0007-permisos-globales-operacion-local-y-snapshots.md` |
| 0008 | 2026-08-10 | Owner como único administrador de su merchant staff | aceptada | `adr/0008-owner-administra-exclusivamente-su-staff.md` |
| 0009 | 2026-08-10 | Cierre de local y elegibilidad dinámica de beneficios | aceptada | `adr/0009-cierre-de-local-y-elegibilidad-de-beneficios.md` |
| 0010 | 2026-08-10 | Dominios de acceso separados para consumidor, plataforma y comercio | aceptada | `adr/0010-dominios-de-acceso-separados.md` |
| 0011 | 2026-08-10 | Monorepo y despliegues web separados | propuesta | `adr/0011-monorepo-y-despliegues-web-separados.md` |
| 0012 | 2026-08-10 | Neon, Better Auth y esquemas de identidad separados | propuesta | `adr/0012-neon-better-auth-y-esquemas-de-identidad-separados.md` |
| 0013 | 2026-08-10 | OTP propio de CheckPass Club con transporte intercambiable: ClickSend y Twilio primero, ClickSend activo, sin fallback automático; selección futura global/por país desde administración de plataforma; SMS ahora y WhatsApp después | aceptada; revisada 2026-08-17, supersede Telnyx/Twilio Verify | `adr/0013-otp-y-tareas-programadas-con-proveedores-intercambiables.md` |
| 0014 | 2026-08-10 | QR, Wallet y check-in con defensa en capas | propuesta | `adr/0014-qr-wallet-y-checkin-con-defensa-en-capas.md` |
| 0015 | 2026-08-10 | Juegos web y AR como fase experimental | propuesta | `adr/0015-juegos-web-y-ar-como-fase-experimental.md` |
| 0016 | 2026-08-10 | Operación de merchant staff rápida y consistente | propuesta | `adr/0016-operacion-de-merchant-staff-rapida-y-consistente.md` |
| 0017 | 2026-08-10 | Estándar de entrega production grade | aceptada | `adr/0017-estandar-production-grade.md` |
| 0018 | 2026-08-10 | Incentive Engine de campañas compuestas | aceptada | `adr/0018-incentive-engine-de-campanas-compuestas.md` |
| 0019 | 2026-08-10 | Branding temático por negocio y primera experiencia consumer | aceptada | `adr/0019-branding-tematico-por-negocio.md` |
| 0020 | 2026-08-10 | Programa de fidelización único por negocio | aceptada | `adr/0020-programa-de-fidelizacion-unico-por-negocio.md` |
| 0021 | 2026-08-11 | Analíticas universales y lentes por rubro | aceptada | `adr/0021-analiticas-universales-y-lentes-por-rubro.md` |
| 0022 | 2026-08-11 | Objetivos de campaña y capacidades medibles | aceptada | `adr/0022-objetivos-de-campana-y-capacidades-medibles.md` |
| 0023 | 2026-08-11 | Disparadores independientes de objetivos de campaña | aceptada | `adr/0023-disparadores-independientes-de-objetivos.md` |
| 0024 | 2026-08-11 | Secretos en entorno y configuración no secreta | aceptada | `adr/0024-secretos-en-entorno-y-configuracion-no-secreta.md` |
| 0025 | 2026-08-11 | Búsqueda de locales con proveedores y procedencia | aceptada | `adr/0025-busqueda-de-locales-con-proveedores-y-procedencia.md` |
| 0026 | 2026-08-12 | Versiones, transiciones y términos de programas | aceptada | `adr/0026-versiones-transiciones-y-terminos-de-programas.md` |
| 0027 | 2026-08-12 | Programa mutable con cierre fechado | aceptada; supersede 0026 | `adr/0027-programa-mutable-con-cierre-fechado.md` |
| 0028 | 2026-08-12 | Auditoría y cancelación de cierre de fidelización | aceptada | `adr/0028-auditoria-y-cancelacion-de-cierre-de-fidelizacion.md` |
| 0029 | 2026-08-13 | Módulo de assets compartido para imágenes en R2 | aceptada | `adr/0029-modulo-de-assets-compartido-para-imagenes-en-r2.md` |
| 0030 | 2026-08-13 | Modelo de datos del diseño de tarjeta de fidelización | aceptada | `adr/0030-modelo-de-datos-del-diseno-de-tarjeta-de-fidelizacion.md` |
| 0031 | 2026-08-14 | Merchant-first, Wallet nativa como superficie de consumidor e identidad de consumidor compartida | aceptada; supersede la "red curada" de 0003 y reencuadra 0019 | `adr/0031-merchant-first-wallet-nativa-e-identidad-de-consumidor.md` |
| 0032 | 2026-08-14 | Identidad de consumidor: esquema pg propio `consumer`, auth phone-OTP purpose-built y DB compartida para analítica aislada por negocio | aceptada; refina 0012 | `adr/0032-identidad-de-consumidor-esquema-propio-y-auth-phone-otp.md` |
| 0033 | 2026-08-14 | Proveedor de Wallet: Apple PassKit + Google Wallet, emisor único, UN pase de identidad por consumidor (barcode = `qr_token`), dev sin pagar Apple, push separado | aceptada; consume 0014/0024 | `adr/0033-proveedor-de-wallet-apple-passkit-y-google-wallet.md` |
| 0034 | 2026-08-14 | Catálogo de productos (no de beneficios) en `core`, global por negocio con visibilidad opt-out por local; precio/coste opcionales, puntos por equivalencia en el programa, snapshot en acreditación | aceptada; reencuadra el catálogo de 0002 y difiere 0021 | `adr/0034-catalogo-de-productos-por-negocio.md` |
| 0035 | 2026-08-14 | Imágenes de stock para productos: interfaz `StockPhotoProvider` intercambiable (Pexels primero), server-proxied (key en env) y anti-SSRF (resolución por id + allow-list de host), import a R2 diferido a Guardar, atribución persistida y visible | aceptada; extiende la spec 0034 | `adr/0035-imagenes-de-stock-para-productos.md` |
| 0036 | 2026-08-14 | Mecánica de acumulación (otorgar X por bloque de $Y, floor sin arrastre; Sellos "1 por compra") en columnas dedicadas + premios (`loyalty_reward`: producto del catálogo / libre / % descuento) en tabla relacional; costo en puntos = gasto-objetivo calculado (no IA), editable; prerequisito de la acreditación 0030 | aceptada; extiende 0024/0027, habilita 0030 | `adr/0036-mecanica-de-acumulacion-y-premios-del-programa.md` |
| 0037 | 2026-08-15 | Cola de push de Wallet (`wallet_push_queue`) escrita como outbox transaccional en el grant de 0030; prioridad transaccional > campaña, cooldown por-consumidor, un solo slot "Última novedad" en el pase compartido; dispatch inmediato best-effort + worker de cron | aceptada; extiende 0033, consume 0013/0024, habilita la spec 0033 | `adr/0037-cola-de-push-de-wallet-con-prioridad-y-cooldown.md` |
| 0038 | 2026-08-15 | Dos transportes de notificación seleccionables — `wallet` (pase Apple/Google, 0033) y `webpush` (Web Push del navegador, 0037) — sobre el mismo modelo de aviso/cola; Web Push arranca **solo Android** (iOS lo cubre el pase de Apple); el transaccional hace fan-out a los transportes del consumidor; las campañas eligen transporte (distinta tasa de conversión) | aceptada; el punto "solo Android" superado por 0039; extiende 0037/0033, habilita la spec 0037 | `adr/0038-transportes-de-notificacion-wallet-y-web-push.md` |
| 0039 | 2026-08-15 | Web Push también en **iOS** (supersede el "solo Android" de 0038): en iOS el permiso/suscripción solo existen dentro de la **PWA instalada en el home** (16.4+), nunca en Safari — dos contextos: landing de Safari (instructivo de instalar + **escape hatch** que da el pase sin instalar) y **micro-portal** (la página `(consumer)/wallet` existente, hecha instalable) donde se suscribe; token del portal = `web_view_token` (rota en recuperación, esa rotación purga las suscripciones); motivación asimétrica (iOS por el portal, Android por el contenido rico); cripto `node:crypto` sin dependencia, verificada contra el vector del RFC 8291 | aceptada; supersede el punto "solo Android" de 0038; extiende 0037/0033 | `adr/0039-web-push-en-ios-via-pwa-y-micro-portal.md` |
| 0040 | 2026-08-16 | El transporte de una notificación lo decide la **clase** del aviso, no el fan-out a todo: **transaccional → solo wallet** (Apple/Google), con **fallback a Web Push** si el consumidor no tiene un pase alcanzable; el **Web Push deja de ser transporte por defecto del transaccional** y pasa a ser transporte de **campaña**. Elimina las notificaciones duplicadas del QA real en iOS (pase + Web Push a la vez) y simplifica el ruteo (la clase basta, sin inspeccionar plataforma/PWA). El opt-in de Web Push se ofrece por plataforma en la confirmación (iOS: instructivo; Android: botón de permiso, única vía del fallback real) | aceptada; supersede el fan-out del ADR 0038 §3; extiende 0037/0039; habilita la spec 0038 | `adr/0040-ruteo-de-transporte-por-clase-de-aviso.md` |
| 0041 | 2026-08-16 | Las subidas de imagen (logo/sello/producto) **recortan y reducen en el CLIENTE** antes de subir (recuadro drag+zoom, touch y desktop; blob acotado por `<canvas>`). Doble motivo: el usuario controla el encuadre (no resize ciego) y el server recibe siempre una imagen chica → puede volver a un `limitInputPixels` estricto (menos superficie de bomba de descompresión). Requisito mobile+desktop; cropper cargado diferido para no engordar el bundle. Supersede el paliativo de 50 MP de la spec 0039 | aceptada (dirección); librería y aspectos los cierra la spec 0040; habilita la spec 0040 | `adr/0041-recorte-de-imagen-en-el-cliente-antes-de-subir.md` |
| 0042 | 2026-08-16 | Atribución por **local** (`location_id`) como dimensión **universal** de todo evento de valor (alta, venta, acumulación, canje futuro), capturada de dos fuentes: **counter** para eventos del operador (venta/acumulación ya en `order.location_id`; canje futuro nace igual) y **QR escaneado** para el alta self-service (`/enroll/<programId>?loc=<locationId>` → `program_membership.origin_location_id`). "Separar estadísticas por local" = `GROUP BY location_id`, sin lógica por pantalla. El evento de canje no existe aún (spec 0036 solo define premios) y el tablero por local es otra feature; el ADR fija el contrato para ambos | aceptada; consume 0030/0025; habilita la spec 0041; fija el contrato para el canje futuro y el tablero por local | `adr/0042-atribucion-por-local-como-dimension-universal.md` |
| 0044 | 2026-08-30 | Roles a nivel negocio: **owner** (backoffice completo) y **staff** (solo la consola de mostrador). `business_membership.role` gana CHECK `in ('owner','staff')` y una columna `status` (`active`/`disabled`) para desactivar sin borrar (preserva la auditoría de `order.operator_user_id`). Staff se provisiona con better-auth (`signUpEmail` server-side sin propagar la cookie del owner), nunca insert crudo. Acceso gateado por rol: owner-only redirige al staff a la consola; staff `disabled` pierde acceso (se revocan sus sesiones). El mostrador ya acepta cualquier membership → funciona para staff sin tocar el dominio; marca/programa/catálogo/analytics ya filtran `role='owner'` → cerrados a staff | aceptada; consume 0022/0030; habilita la spec 0043; supersede la parte de roles/staff del borrador 0005 y el mock `demo/staff` | `adr/0044-roles-de-negocio-owner-y-staff.md` |
| 0045 | 2026-08-30 | Recovery de **owner/staff por OTP al email** (no SMS): el email ya es su identidad en better-auth y hoy no tienen recuperación. El OTP y el set de contraseña los posee el plugin `emailOTP` de better-auth (hashing + revocación de sesiones correctos, no se toca `account.password` a mano); **Resend** es el proveedor activo detrás de un contrato `EmailChannel` intercambiable (ahí vive el "cambiar de canal a futuro"). Enumeración-resistente, rate-limit persistente, gate por env. El OTP del consumidor (SMS, 0013/0032) queda intacto y aislado | aceptada; enmienda 0013; consume 0044; habilita la spec 0046 | `adr/0045-recovery-de-owner-staff-por-otp-al-email.md` |
| 0046 | 2026-09-02 | Versión de Node: el proyecto sigue la **Active LTS que Vercel ofrece como runtime** (hoy 24.x), no la Current de Node (26.8.1). Migrar a 26 ahora dejaría local/CI en 26 y producción en 24 — el CI dejaría de probar lo que se despliega, justo lo que acaban de arreglar 0047/0048. Vercel sólo ofrece 24.x/22.x/20.x, así que el techo no es negociable desde el repo. Node 24 tiene soporte hasta 2028-04-30: no hay urgencia. La versión pasa a una sola fuente de verdad con guard anti-drift; el salto a 26 queda agendado y gateado por que Vercel lo ofrezca | aceptada | `adr/0046-version-de-node-seguir-la-lts-de-vercel.md` |
| 0047 | 2026-09-02 | **El cropper de imagen es best-effort; el server sigue siendo el límite.** Enmienda el punto 2 del ADR 0041: Chrome/Firefox/Edge **no decodifican HEIC** (HEVC está bajo patente y no lo licencian; Safari sí, delegando en el SO), así que un cropper obligatorio rompería las fotos de galería de Android — el caso exacto que ya costó dos rondas de QA (0033 y 0039). El cropper cae al camino de hoy si el navegador no puede decodificar, y por eso `limitInputPixels` estricto aplica **sólo** al camino con recorte. Se descartó convertir HEIC en el cliente: implica un decoder HEVC en WASM (1–2 MB reales, y los dos paquetes mantenidos son **LGPL-3.0**) antes de medir si hace falta; reapertura condicional al QA, sin fecha | aceptada; enmienda el 0041 | `adr/0047-el-cropper-es-best-effort-el-server-sigue-siendo-el-limite.md` |
| 0048 | 2026-09-05 | **El manifest per-consumidor lleva el token en su URL, no en la cookie.** Enmienda el punto 5 del ADR 0039. Un `<link rel="manifest">` se pide **sin credenciales** salvo que lleve `crossorigin="use-credentials"`, que producción no tiene — así que la cookie de sesión nunca llega, `resolveSession` recibe `undefined` y `start_url` cae a `/wallet` **siempre**: el re-bootstrap por `/c/<token>` nunca funcionó, ni siquiera desde `/wallet` (verificado en prod). El token pasa a viajar en la URL del manifest (`?c=<token>`), server-renderizada por la página que ya tiene la sesión. Se descarta `crossorigin="use-credentials"` porque **falla en silencio hacia el estado roto**: si Safari no manda la cookie, el síntoma es indistinguible del bug. No abre amenaza nueva — el `web_view_token` ya es at-bearer (ADR 0014) y ya viaja en URLs dentro del pase | aceptada; enmienda el 0039 §5; habilita la spec 0050 | `adr/0048-el-manifest-per-consumidor-lleva-el-token-en-su-url.md` |
| 0049 | 2026-09-05 | **El instructivo vuelve a la confirmación del enroll.** Revierte la decisión 3 de la spec 0050 por orden del owner tras QA en vivo (instalar pasando por `/wallet` agregaba dos pasos: flujo inaceptable) y enmienda la consecuencia del ADR 0048 que prohibía enlazar el manifest en el enroll — el núcleo del 0048 (token en la URL del manifest, nunca cookie) queda intacto. El 201 del enroll devuelve `walletManifestPath` y la confirmación lo inyecta como `<link rel="manifest">`. Seguro porque viaja en la MISMA respuesta que ya setea la cookie de sesión (la sesión se emite sólo en el 201): mismo destinatario, mismo poder. Invariante a blindar: el path aparece SOLO en el 201 | aceptada; revierte spec 0050 §3; enmienda ADR 0048; habilita la spec 0051 | `adr/0049-el-instructivo-vuelve-a-la-confirmacion-del-enroll.md` |
| 0050 | 2026-09-05 | **El re-enroll actualiza el nombre de la cuenta.** Enrolarse con un teléfono ya registrado pisaba el nombre tipeado en silencio (el owner se enroló como "Logan Wolf" y el pase salió "Cliente iOS 4", verificado por SQL en prod). Gana el dato más fresco. No toca teléfono, país ni tokens. **Deja anotado, sin saldarlo, que ese mismo camino ya entrega una sesión completa a quien conozca un teléfono ajeno** (el enroll no verifica el teléfono) — hallazgo preexistente, tarea 41 | **SUPERSEDIDO por el ADR 0051** (el owner rechazó la decisión al verla implementada) | `adr/0050-el-re-enroll-actualiza-el-nombre-de-la-cuenta.md` |

## Specs — que se construye

| # | Fecha | Spec | Estado | Disjunta? | Archivos |
|---|---|---|---|---|---|
| 0001 | 2026-08-09 | Acceso de backoffice, membresías y auditoría | **deprecada (informativa)** — reemplazada por decisiones/implementaciones posteriores | no | `specs/0001-fundacion-identidad-y-roles.md` |
| 0002 | 2026-08-09 | Local, personal y catálogo económico | reencuadrada por ADR 0034 | no | `specs/0002-local-personal-y-catalogo.md` |
| 0003 | 2026-08-10 | Wizard de campañas e Incentive Engine | borrador | no | `specs/0003-wizard-de-campana-y-guardrails.md` |
| 0004 | 2026-08-09 | Cuenta de consumidor, OTP y wallet | reencuadrada por ADR 0031 | no | `specs/0004-consumidor-checkin-y-wallet.md` |
| 0005 | 2026-08-09 | Consola de personal y compra acreditada | deprecada — 0030/0055 + futuro 0003 | no | `specs/0005-consola-de-personal-y-compra-acreditada.md` |
| 0006 | 2026-08-09 | Ruleta, cupones y canje atómico | deprecada — juegos fuera del roadmap; canje en 0055 | no | `specs/0006-ruleta-cupones-y-canje-atomico.md` |
| 0007 | 2026-08-09 | Tablero y medición del piloto | borrador | no | `specs/0007-tablero-y-medicion-del-piloto.md` |
| 0008 | 2026-08-09 | Administrador, rutas, categorías y eventos | diferida a largo plazo | no | `specs/0008-administrador-rutas-y-eventos.md` |
| 0009 | 2026-08-09 | Check-in QR por local | borrador | no | `specs/0009-checkin-qr-por-local.md` |
| 0010 | 2026-08-10 | Scaffold production grade de la plataforma | **implementada** (2026-09-15; estaba en `cerrada` por omision — el codigo esta vivo y en uso) | no | `specs/0010-scaffold-production-grade.md` |
| 0011 | 2026-08-10 | Prototipo QA de check-in consumer v0.1 | cerrada — superada (sin código vivo) | no | `specs/0011-prototipo-qa-checkin-consumer.md` |
| 0012 | 2026-08-10 | Onboarding demo de owner, negocio y sucursales | cerrada — superada por 0022 | no | `specs/0012-onboarding-owner-y-negocio-demo.md` |
| 0013 | 2026-08-10 | Home demo del Backoffice owner | cerrada — superada; el mock sigue servido en `/backoffice/demo` | no | `specs/0013-home-backoffice-owner-demo.md` |
| 0014 | 2026-08-10 | Marca de negocio demo | cerrada — superada por 0025 | no | `specs/0014-marca-negocio-demo.md` |
| 0015 | 2026-08-10 | Locales del owner demo | cerrada — **demo TODAVÍA EN USO**: es el destino real del tile «Locales» (tarea 47) | no | `specs/0015-locales-owner-demo.md` |
| 0016 | 2026-08-10 | Staff del owner demo | cerrada — superada por 0043 | no | `specs/0016-staff-owner-demo.md` |
| 0017 | 2026-08-10 | Campañas del owner demo | **superada por la spec 0065 (2026-09-16, fase B3)**: el tile «Campañas» apunta a `/backoffice/marketing`, una pantalla real; las rutas `/backoffice/demo/campaigns*` quedan sin entrada desde el backoffice. Su compositor en bloques cerrados fue la referencia visual del de la 0065 | no | `specs/0017-campanas-owner-demo.md` |
| 0018 | 2026-08-10 | Fundación UI production-grade de merchant demo | **implementada** (2026-09-15; el texto ya decia «implementada y en uso», el drift era la palabra clave del frontmatter, que es la que leen los barridos) | no | `specs/0018-fundacion-ui-merchant-demo.md` |
| 0019 | 2026-08-10 | Programa de fidelización del owner demo | cerrada — superada por 0024/0027 | no | `specs/0019-programa-fidelizacion-owner-demo.md` |
| 0020 | 2026-08-11 | Analíticas owner demo multirubro | cerrada — **demo TODAVÍA EN USO**: destino real del tile «Analíticas»; no hay analíticas reales (tarea 47/spec 0007) | no | `specs/0020-analiticas-owner-demo-multirubro.md` |
| 0021 | 2026-08-11 | Catálogo único de beneficios | diferida (con campañas) por ADR 0034 | no | `specs/0021-catalogo-unico-de-beneficios.md` |
| 0022 | 2026-08-11 | Registro, auth, suscripción y negocio inicial de Owner | **implementada** (2026-09-15; estaba en `cerrada` por omision — el codigo esta vivo y en uso) | no | `specs/0022-registro-auth-owner-suscripcion-y-negocio.md` |
| 0023 | 2026-08-11 | Búsqueda y procedencia de locales (Geoapify único; Mapbox retirado por costo 2026-08-16) | **implementada parcialmente** — sólo el alta en onboarding; no hay locales en el backoffice ni procedencia versionada (tarea 47) | no | `specs/0023-busqueda-y-procedencia-de-locales.md` |
| 0024 | 2026-08-12 | Programa de fidelización real y términos | implementada | no | `specs/0024-programa-fidelizacion-real-y-terminos.md` |
| 0025 | 2026-08-12 | Marca real del negocio y assets R2 | implementada | no | `specs/0025-marca-real-y-assets-r2.md` |
| 0026 | 2026-08-13 | Diseño de sello del programa de fidelización en R2 | implementada | no | `specs/0026-diseno-de-sello-en-r2.md` |
| 0027 | 2026-08-13 | Wizard de creación y diseño visual de la tarjeta de fidelización | implementada | sí | `specs/0027-wizard-y-diseno-de-tarjeta-de-fidelizacion.md` |
| 0028 | 2026-08-14 | Identidad de consumidor y enrolamiento (landing pública sin verificar, membresía aislada, QR personal) | implementada | sí | `specs/0028-identidad-de-consumidor-y-enrolamiento.md` |
| 0029 | 2026-08-14 | Pase de Wallet (Apple / Google): UN pase de identidad por consumidor (barcode = `qr_token`), "Ver mis programas", provider intercambiable | implementada | sí | `specs/0029-pase-de-wallet-apple-google.md` |
| 0030 | 2026-08-14 | Acreditación en mostrador: consola web móvil (URL backoffice, cámara) que escanea el QR, auto-enrola y acredita puntos/sellos por venta detallada (carrito) o rápida (importe+nota) — saldo por membresía + orden/ledger owner-facing, atómico e idempotente; **solo acreditación** (el canje es otra feature) | implementada | no | `specs/0030-acreditacion-en-mostrador.md` |
| 0031 | 2026-08-14 | Micro-portal del consumidor estilo iOS (único acceso hoy: agregar a inicio): nav inferior de 2 pestañas — Programas (tarjeta brandeada por membresía, orden por última actividad, filtro cerrados/inactivos, ícono info→T&C) y Mi QR (con PushPrompt); Mi QR primero hasta confirmar notificaciones. Reencuadrada 2026-08-16, la notificación en vivo ya la cubre 0030/0033/0038/0040 | implementada | sí | `specs/0031-notificacion-y-landing-en-vivo.md` |
| 0032 | 2026-08-14 | Recovery passwordless por OTP propio vía SMS: ClickSend y Twilio intercambiables (ClickSend activo), número existente recupera y rota credenciales; número nuevo pasa a onboarding verificado; 6 dígitos/5 min/2 intentos, un reenvío, límites persistentes 3/h y 5/día | implementada | no | `specs/0032-recuperacion-de-cuenta-y-verificacion-por-otp.md` |
| 0033 | 2026-08-14 | Canal de actualización y push de Wallet: web service PassKit + APNs + `addMessage`/`PATCH` de Google, alimentado por cola `wallet_push_queue` (outbox transaccional en el grant de 0030) con prioridad transaccional > campaña y cooldown; slot "Última novedad"; + mecanismo de rotación del pase (lo invoca 0032) | implementada | no | `specs/0033-canal-de-actualizacion-y-push-de-wallet.md` |
| 0034 | 2026-08-14 | Catálogo de productos del negocio (precio/coste opcionales, categorías, visibilidad por local, imagen R2) — alimenta el carrito de 0030 | implementada | sí | `specs/0034-catalogo-de-productos-del-negocio.md` |
| 0035 | 2026-08-14 | Imágenes de stock para productos (buscador Pexels server-proxied, import a R2 diferido, atribución) — extiende la imagen de 0034 | implementada | no | `specs/0035-imagenes-de-stock-para-productos.md` |
| 0036 | 2026-08-14 | Mecánica de acumulación (X por bloque de $Y, floor sin arrastre; Sellos "1 por compra") + premios del programa (producto catálogo / libre / % descuento, costo en puntos = gasto-objetivo) en el wizard — prerequisito duro de la acreditación 0030 | implementada | no | `specs/0036-mecanica-y-premios-del-programa.md` |
| 0037 | 2026-08-15 | Web Push (notificaciones de navegador, **iOS + Android**, ADR 0039): 2º transporte junto al pase para dar contenido controlado donde el banner de Google es genérico y para dar un **micro-portal instalable** en iOS; `web_push_subscription` + canal `webpush` (cripto `node:crypto`, verificada vs vector RFC 8291) + service worker + manifest + VAPID; en iOS dos contextos (landing Safari con escape hatch + PWA que suscribe), la página `(consumer)/wallet` existente hecha instalable; fan-out del transaccional + purge de suscripciones en la rotación del pase | implementada | no | `specs/0037-web-push-notificaciones-android.md` |
| 0038 | 2026-08-16 | Ruteo de notificación por **clase** (ADR 0040): el transaccional se entrega **solo por wallet** (Apple/Google) y cae a **Web Push por fallback** si no hay pase alcanzable — mata el duplicado del QA iOS (pase + Web Push). Web Push deja de ser transporte por defecto del transaccional. Suma en la confirmación del enroll el opt-in por plataforma: iOS mantiene el instructivo "añadir a inicio", Android suma el botón "Activar notificaciones" (única vía del fallback real). Sin migración ni secreto nuevo | implementada | sí | `specs/0038-ruteo-de-notificacion-por-clase-y-opt-in-android.md` |
| 0039 | 2026-08-16 | Branding de la landing de enrolamiento (`/enroll/[programId]` + confirmación): logo del negocio (ruta pública existente, sin exponer la clave R2) + color de marca en los botones de acción (form + "Activar notificaciones" + acento del instructivo iOS) con texto por luminancia; helper puro `readableTextColor`; fallback al nombre si no hay logo. NO re-diseña `/wallet` (arco 0031); componentes compartidos brandeados solo con prop `accentColor` opcional. Sin migración ni secreto nuevo | implementada | sí | `specs/0039-branding-de-la-landing-de-enrolamiento.md` |
| 0040 | 2026-08-16 | **Cropper de imagen en el cliente**, recuadro **1:1** con drag+zoom (touch y desktop) en las 3 superficies de subida (logo/sello/producto). El 1:1 no es preferencia: las 3 ya se renderizan cuadradas con `object-fit: cover`, así que el recorte **ya ocurre a ciegas** y el cropper sólo se lo da al usuario. `react-easy-crop` (una sola librería, no dos por plataforma), cargada con `dynamic import`; salida WebP 2048 px (PNG de respaldo donde hay transparencia). **Best-effort** por el ADR 0047: si el navegador no puede decodificar (HEIC fuera de Safari) se sube el original y el server hace lo de hoy, así que `limitInputPixels` queda estricto sólo en el camino con recorte. Orden marca → QA en teléfono real → sello → catálogo; ese QA produce el dato que decide si se reabre lo del decoder WASM. Incluye el arreglo del `accept` angosto del catálogo — que en la implementación resultó ser la **3ª, 4ª y 5ª** aparición: el guard nuevo era ciego a `accept="…"` y tapaba dos listas angostas más en `demo/brand` y `demo/loyalty`. Implementada **sin migración**: el flag `cropped` viaja en el payload de guardado, no en el presign | implementada | no | `specs/0040-recorte-de-imagen-en-el-cliente.md` |
| 0041 | 2026-08-16 | Brand kit — wizard en backoffice "Marca" que genera el afiche imprimible con el QR de enrolamiento. 3 pasos (elegir plantilla → chequear logo/colores → preview editable), 5 plantillas curadas por rubro pintadas con logo + colores de marca, QR server-side (lib `qrcode`, SVG, sin dep) con estilos básicos (negro/teñido/logo al centro a EC-H). Salida = HTML/SVG + CSS de impresión A4/A5 (imprimir → PDF del navegador). Alcance Global o por local (oculto con 1 local); el QR por local codifica `?loc=<localId>` y el alta lo persiste en `program_membership.origin_location_id` (columna nueva, migración aditiva) — primera pieza del ADR 0042. NO incluye tablero de stats ni canje | implementada | no (comparte `brand/page.tsx` con 0040) | `specs/0041-brand-kit-afiche-de-enrolamiento-por-local.md` |
| 0043 | 2026-08-30 | Login de staff + consola de mostrador (tajada del borrador 0005, implementa ADR 0044). El owner da de alta/desactiva personal (nombre/email/contraseña, rol `staff`) desde una pantalla "Personal" real que reemplaza el mock `demo/staff`. El staff logueado aterriza **solo** en la consola de mostrador: historial de acreditaciones **del día** (operador/cliente/programa/+puntos-sellos) + botón Escanear que lanza el flujo de acreditación existente (0030, sin cambios). Gating por rol (staff no ve marca/programa/catálogo/analytics/personal). Migración aditiva (`status` + CHECK de rol/estado). NO incluye canje de cupones (0006) ni atar staff a local | implementada | sí | `specs/0043-login-de-staff-y-consola-de-mostrador.md` |
| 0045 | 2026-08-30 | Landing de entrada: la raíz `/` (checkpass.club) deja de redirigir al form de registro y muestra una landing mínima (estructura, sin diseño) con "Acceder" (→ `/login`) y "Crea tu negocio" (→ `/onboarding`); con sesión sigue yendo a `/backoffice`. Login/registro ya existen y se enlazan. Solo estructura navegable | implementada | sí | `specs/0045-landing-de-entrada-y-acceso.md` |
| 0046 | 2026-08-30 | Recuperación de contraseña de **owner/staff por OTP de 6 dígitos al email** (Resend, intercambiable), usando el plugin `emailOTP` de better-auth para OTP y set de contraseña. Página `/forgot-password` (pedir código → código + clave nueva), resistente a enumeración, rate-limit persistente por email/IP, gate `PASSWORD_RECOVERY_ENABLED`, revocación de sesiones al resetear. Consumidor (SMS, 0032) intacto. Implementada con `disabledPaths` que cierra los endpoints HTTP del plugin (si no, el catch-all `/api/auth/[...all]` salteaba gate, rate-limit, staff deshabilitado y auditoría) y `middleware.ts` para el 503 de la página | implementada | sí | `specs/0046-recuperacion-de-owner-staff-por-otp-al-email.md` |
| 0047 | 2026-08-30 | **Deuda de formato y CI rojo.** `pnpm format:check` falla con 20 archivos sin formatear y es el **primer** paso de `.github/workflows/ci.yml`, así que el CI corta ahí y `lint`/`typecheck`/`test`/`test:e2e`/`build` **nunca corren en GitHub** (3 corridas seguidas en `failure` sobre `main`). Formatear los 19 versionados (el 20º, `.claude/settings.local.json`, es untracked por el ignore global del usuario → va a `.prettierignore`; explica el `19` del log de CI vs el `20` local), agregar el script `format` que falta en el root, y prevenir la recaída con la opción **(c)**: `format:check` pasa al **final** de `ci.yml` + hook `PostToolUse` `format-on-write.sh` que corre `prettier --write` sobre el archivo tocado (parado en la raíz del repo —prettier resuelve `.prettierignore` desde el CWD— y con guard de contención: no toca archivos fuera del repo). Sin cambio de comportamiento. Su último criterio (corrida de CI en `success`) se cumplió **vía la spec 0048**: con el CI por fin corriendo, `lint`/`typecheck`/`test` pasaron en GitHub por primera vez y el job murió en `test:e2e` por deuda destapada (browsers no instalados), que 0048 arregló | implementada | sí | `specs/0047-deuda-de-formato-y-ci-rojo.md` |

| 0048 | 2026-09-02 | **e2e en CI: los browsers de Playwright no se instalan.** El workflow nunca corre `playwright install` — `pnpm install` trae el paquete `@playwright/test`, no los binarios (`~/.cache/ms-playwright` vacío en un runner limpio), así que `test:e2e` muere con `browserType.launch: Executable doesn't exist` (3 pasan —los que no abren browser—, 2 fallan) y `build`/`format:check` nunca llegan a correr. Destapada por la 0047. **Ojo: es fallo de infraestructura del runner, no dice nada sobre si los e2e pasan** — nunca se ejecutaron con browser, ni en CI ni en local, así que arreglar la instalación puede destapar fallas reales. Fix: un step `pnpm exec playwright install --with-deps chromium` antes de `test:e2e` (sólo chromium: `playwright.config.ts` no declara `projects`; sin caché a propósito, para no sumar una pieza que dé falsos verdes). Los tests estaban **sanos**: 5 passed / 1 skipped corridos con browser real. El skipped (`loyalty-real`) es **opt-in deliberado** —muta datos reales, exige `E2E_LOYALTY_MUTATION_TEST=true`—, no deuda. **Cierra también el último criterio de la 0047: corrida `33579163212` en `success`, la primera verde en 101 corridas** | implementada | sí | `specs/0048-e2e-en-ci-browsers-de-playwright.md` |

| 0049 | 2026-09-02 | **Migración de Node a la LTS vigente** (implementa ADR 0046). Plan por fases, cada una un commit con su verificación y su rollback: (1) `.node-version` 24.19.0→**24.20.0** + `engines`; (2) `@types/node` 24.10.1→24.13.3 —la fase más riesgosa: tipos nuevos pueden romper `typecheck`, y es la única que mueve el lockfile (exige `pnpm fetch` con red después); (3) **guard anti-drift** sobre los 4 lugares donde vive la versión, probado rompiéndolo; (4) actions al día (checkout@v7, setup-node@v7, pnpm/action-setup@v6) para matar el warning de Node 20. **Node 26 NO entra** — Fase 5 agendada, gateada por que Vercel lo ofrezca. Producción no cambia: ya está en 24.x. Decisiones cerradas: guard como 4º project de vitest (corre con `pnpm test`, o sea también en el Stop hook — caza el drift en el turno en que se produce) y las 4 fases se ejecutan. **Las 4 aplicadas, cada una con su corrida de CI en `success`**; tests 254→259; prod desplegada y sana en 24.x | implementada | sí | `specs/0049-migracion-de-node-a-la-lts-vigente.md` |
| 0050 | 2026-09-05 | **El ícono instalado abre el wallet del consumidor**, no el formulario de registro (implementa el ADR 0048). Nace del QA en vivo del owner: instalar "Agregar a inicio" desde la confirmación del enroll dejaba el ícono apuntando a esa URL, porque esa página **no enlaza el manifest** — y al ir a arreglarlo apareció la falla de fondo (el manifest nunca recibía la sesión). `generateMetadata()` en `/wallet` emite el manifest con `?c=<token>`; la ruta valida el token y arma `start_url = /c/<token>`, degradando a `/wallet` si está ausente/inválido/revocado; la confirmación del enroll deja de mostrar el instructivo iOS y manda al usuario a su wallet. **No reescribe** el instructivo: ya vive en `/wallet` y ya se oculta en standalone (`isIosSafariBrowser`). El botón de push de Android en la confirmación se queda (no romper 0038). Sin migración. **Implementada con PASS a la primera**: el test del manifest sin cookies usa un `NextRequest` real y un mock de `next/headers` que TIRA si alguien llama `cookies()`; probe adversarial de 12 payloads (`https://evil.tld`, `//evil.tld`, `%00`, `javascript:`) → `start_url` siempre `/wallet`, cero reflejo | implementada | sí | `specs/0050-el-icono-instalado-abre-el-wallet-del-consumidor.md` |
| 0051 | 2026-09-05 | **El re-enroll no toca el perfil, y avisa con un toast.** Supersede al ADR 0050 ENTERO: el owner lo rechazó al verlo implementado. Enrolarse con un teléfono ya registrado NO modifica nada de la cuenta — la membresía se crea con los datos guardados y la confirmación avisa ("Ya tienes una cuenta con ese teléfono: te enrolaste en el programa con tus datos"). La confusión que originó el 0050 era de **información**, no de datos: se arregla avisando, no mutando. Se conserva el invariante "una operación no exitosa no deja efectos" | aceptada; supersede al 0050; habilita la spec 0054 | `adr/0051-el-re-enroll-no-toca-el-perfil-y-avisa-con-un-toast.md` |
| 0052 | 2026-09-05 | **El decoder HEIC en WASM queda cerrado, no diferido.** Cierra el punto 4 del ADR 0047, que dejaba la reapertura *condicional a un dato del QA que ningun test podia producir*. El dato llego: en un Android real, subir una foto de **galeria** en marca **abre el cropper** y el fallback **no se dispara nunca** — o sea el picker entrega algo decodificable (JPEG ya convertido) y **HEIC crudo no llega desde Android**. No se embarca WASM: se evitan 1-2 MB reales que pagaria el usuario de Android justo al esperar su foto, con LGPL-3.0 en las dos librerias mantenidas. **El fallback se conserva igual**: su justificacion nunca fue Android sino que el cropper es best-effort (0047 §1). Sin condicion de reapertura nueva; si algun dia un picker entrega HEIC crudo, el sintoma es el fallback disparandose y eso abre una spec nueva. **No cambia una linea de codigo** | aceptada; cierra el punto 4 del 0047 | `adr/0052-el-decoder-heic-en-wasm-queda-cerrado.md` |
| 0053 | 2026-09-07 | **El canje es un log propio y el saldo nunca queda negativo.** Cierra el agujero que dejaron dos specs cerradas y coherentes (la 0036 §8 delegó el canje en la 0030; la 0030 lo declaró fuera de alcance). Cuatro decisiones: (1) el canje ocurre en el **mostrador** —el operador escanea, el cliente elige, el operador confirma— y **no** por un inbox de solicitudes, porque el escaneo del QR **es** la prueba de quién reclama, que es justo lo que se quería auditar (el inbox queda aditivo); (2) tabla propia `core.reward_redemption`, **no** un `mode='redeem'` en `core.order` —los checks de `order` son de venta y, sobre todo, toda analítica tendría que acordarse de filtrar canjes: un olvido infla las ventas con premios regalados—; (3) el premio se **snapshotea** y su FK es best-effort, porque `saveProgram` borra y re-inserta los `loyalty_reward` en cada guardado y el `reward_id` **no es estable**; (4) el saldo **nunca** queda negativo, y el invariante vive en el `SET` (`GREATEST(saldo - costo, 0)`), no en una validación —los checks `>= 0` de 0030 quedan como red, no como mecanismo—: sin saldo el canje se bloquea, salvo dispensa **por programa**, y entonces el saldo cae a 0 | aceptada; consume 0037/0042/0044; cierra el hueco entre las specs 0030 y 0036; lo implementa la spec 0055 | `adr/0053-el-canje-es-un-log-propio-y-el-saldo-nunca-queda-negativo.md` |
| 0054 | 2026-09-07 | **La idempotencia no vive en un `NOT EXISTS` de un CTE.** La acreditación (0030) prometía idempotencia porque EvalPlanQual re-evaluaría ese qual bajo concurrencia; **verificado ejecutándolo: es falso** — Postgres lo planea como `InitPlan` + `One-Time Filter`, una vez y **antes** del lock. Carrera reproducida: saldo 100, dos requests con el MISMO `client_request_id`, +40 c/u → **saldo 180** con **una sola** orden diciendo `balance_after = 140`; el `ON CONFLICT DO NOTHING` se traga el 2º INSERT y **oculta el daño** (la API responde un saldo que no existe). Tres decisiones: (1) **fix mínimo del código vivo** — sacar el `ON CONFLICT DO NOTHING` para que el `23505` aborte y revierta el bump (`grant.ts:270` ya lo captura y re-lee); (2) **todo flujo nuevo que mute saldo usa transacción interactiva** (`withDbTransaction` + `SELECT … FOR UPDATE`), para que la corrección no dependa de una propiedad del planner **y** para que la función pura sea el decisor real en vez de una aritmética paralela al SQL; (3) el índice único es la **red**, no el mecanismo; (4) los tests de idempotencia aseveran el **saldo por SQL**, no la respuesta de la API — que en este bug miente | aceptada; corrige un invariante de la spec 0030; lo implementan las specs 0056 (grant) y 0055 (canje) | `adr/0054-la-idempotencia-no-vive-en-un-not-exists-de-un-cte.md` |
| 0055 | 2026-09-08 | **El rechazo por staff desactivado se dice en voz alta.** Sale del QA del owner (item C8 de la spec 0055). Hoy better-auth autentica contra `merchant_auth`, que **no sabe nada** de `core.business_membership`: el staff desactivado con la contraseña correcta **se loguea con exito y se le crea una sesion nueva**, y recien despues el guard lo rebota a `/login` **sin motivo**. Dos consecuencias: el silencio que el owner reporto, y una **sesion viva de un miembro desactivado** que contradice a `setStaffStatus` (que al desactivar borra todas las sesiones). Se decide **decirlo**: «Miembro del staff desactivado», y **revocar la sesion** en el mismo paso. **El tradeoff de enumeracion se acepta a proposito y por una razon concreta: el mensaje solo aparece DESPUES de una autenticacion exitosa**, o sea que quien lo ve ya probo conocer email y contraseña — no se abre ningun canal nuevo. Se descarta bloquear dentro de better-auth (`databaseHooks.session.create.before`): acopla `core` al ciclo de vida de `merchant_auth` y el mensaje habria que resolverlo igual | aceptada | `adr/0055-el-rechazo-por-staff-desactivado-se-dice-en-voz-alta.md` |
| 0056 | 2026-09-08 | **Checkout solo existe durante onboarding, antes del staff.** Se verifica el flujo: crear negocio inserta al owner antes de Checkout; el staff nace luego desde una ruta que exige owner; no existe UI de upgrade ni acceso del mostrador a ese flujo. El hallazgo se cierra como no aplicable. S4 sí se resuelve con **toast flotante** reutilizable, reemplazado por el error de reintento sin apilar mensajes | aceptada | `adr/0056-suscripcion-solo-la-administra-el-owner.md` |
| 0057 | 2026-09-08 | **Marketing por fidelización, geofencing y segmentación; no juegos.** Se acepta temporalmente el re-enroll sin OTP y se agenda su endurecimiento futuro. Las cargas de imagen muestran progreso y dan cámara móvil explícita | aceptada | `adr/0057-roadmap-de-marketing-y-captura-de-imagen.md` |
| 0058 | 2026-09-10 | **El cambio de plan es una seccion propia del backoffice, con bloqueo duro.** Hoy no existe ningun camino de upgrade/downgrade (`api/billing/checkout` es la unica ruta de billing y solo la llama el onboarding: un negocio que eligio `free` **no tiene forma de pagar**). Decisiones del owner: una **seccion de gestion de suscripcion** donde elige plan A → B; **upgrade por Stripe Checkout** como hoy, aplicado cuando el pago se confirma; **downgrade de bloqueo duro, sin estado intermedio** — si no cumple las condiciones del plan destino no se ejecuta, primero archiva los locales que sobran y elige cuales quedan (bloqueantes: **un local activo** + **sin campanas corriendo**, esta ultima feature todavia no existe y su chequeo lo agrega la spec de campanas, no esta); **boton de cancelar suscripcion** que baja a `free` al terminar el periodo pagado; y el **bug del webhook se arregla en este arco** (hoy escribe `plan: "plus"` para cualquier `customer.subscription.*`, incluido `deleted`, asi que una cancelacion deja el plan en `plus` para siempre). **Dos hallazgos a decidir, NO acordados:** (a) chequear el tope solo al apretar cancelar **no sostiene el invariante** — mientras siga en `plus` reactivar locales hasta 3 es una operacion valida (`locations/store.ts:70` compara contra el plan **vigente**), y al cerrar el periodo queda `free` con 3 activos; hay que sostenerlo tambien donde el plan cambia de verdad (el webhook); (b) **la premisa del ADR 0056 queda invalidada** — ese ADR no restringio checkout por rol argumentando que «no existe una UI de upgrade posterior», y esta seccion la crea: la ruta solo verifica que exista fila en `memberships`, ni rol ni `status`, y el staff nace en esa misma tabla, asi que un staff **incluso desactivado** podria cambiar el plan | **Ampliado el 2026-09-10 con dos decisiones mas del owner: (6)** el tope de locales se endurece — «no podes dar de alta ni desarchivar locales si el plan en el que estas no te lo permite», que es una regla de **transicion** (lo prohibido es *aumentar* los activos por encima del tope) y deja abierto solo el camino involuntario; **(7)** el orden queda cerrado: **el cambio de plan va antes que el arco de campanas**, con el numero de spec 0063. Ademas se corrigio una redaccion propia del ADR («sin estado intermedio», demasiado amplia: lo rechazado fue un «downgrade pendiente» que permita pedir la baja sin cumplir, no registrar que la suscripcion ya esta programada para terminar — que la decision 4 exige) y un hecho que el ADR daba por sentado y estaba **viejo** (A1 no tiene «locales de sobra»: tiene 1 activo y ninguna suscripcion de Stripe). **Cerrado el 2026-09-10 con cuatro decisiones mas: (8)** el boton de cancelar **nunca** se muestra bloqueado —«el usuario no sabria que debe hacer»— sino que abre un modal con lo que falta y «Confirmar» solo disponible si cumple; **(9)** el `plus` sin suscripcion de A1 se arregla **creando la suscripcion real en Stripe** (el `no_subscription` del codigo queda como guard defensivo, no como flujo); **(10)** el **cambio de intervalo mensual↔anual entra**, en las dos direcciones; **(11)** el impago **no baja el plan, bloquea el acceso** → ADR **0059**, que cierra el hallazgo del downgrade involuntario de este ADR; **(12)** «sin suscripcion» es un estado propio y **no es `free`** — «free es una suscripcion, plus es otra, enterprise es otra; si tenemos delete eso no es ninguna de las tres»: un `deleted` inesperado deja `plan='none'` y bloquea, y se sale ajustandose a free o pagando, lo que **cierra el invariante del todo** (no queda ningun camino a `free` sin verificar locales) | **SUPERADO EN DOS PUNTOS** (el ADR lleva los dos avisos al inicio): (1) el discriminante de la baja «esperada» del §12 → **ADR 0060**; (2) **la decision 6, la BAJA PROGRAMADA a fin de periodo → ADR 0063**, implementada por la spec 0064 con QA del owner OK — `resume` y «Reanudar» **ya no existen en el arbol**. El invariante de locales sigue sostenido por otro camino: si la baja es inmediata **no hay ventana** entre pedirla y que ocurra. Las doce decisiones de fondo siguen vigentes. aceptada; **supera al ADR 0056 en su punto de checkout** (el resto de 0056 intacto); lo implementa la **spec 0063**, salvo el punto 11 que va en la tarea 54 | `adr/0058-el-cambio-de-plan-es-una-seccion-propia-con-bloqueo-duro.md` |
| 0059 | 2026-09-10 | **El impago bloquea el acceso, no baja el plan.** Sale de una pregunta abierta de la spec 0063 (que hace el producto cuando el plan cae sin que nadie pase por la UI). Decision del owner: un cobro fallido espera **3 reintentos, los tres en la misma semana**, y despues **se bloquea el acceso** al backoffice, a la cuenta y al **mostrador**, con un modal que pide pagar para rehabilitar. El plan **no** baja y los locales no se tocan, asi que el «downgrade involuntario» deja de existir como camino y el invariante de locales de la 0063 se sostiene **sin** tolerar un sobre-tope. El disparador es el estado de Stripe (`unpaid`, o `past_due` con `next_payment_attempt: null` — `Invoices.d.ts:336`), **no** un contador propio. **Requisito de configuracion que hay que verificar o el ADR no se cumple:** el default de Stripe al agotar los reintentos es **cancelar**, lo que haria caer el plan a `free`; la cuenta tiene que dejar la suscripcion en `unpaid`. **No entra en la 0063**: el bloqueo vive en el guard compartido que usan 8 paginas y el mostrador (`requireBackofficeSession`), o sea el radio de daño mas grande del producto — va en la **tarea 54** con su propia spec. **Ampliado el mismo dia con la correccion conceptual del owner (ADR 0058 §12): quedarse SIN suscripcion tampoco es `free`.** «Free es una suscripcion, plus es otra, enterprise es otra; si tenemos delete eso no es ninguna de las tres» — asi que un `deleted` **inesperado** deja `plan='none'` y **bloquea**, con dos salidas: **ajustarse para bajar a `free`** (misma condicion de locales, mismo modal, y sin tocar Stripe porque no hay nada que cancelar) o **pagar**. Con eso el residual que este ADR declaraba abierto —cancelar desde el dashboard de Stripe— **queda cerrado**: ya no existe ningun camino a `free` que se saltee la verificacion de locales. **Hallazgo que salio de implementarlo, no decision del owner:** el `deleted` de **fin de periodo** es el final normal del flujo de cancelacion, asi que aplicarle `none` **bloquearia a quien hizo todo bien**. **[SUPERADO EN ESE PUNTO POR EL ADR 0060]** la version original decia que el discriminante era `pending_plan` y que «no hace falta ninguna columna nueva» — **era falsificable por el actor del que habia que defenderse**: `pending_plan` lo escribe tambien el webhook ante cualquier `cancel_at_period_end`, o sea tambien el boton del dashboard de Stripe. El discriminante real es **`downgrade_requested_at`**, que solo escriben nuestras rutas. El bloqueo tiene entonces **dos causas con salidas distintas** (impago → pagar; sin suscripcion → bajar a free o pagar), y **tiene que dejar pasar `/backoffice/subscription` y `/backoffice/locations`** o sus propias salidas son inalcanzables | aceptada; responde el hallazgo abierto del ADR 0058; la implementa la spec de la tarea 54, salvo la **salida** a `free` que entrega la 0063 | `adr/0059-el-impago-bloquea-el-acceso-no-baja-el-plan.md` |
| 0060 | 2026-09-11 | **La baja esperada se prueba con una marca que solo escribimos NOSOTROS.** Supersede **un punto** del ADR 0059 §5 y del ADR 0058 §12: el discriminante de «esta baja la pedimos nosotros» **no** es `pending_plan`, es **`downgrade_requested_at`**. `pending_plan='free'` lo escribe el **webhook** ante cualquier `cancel_at_period_end`, que es justo lo que setea el boton «Cancel subscription» del **dashboard de Stripe** — asi que era **falsificable por el actor del que habia que defenderse**, y cancelar desde el dashboard con 3 locales activos aterrizaba en **`free` con 3 activos**, el estado que todo el arco existe para prohibir. **La decision de producto NO cambia** (un `deleted` inesperado sigue yendo a `none` y bloqueando, con sus dos salidas); cambia **como se prueba la intencion**. **La regla general, que es lo que hay que llevarse: un discriminante de intencion no puede leerse de un campo que el otro lado tambien escribe — la pregunta no es «¿distingue los casos que se me ocurren?» sino «¿quien mas puede escribir esto?».** Reusar una columna existente «para no agregar una nueva» es la forma que toma este error **y se siente como economia**: la columna costaba una linea de migracion, no tenerla costaba el invariante central. Lo cazo un revisor independiente en la 2a ronda de la spec 0063, y el **drift residual en los dos ADR viejos** lo cazo otro revisor en la fase B — por eso este ADR existe. Los dos ADR superados llevan el aviso **al inicio**, diciendo exactamente que dejo de valer y que sigue vigente | aceptada; **documenta lo que el codigo YA hace** (migracion `0030` + fases A y B de la spec 0063, las dos con PASS de revisor) | `adr/0060-la-baja-esperada-se-prueba-con-una-marca-que-solo-escribimos-nosotros.md` |
| 0061 | 2026-09-11 | **La entrega que se retira no contesta 2xx.** El owner decidio (2026-09-11) **cerrar el solape** de dos entregas simultaneas del webhook en vez de aceptarlo como limite: entra como **fase C** de la spec 0063 y lo que era la fase C (rutas + UI + D8 + D10) pasa a **fase D**. **El ADR existe por lo que se encontro al implementarlo, no por la decision:** el lease sobre `received_at` que la fase B habia propuesto —y medido— era, tal como estaba escrito, **una REGRESION**. Su trade-off declarado decia que un reintento rechazado por el lease «esperaria al reintento siguiente»; pero ese rechazo contestaba `{duplicate:true}` con **HTTP 200**, y para Stripe cualquier 2xx es entrega exitosa: **no hay reintento siguiente**. Un evento cuya primera entrega muriera no se habria procesado **nunca** — el bug del §Problema-4 que la spec 0063 vino a matar, por la puerta de atras, y peor que no hacer nada (hoy el solape deja el estado final correcto porque el `UPDATE` es idempotente). La regla que ordena el arreglo: **un 2xx es una promesa de «este evento ya no es tuyo», y retirarse porque otro lo tiene tomado no es terminarlo.** El claim pasa a tener **tres** resultados con tres respuestas: `claimed` (sigue), `already_processed` (**200** `{duplicate:true}`, sin cambios) e `in_flight` (**409**, para que Stripe reintente). **Es `CLAUDE.md` en la variante del COSTO declarado, y en segunda vuelta:** el parrafo que este ADR corrige habia nacido corrigiendo un limite sobredimensionado y, al corregirlo, fijo un precio nuevo («una linea de SQL mas su test») que **tampoco se verifico** — sub-corregir se siente como rigor y deja el mismo agujero mas chico. Anota ademas por que el timestamp de Stripe **no** reemplaza al lease (lo pregunto el owner): `event.created` es del EVENTO e identico en las dos entregas, y el `t=` de la firma —que si es por entrega (verificado en `stripe@22.5.0`, `esm/Webhooks.js:208`)— no aporta nada sobre `received_at`, porque **un timestamp dice cuando empezo la primera, no si sigue viva** | aceptada; **premisa declarada y NO verificable por nosotros**: que Stripe deja de reintentar ante un 2xx no lo puede pinnear ningun oraculo de este arbol — lo que se pinnea es el status que devolvemos nosotros | `adr/0061-la-entrega-que-se-retira-no-contesta-2xx.md` |
| 0062 | 2026-09-12 | **Lo que cruza al cliente se pinnea por VALOR EXACTO y en UNA SOLA LECTURA.** Decision de metodo, no de producto: como se prueba que un server component no filtra datos internos al navegador. El oraculo se escribio **seis veces** y las cinco primeras estaban VERDES sin cerrar la propiedad — **cinco de las seis las cazo un revisor independiente**, y las dos ultimas las ABRIO el intento de cerrar la anterior. (1) El **render del HTML es un proxy**: `renderToStaticMarkup` **no emite el payload RSC**, asi que bajar la fila CRUDA dejaba **66/66 en verde** — pinneaba «la consola no IMPRIME las claves», no «la pagina no las PASA». (2) **`JSON.stringify` de las props tambien**, y su preimagen es **idiomatica**: un `Map` da `{}` y `Promise.resolve(row)` es el idiom de Next 15 (`searchParams`), y **Flight SI manda Map/Set/Promise**. (3) Una **allow-list de CLAVES no ve el valor**: un `cus_…` en **base64** bajo la clave permitida `notice` pasa la lista, la planitud y el substring — **todo guard por forma o por substring tiene preimagen por transformacion**. (4) Y un `toEqual` exacto **tampoco cierra si LEE DOS VECES lo que Flight lee UNA**: un **getter con estado** o un **`Proxy`** devuelven el secreto en la 1a lectura y `"Plus"` en la 2a → 15/15 verde con la fuga viva. **La regla: el oraculo tiene que leer igual y la misma cantidad de veces que el consumidor real** — de ahi `structuredClone` como lectura unica, que ademas mata al `Proxy` con `DataCloneError`. (5) **Y aun asi faltaba un canal ENTERO: `element.key`** — Flight serializa el ELEMENTO (`type`, `key`, `props`), el `key` **no vive en `props`** ni en su clon y **tampoco se renderiza a HTML**, asi que era invisible para las aserciones y para el test del markup **a la vez**: con `key={row.stripeCustomerId}` el alcance quedaba **15/15 verde** y el wire salia `["$","div","cus_secret_ZZ9",{…}]`. La leccion es de **ALCANCE**, no de forma — aseverar exhaustivamente un objeto no dice nada de lo que vive **al lado**. (6) **Y la sexta la abrio el fix de la quinta: el 2o estado se aseveraba PARCIAL** («el conjunto entero no entraba en el archivo»), asi que un secreto en otra prop gateado a ese estado —o dentro de `downgradeBlock.message`, respetando el `stringContaining`— dejaba **44/44 verde**, y el primero **ademas se imprimia en el HTML**. **Un oraculo parcial en un estado nuevo no es cobertura incompleta: es una puerta**, y su sintoma es PRESUPUESTARIO («no entra»), no conceptual — de ahi que las 4 aserciones vivan en un HELPER, para que repetirlas enteras sea mas BARATO que recortarlas, con cada estado en su propio `it` (el primer rojo corta) y sembrado con claves REALES (sin ellas la fuga del `key` solo exhibe el string `"null"`). Y antes de creerle a un guard sobre serializacion, **medi que manda el serializador de verdad** (sonda sobre `react-server-dom-webpack`), o no distinguis una **fuga** de un **limite** | aceptada; **los 4 puntos aplicados al arbol y cada mutacion EJECUTADA con su asercion transcrita** (sesion B-ter: el `key` da DOS rojos con DOS secretos reales, `cus_props_…` y `cus_bloq_…`). El ADR ademas se corrige a si mismo DOS veces: su 1a version afirmaba en absoluto que contra un `toEqual` del objeto entero «no hay clave de mas, valor de mas ni transformacion que pase» **sin nombrar sobre QUE lo cerraba** (por ahi entro la 5a preimagen), y la 2a **declaraba aplicado un requisito que no lo estaba** y citaba una asercion inflada — el 2o estado no tenia claves sembradas y encima compartia el `it`, asi que ni se evaluaba | `adr/0062-lo-que-cruza-al-cliente-se-pinnea-por-valor-exacto-y-en-una-sola-lectura.md` |
| 0063 | 2026-09-13 | **La baja de plan es INMEDIATA y sin devolucion.** Lo cazo el **owner en el QA de prod**, no una revision: la 0063 programaba la baja a fin de periodo pero bajaba el tope de locales **al momento de programarla**, asi que el merchant **pagaba Plus hasta la fecha y desde el minuto cero solo podia usar 1 local**. Es una clase de defecto que la revision por mutacion NO encuentra —ningun invariante estaba roto; **cada regla cumplia su contrato y la COMPOSICION era incoherente**—. Se cancela en el acto, **sin devolver ni acreditar** (decision literal del owner), y el diferimiento se reemplaza por un **aviso** de cuando conviene bajar (2 dias antes de la renovacion). Verificado antes de decidir: `subscriptions.cancel` trae `prorate` en **`false` por default**, o sea que cancelar sin parametros es exactamente lo pedido; y ojo con la palabra «reembolso» —el prorrateo de Stripe **acredita saldo**, no devuelve a la tarjeta, y son productos distintos—. **Efecto lateral que hay que mirar: «Reanudar» sale de nuestro flujo, pero el estado diferido puede seguir LLEGANDO desde el dashboard de Stripe** (mismo hecho del ADR 0060), asi que `min(vigente, pendiente)` se queda | aceptada; **supersede la decision 6 del ADR 0058**; **IMPLEMENTADO por la spec 0064** (commit `ca2d746`, `Vercel: success` para ese sha) con **QA del owner en prod EN VERDE** (2026-09-15). **Las tres decisiones del ORQUESTADOR (O-2, O-3, O-4) quedaron ACEPTADAS** por decision del owner del 2026-09-15 («si estan implementadas y funcionando… marcalas como terminadas»): **en bloque y sobre el producto ya funcionando**, no evaluadas una por una, con O-3 presentada como contraria a un pedido literal suyo. Son decisiones vigentes; cambiarlas exige spec nueva. **Nota operativa:** el camino de O-3 no lo ejercito nadie — `A3 Test` cae el 13-10, y si ese dia no aterriza en `free` limpio el sospechoso es O-3, no un bug nuevo | `adr/0063-la-baja-de-plan-es-inmediata-y-sin-devolucion.md` |
| 0064 | 2026-09-15 | **El motor de marketing arranca por AUDIENCIAS, un tipo de campaña por spec, y sobre la base PROPIA.** Dos fases: primero un motor de audiencias (consulta sobre el comportamiento + mensaje + efecto opcional, evaluada por el PASO DEL TIEMPO), despues el motor reactivo del ADR 0018 — porque los tres disparadores del 0018 exigen que alguien HAGA algo y el ejemplo del ADR 0057 («N dias sin visita») es la AUSENCIA de un evento, inexpresable en un motor reactivo (verificado contra spec 0003 y el arbol: no existe ninguna tabla de campañas). Cada tipo de campaña tiene su spec + ADR; la primera es proximidad por wallet (0065) y fija el journey de composicion + resultados que heredan las demas. Los $20 compran el motor sobre `program_membership` del negocio; la red cruzada es otro producto con otro precio (decision del owner; linea Fivestars software/Love Local Network). El cupon es un EFECTO con costo declarado. Medicion por contrato del ADR 0021: «compro en ventana» observada, «lo trajo la campaña» solo con holdout. Sin email; fecha de nacimiento mas adelante; categoria del negocio falta. Corrige una afirmacion del orquestador: el ADR 0057 §1 (OTP) NO se gatilla — el canal entrega al pase, no al numero | aceptada | `adr/0064-el-motor-de-marketing-arranca-por-audiencias-y-sobre-la-base-propia.md` |
| 0065 | 2026-09-15 | **La proximidad por wallet es un TURNO ROTATIVO con separacion geografica.** El pase es UNO por consumidor (ADR 0033) y admite ≤10 ubicaciones (Apple `locations` con `relevantText`; Google `merchantLocations`, sin texto, radio de Google, exige *dwell*), asi que son un recurso escaso POR CONSUMIDOR. Dos bolsas: UTILIDAD (≤3 locales con relacion viva, texto = su propio saldo, siempre encendida, sin turno ni cuota) y REACTIVACION (≤5 TURNOS de 5 dias sobre relaciones dormidas). El turno es un PRESTAMO: lo gana quien NO tiene al cliente; ≥400 m entre turnos activos, 1 por negocio, FIFO puro, cooldown 30 dias, cuota de 50 turnos CONCURRENTES por negocio (no «campañas/mes»), holdout 10 % como unico oraculo. Nadie «lanza»: no hay boton de enviar, la cola coloca. Refresco silencioso por clase nueva `pass_refresh` (no toca `last_push_at`, no posterga campañas — el camino actual de `rotate.ts` SI lo haria). **Tres ideas murieron por MEDICION y quedan como descartadas**: «zona» (`maxDistance` es del PASE y solo ACHICA), ranking por frecuencia (le da el slot a quien no lo necesita — lo cazo el owner) y «1 turno a la vez» (confundia ocupacion con exposicion, desperdiciaba 7 slots — lo cazo el owner). Merito por resultado NO en fase 1 para proximidad: sin reporte de impresion (ninguna plataforma lo da) la metrica esta sesgada y reintroduce el bug de la frecuencia (ejemplo numerico en el ADR). Limites declarados: la exposicion solo se controla al COLOCAR; en Android el mensaje no viaja en el aviso; el tope de 4/dia de Android es fuente secundaria y NO es load-bearing | aceptada, **con la decision 4 SUPERSEDIDA por el ADR 0066** (el merito rige desde el dia uno); parametros numericos del orquestador, configurables | `adr/0065-la-proximidad-por-wallet-es-un-turno-rotativo-con-separacion-geografica.md` |
| 0066 | 2026-09-15 | **El merito en proximidad rige desde el DIA UNO, y se mide por LIFT, no por tasa cruda.** Decision del owner, que supersede la decision 4 del ADR 0065 («cola por rotacion pura, merito recien cuando el holdout de señal»). El argumento numerico del 0065 era correcto pero apuntaba al blanco equivocado: refuta la **tasa cruda** de «compro en su ventana» (A mide 33 % generando +3 y le gana a B, que mide 10 % generando +8), **no** el merito — y su propia ultima frase, «solo el holdout separa esos dos numeros», es la receta, porque el holdout se retiene desde el primer turno. Se rankea por `lift = tasa_colocados − tasa_holdout` (33−30=+3 vs 10−2=+8 → gana B, que es lo correcto), con **encogimiento hacia el lift promedio global** (`(lift·n + globalLift·α)/(n+α)`, α=20 turnos) para que un negocio sin historia arranque **en el medio de la tabla y no ultimo** — la «balanza con piso para debutantes» del ADR 0064 §6 — y **FIFO como desempate**, que es el criterio efectivo las primeras semanas. El score se calcula en SQL y entra **inyectado** al planner puro. Limites declarados: sigue sin haber impresiones (el lift mide «compro», no «vio»); el sesgo de transito se cancela **en parte** porque el holdout es del mismo negocio, y eso es **un argumento, no una medicion**; el ranking no se muestra en el backoffice | aceptada | `adr/0066-el-merito-en-proximidad-rige-desde-el-dia-uno-y-se-mide-por-lift.md` |
| 0067 | 2026-09-16 | **El tick de marketing se serializa con un lock de TRANSACCION, no de sesion.** La spec 0065 (paso 0) pide `pg_try_advisory_lock`, que es de **sesion** y exige que toda la corrida viaje por la misma conexion — algo que este repo no puede prometer: `getDb()` es el driver HTTP de Neon (una conexion por sentencia) y el pool de WebSocket solo fija un cliente **mientras dura una transaccion** (`server/db.ts`). Asi que el tick entero corre dentro de UNA `withDbTransaction` y toma `pg_try_advisory_xact_lock(hashtextextended('marketing_tick', 0))`; se libera en el commit y no hay ningun `unlock` que se pueda filtrar. **Costo declarado:** los `select … for update` por consumidor quedan tomados hasta el final de la corrida, asi que un tick largo bloquea a quien escriba `consumer_account`; cuando eso duela, la salida es fijar una conexion de sesion, **no** aflojar el lock. Beneficio no buscado: la corrida es atomica. Y como la exclusion es **global por diseño**, las suites de integracion en paralelo se contestaban `tick_in_flight` entre si (**medido**: 17 rojos en la corrida completa, cero en aislamiento) → `TickOptions.lockNamespace`, el unico costurón de test del modulo que puede debilitar una garantia de produccion, documentado como tal (la ruta llama sin argumentos y la suite que asevera el skip usa el default) | aceptada (decision del ORQUESTADOR; el owner puede revertirla) | `adr/0067-el-tick-de-marketing-se-serializa-con-un-lock-de-transaccion.md` |
| 0068 | 2026-09-17 | **Cierre de la API de staff**: `GET /api/staff`, `email` fuera del `StaffDTO` (es el sintetico `@staff.invalid`), el guard adentro del `try` (afuera daba 500 sin `code`, contra lo que el contrato declaraba) y el borrado de `emailOTP`. **El gate de email en las otras 9 superficies de API lo saco el owner del alcance** → `PARQUEADO.md` fila 56. Un **FAIL** real del revisor: el DoD pedia un oraculo contra la fuga del sintetico y no existia para 2 de las 5 rutas | implementada (PASS 2026-09-17) | sí | `specs/0068-cierre-de-la-api-de-identidad.md` |
| 0069 | 2026-09-16 | **El harness se repara ANTES del arco de alta.** Medido: `CLAUDE.md` = **696 lineas / 61 KB** cargadas en CADA request; `docs/TASKS.md` = **7.197 lineas / 712 KB**, con la cabecera que ya mintio dos veces; `.claude/agents/` **no existe**, asi que el protocolo de mutaciones se reescribe a mano en cada encargo; y los worktrees son inutilizables porque `pnpm run` intenta purgar el `node_modules` symlinkeado. El costo compuesto esta cuantificado: la cola de revisiones de la 0065 corrio 4 revisores, dio 14 hallazgos y **UNO** cambio codigo de produccion. La doc oficial de Claude Code nombra las dos causas como patrones de falla (el `CLAUDE.md` inflado hace que se ignoren las reglas; un revisor al que se le pide huecos siempre reporta huecos) — el segundo es palabra por palabra el ADR 0062 de este repo. Decision del owner: (1) podar a <=200 lineas mudando a skills y `docs/LECCIONES.md`, y dejar en `TASKS.md` solo lo que esta en ejecucion; (2) `.claude/agents/` con el presupuesto y la condicion de corte ADENTRO, para que el corte no dependa de que el orquestador se acuerde; (3) worktrees con `node_modules` propio offline desde el `.pnpm-store` que ya vive en el repo — que es lo que hace que la muerte de un agente deje de contaminar el arbol de todos. Recien despues: paralelo por **slice vertical** (techo 2-3, arriba de eso el cuello es la revision humana) y **oraculo antes que codigo**. Riesgo declarado: podar puede perder una leccion, por eso nada se borra y la spec exige demostrarlo por script. NO se tocan los 10 hooks: son la parte que funciona | aceptada | `adr/0069-el-harness-se-repara-antes-del-arco-de-alta.md` |
| 0070 | 2026-09-16 | **El alta del comercio es un WIZARD de tres pantallas** (entrar · negocio+local · programa) que termina en una pantalla con **el QR listo** y el programa de fidelizacion YA ACTIVO; el onboarding es posterior, guiado y no bloqueante, con el progreso **DERIVADO de los hechos de la base** y no de una columna `onboarding_step`. Decisiones del owner: no se elige plan (todos `free`, «friccion innecesaria»); **no hay contraseña** — el owner entra con el email y **queda logueado al registrarse**, verifica el email para el onboarding y usa **link magico** despues; el **staff NO usa email**: handle por negocio (`@lafarmacia`) + `nombre@lafarmacia` + **PIN de 6 digitos** que el owner genera, el staff cambia en su primer login y el owner regenera si se pierde; marca sin logo ni colores con «marca avanzada» opcional, y **sello placeholder nuestro** hasta que suba el suyo; **categoria OBLIGATORIA** guardada como `gcid:` de Google; pais prellenado (timezone del dispositivo → IP) pero **editable**, + Mexico; los limites por plan en **una** capa de entitlements (hoy estan en 3 lugares y ya divergieron una vez); y **la base se borra entera**. Cuatro hechos medidos abarataron el plan: la marca ya nace con colores default, el sello ya es nullable **y su ruta publica ya existe** (el placeholder se implementa ahi y lo reciben Wallet, tarjeta y preview sin tocarlos), y el plan ya nace `free` en el servidor. **El handle del staff y la URL publica futura (`checkpass.club/es/lafarmacia`) son el MISMO slug**: se diseña una sola vez. Consecuencia que se ejecuta: sin contraseña **se borra** todo el arco de recuperacion. Google Business queda SEPARADO —reemplaza la pantalla 1 y autocompleta el wizard— y su reloj largo no es codigo: exige un GBP verificado **60+ dias**. **4 hallazgos a decidir, NO acordados**: que bloquea exactamente la falta de verificacion del email, las reglas del slug, la defensa del PIN (6 digitos = 1M, forzable sin rate limit) y el pais fuera de la lista — **los 4 se cerraron con el owner el mismo dia (secciones 11-14)**: la verificacion de email no bloquea el alta pero **bloquea todo el onboarding** y es su primer paso (motivo de negocio: staff hoy gratis puede pasar a ser pago); el **slug NO sigue al nombre**, se cambia por accion explicita con chequeo de disponibilidad; el **PIN** va con bloqueo escalado propio (5 fallos → 15 min, 3 mas → 1 h, el siguiente → 24 h), hasheado, visible UNA sola vez al generarlo y despues solo regenerable — **medido que el rate limit del plugin `username` NO sirve**: no trae reglas propias, la global `/sign-in*` (3/10 s) es `(IP, path)` asi que el staff de un local se bloquea entre si y un atacante que rota IPs no tiene limite por cuenta, el storage es `memory` (se evapora en lambda, no hay donde persistir el escalado) y `enabled` = `isProduction` la apaga en test; ~26k intentos/dia por IP = <10 h con 100 IPs contra 1M de combinaciones; el plugin ademas publica `/is-username-available`, un enumerador gratis de handles; y el **pais se prellena pero el selector ofrece SIEMPRE la lista completa** (VPN), sin caso de pais no soportado porque por ahora es solo LATAM. **§15 — tres confirmaciones del owner**: el escalado del PIN tal cual; un email existente **no abre sesion** y manda link magico (nacio como requisito tecnico del orquestador y el owner lo adopto); y **el slug no se escribe en el alta** —el wizard no expone el campo— mientras que dar de alta a un integrante es escribir **solo el nombre**, y el servidor agrega el `@` y el slug **tomandolo de la sesion, nunca del body** (si viajara en el body seria un parametro apuntable al negocio de otro). **§16 — el arco entrega API, no UI**, y eso incluye el **contrato HTTP escrito**: se subio a decision explicita porque el orquestador se la salteo listando dos pantallas como archivos a editar, con la restriccion copiada en la prosa de la misma spec (caso en `LECCIONES.md`, regla en `CLAUDE.md`). **§17 — la UI vieja de lo que se refactoriza se BORRA**, decision del owner contra la recomendacion del orquestador: «no dejar rastros viejos de lo que ya no usaremos», porque el contrato de API **es** lo que le va a entregar a ChatGPT para que construya la UI nueva. Eso convierte al contrato en el entregable critico del arco —un contrato incompleto no produce una UI incompleta, produce una UI **inventada**— y tiene un costo que el owner acepta: mientras dure el arco **no hay entrada por navegador y por lo tanto no hay QA de pantalla**, la excepcion explicita a «gana la pantalla». La limpieza de enlaces muertos es **parte** del borrado | aceptada — **la spec 0067 existe, en `borrador` y sin bloqueantes** | `adr/0070-el-alta-del-comercio-es-un-wizard-de-tres-pantallas.md` |
| 0071 | 2026-09-17 | **El proceso se recorta.** Medido: los gates cuestan <1 min; el tiempo se iba en 493k tokens de contexto re-leido y prosa. Spec chica para un dominio sin esquema, decisiones del owner antes de la prosa, UN implementador y UN revisor por spec, filas de INDEX de 3 lineas, gates una vez por spec. **No se toca el protocolo de mutaciones ni la revision independiente** | aceptada | `adr/0071-el-proceso-se-recorta-spec-chica-y-un-solo-ciclo-de-revision.md` |
| 0073 | 2026-09-17 | **El estado del negocio y el plan son dos ejes que no se mezclan.** Decision del owner: «si yo suspendo un comercio no es cambiar de plan, es dejar de acceder a las funciones». Se evaluan en orden fijo (sesion → owner → email → **status** → **plan**) y `can()` NUNCA mira `status`: mezclarlos hace que la superficie diga «mejora tu plan» a un negocio suspendido, el mismo error que el ADR 0063 [R2] ya corrigio en locales. El catalogo de entitlements declara `requiresLiveSubscription` **por entrada** en vez de derivar un `effectivePlan` unico, porque la asimetria es deliberada: un `plus` sin suscripcion viva (forma A1) no activa campañas pero **si** conserva sus 3 locales, para poder archivar y salir. Agregar un limite es UNA fila, y un plan sin fila **pone la suite en rojo** en vez de aterrizar en el tope mas restrictivo en silencio. Consecuencia medida: el guard de `status` va en CADA superficie, no en el login — better-auth 1.6.26 da sesiones de **7 dias** por default, asi que cerrar la puerta no expulsa a quien ya entro | aceptada | `adr/0073-el-estado-del-negocio-y-el-plan-son-dos-ejes-que-no-se-mezclan.md` |
| 0051 | 2026-09-05 | **Instalar desde la confirmación del enroll** (implementa el ADR 0049). Restaura el flujo que el owner pidió: enrolarse → UNA pantalla con felicitación + instructivo "Agregar a inicio" + botón de Apple Wallet → el ícono instalado ahí abre MI wallet. El 201 del enroll suma `walletManifestPath` (manifest con `?c=<token>`, armado server-side con el token de la base) y la confirmación lo inyecta en el `<head>`; antes del 201 la página sigue sin manifest. El instructivo iOS vuelve **directo** (desacoplado de VAPID, como pre-0050); `PushPrompt` de Android intacto (0038). Reescribe `enroll-install-hint.test.ts` con autorización (cambio de requisito, no maquillaje). Plan B si Safari ignora el link inyectado: confirmación server-renderizada (ADR 0049). **Implementada con PASS a la primera**: invariante blindado por mutación (path en la rama de error → 5 rojos); la sesión del 201 es preexistente y ya exponía el token vía el HTML de `/wallet`, así que el campo no amplía poder | implementada | sí | `specs/0051-instalar-desde-la-confirmacion-del-enroll.md` |
| 0052 | 2026-09-05 | **El probe de decode cae en falso fallback en iOS (galería).** Bug de la 0040 cazado por el QA: con la **cámara** el cropper funciona perfecto, con la **galería** no aparece nunca — ni con un PNG. Ese contraste absuelve al chunk, a react-easy-crop y al canvas: el falso negativo está en `canDecodeImage`. Sospechosos: `img.decode()` que rechaza espuriamente en WebKit (y nuestro probe le da veto) y la carga flaky por blob URL del Photo Library (que además puede COLGAR `choose()`). Fix por capas: decode() pierde el veto, retry con data URL, timeout de 8s, y el probe devuelve el src utilizable para que el cropper consuma el mismo camino que funcionó. **Implementada con PASS**: el revisor aplicó 6 mutaciones (devolver el veto de `decode()` → 6 rojos) y verificó que el **fallback real sigue vivo** — el riesgo grande era volver "decodificable" a todo y romper la 0040/ADR 0047 | implementada | sí | `specs/0052-el-probe-de-decode-cae-en-falso-fallback-en-ios.md` |
| 0053 | 2026-09-05 | **El re-enroll actualiza el nombre** (implementa el ADR 0050). `enroll()` reutiliza la cuenta por teléfono y descartaba el nombre tipeado; ahora lo actualiza, también en el camino de carrera concurrente. No toca teléfono, país ni tokens. **El nombre se escribe SÓLO si el alta de membresía tuvo éxito**: un enroll que termina en 409 `already_member` deja la cuenta intacta (la primera implementación actualizaba y después rechazaba — el owner lo rechazó, ver ADR 0050). Test de integración Neon comparando la fila entera (13 columnas) antes/después, rojo contra el orden viejo | implementada y **REVERTIDA por la 0054** (ADR 0050 supersedido por el 0051) | sí | `specs/0053-el-re-enroll-actualiza-el-nombre.md` |
| 0054 | 2026-09-05 | **El re-enroll usa los datos guardados y avisa** (implementa el ADR 0051, revierte la 0053). `enroll()` vuelve a no escribir en `consumer_account`; el 201 suma `existingAccount` y la confirmación muestra el toast "Ya tienes una cuenta con ese teléfono: te enrolaste en el programa con tus datos" sólo en ese caso. Se conservan el test de que el 409 no modifica la fila y los de tokens intactos. **Implementada con PASS sin hallazgos**: reintroducir el update de la 0053 pone 4 unit + 3 Neon en rojo; `enrollment.ts` achicó 286→252 líneas | implementada | sí | `specs/0054-el-re-enroll-usa-los-datos-guardados-y-avisa.md` |
| 0055 | 2026-09-07 | **Canje de premios en mostrador — cierra el loop del producto** (tarea 44). Hoy el owner configura premios que nadie puede canjear: el agujero está ENTRE la 0030 ("el canje es otra feature") y la 0036 ("la ejecución del canje es de la 0030"), dos specs cerradas y coherentes. Flujo: el mostrador escanea el QR, el cliente elige premio, el operador confirma → Puntos resta `points_cost`, Sellos consume `target` **con arrastre** (12 en tarjeta de 10 → quedan 2). Log de contabilidad propio `core.reward_redemption` (**no** `mode='redeem'` en `core.order`: sus checks son de venta y toda analítica tendría que acordarse de filtrar canjes) con **snapshot** del premio — `saveProgram` borra y re-inserta los `loyalty_reward` en cada guardado, así que el `reward_id` NO es estable. Débito atómico con el guard de saldo **dentro del UPDATE** (sin TOCTOU) + idempotencia por `client_request_id`, patrón de `persistGrant`. Un premio por operación; push transaccional; saldo insuficiente = **configuración avanzada por programa**; catálogo de premios en el wallet (el `i` pasa a ofrecer Términos + Catálogo). Descarta el *inbox* de solicitudes (opción 2): no quita fricción, la mueve, y la solicitud no prueba quién está en el mostrador — el escaneo sí. Queda **aditiva** si algún día se quiere | **IMPLEMENTADA, con PASS de revisor independiente, PUSHEADA Y DESPLEGADA** (2026-09-08, commit `a9cbf3f`, `Vercel: success` para ese sha exacto; migracion `0028` aplicada a prod y verificada por SQL: `core` 22→23 tablas, `consumer` y `merchant_auth` **sin cambios**, 5 programas / 11 membresias / 7 ordenes intactos). **El revisor corrigio la propia spec:** su plan de pruebas predecia que sacar el `FOR UPDATE` ponia roja «la carrera de mismo `clientRequestId`» — **es FALSO, esa carrera queda VERDE**, porque ahi el indice unico solo ya alcanza (el `23505` aborta y revierte el `UPDATE` del saldo). El lock **si** es load-bearing, pero su oraculo es el test de **8 concurrentes** (1 exito → 8), y las mutaciones (a) y (b) resultaron indistinguibles. Es el ADR 0054 otra vez —un documento afirmando un invariante que el test no pinnea— pero del lado del plan de pruebas; corregido en la spec, en el JSDoc de `redemptions.ts` y como regla en `CLAUDE.md`. Ademas el guard de fuga de DTO **se pudo evadir 2 de 3 veces** (una clave real de R2 y el jsonb entero, con otro nombre) y paso a ser **allow-list positiva** sobre el conjunto exacto de claves. 453 unit + 48 de integracion Neon. 3 casilleros del DoD quedan abiertos **a proposito** (comportamiento de UI sin oraculo, van al QA del owner) — antes (2026-09-07, implementa los ADR 0053 y 0054; dependia de la spec 0056) — una revisión independiente del plan dio **NO PASA** con 5 bloqueantes de mecanismo (ninguna decisión del owner cambió): el débito pasa a **transacción interactiva** (el patrón de `persistGrant` que iba a copiar está roto), `insufficient_override` se deriva del saldo **bloqueado** y no de un pre-read, se rechazan `points_cost` nulo y `target` 0 (los dos borraban saldo en silencio), y `operatorBusiness` gana el filtro `status = 'active'` (hoy un empleado **deshabilitado** puede operar el mostrador). Además — el owner ratificó el flujo y resolvió el saldo insuficiente: **vuelve a 0, nunca negativo** (9 sellos de 10 → 0; 80 puntos contra un premio de 100 → 0), con clamp en el `SET` y el log guardando lo que costaba vs. lo que se pagó | no | `specs/0055-canje-de-premios-en-mostrador.md` + anexo del orquestador `specs/0055-contratos-del-orquestador.md` (los dos contratos compartidos que la spec le exige fijar **antes de despachar**: `RewardDTO` —se extiende el `toRewardDTO` existente, no se crea un segundo DTO— y la firma de `planRedemption` con su **tabla de 24 casos** como oraculo del unit) |
| 0056 | 2026-09-07 | **La acreditación no puede acreditar dos veces** (implementa el ADR 0054, **va antes que la 0055** por decisión del owner). Bug **vivo en producción**: con el mismo `client_request_id` en dos requests concurrentes, `persistGrant` incrementa el saldo dos veces y deja una sola orden que reporta el saldo intermedio — el disparador realista es un reintento de red o un doble submit, porque la consola mintea el `requestId` en el escaneo y lo mantiene estable. Fix de una línea (sacar el `ON CONFLICT DO NOTHING`) **verificado en la misma sonda que encontró el bug**: el `23505` aborta el statement y revierte el bump, el reintento secuencial sigue sin debitar ni fallar, y `grant.ts` ya sabía capturarlo. Sin migración: el deploy es el fix. Exige reproducir la carrera **sobre `neon-http`** (la sonda fue Postgres local) y un test que asevere el **saldo por SQL**; si el `23505` no revirtiera sobre HTTP, la spec se reabre con `withDbTransaction`. **Implementada con PASS: la carrera se reprodujo sobre `neon-http`** (no solo en el Postgres local de la sonda) — reponer el `ON CONFLICT` da `points_balance` 40/60/80 donde debe haber 20, y el `EXPLAIN` del plan real confirma `InitPlan` + `One-Time Filter`. La reapertura NO se disparo: el 23505 aborta y revierte el bump sobre HTTP. Ningun test viejo del dominio ejercia concurrencia — el bug era invisible por diseno de la suite | implementada | sí | `specs/0056-la-acreditacion-no-puede-acreditar-dos-veces.md` |
| 0057 | 2026-09-08 | **El login dice por que rechaza a un staff desactivado** (implementa el ADR 0055). Nace del QA del owner: el rechazo funcionaba y era **mudo** — el usuario volvia al formulario vacio, indistinguible de «puse mal la contraseña». Al investigar aparecio lo que el QA no podia ver: ese login **deja una sesion viva** de un miembro desactivado. El guard **revoca y despues** redirige con motivo (`?e=staff_disabled`); `/login` pasa a server component que traduce el motivo **por allow-list** —el valor crudo del query param nunca llega al DOM— mas `login-form.tsx` cliente. Sin migracion: el deploy es el fix. **El revisor independiente dio FAIL y el hallazgo fue un LIMITE FALSO en esta misma fila:** se habia escrito que «el aviso no tiene oraculo porque vitest corre en `node` sin jsdom». Es cierto solo para la **interaccion** (reintentar con contraseña mala pisa el aviso); el **renderizado** —que es el DoD #1 y el pedido literal del owner— **si es pinneable sin dependencias nuevas**, con `react-dom/server` bajo el mismo `environment: "node"`. El revisor lo demostro escribiendolo. Era el patron del ADR 0054 —un documento afirmando un limite que no existe— y ya habia viajado a este indice. Corregido: se agrega el test de render y el limite queda acotado a la interaccion. Segundo hallazgo: las mutaciones (b) «no revocar» y (c) «revocar despues del redirect» **no** son indistinguibles —las separa `typecheck` con `TS18047`—. **Abierto, no decidido por el owner:** `api/billing/checkout` no filtra por `status` — misma familia que el hueco que la 0055 cerro en `operatorBusiness` | **implementada con PASS de revisor independiente al TERCER intento** (2 FAIL previos, los dos por el MISMO defecto: un limite declarado sin intentarlo, y despues **sub-corregido**. El bloqueante final se cerro cuando el revisor escribio el test que la spec declaraba imposible y demostro con una mutacion que **se podia romper el DoD con los 5 gates verdes**) | si | `specs/0057-el-login-dice-por-que-rechaza-a-un-staff-desactivado.md` |
| 0058 | 2026-09-08 | **Cierre de Checkout + toast del login** (ADR 0056). Se prueba que Checkout solo es parte del onboarding: la membresía owner ya existe y el staff solo puede nacer después, desde una pantalla owner-only; no hay upgrade posterior, así que el hallazgo se cierra sin tocar la ruta. S4: el aviso de staff desactivado y el error de credenciales son un toast flotante que se reemplaza, nunca se apila | cerrada | **implementada** (2026-09-15, por decision del owner: «ya estan corriendo»; 17 tests verdes en `src/app/login`). **OJO: su PREMISA sobre Checkout CADUCO** — cerro el hallazgo como «no alcanzable» porque habia «un unico llamador (`OnboardingPage`)», y **hoy hay DOS**: la seccion de suscripcion de la spec 0063 tambien lo llama. **El riesgo lo cerro esa misma 0063** (`requireBillingOwner` + test de auth que cubre checkout), no este documento. Cerrar un hallazgo como «no alcanzable» ata la spec a una foto del producto | `specs/0058-checkout-solo-owner-y-toast-login.md` |
| 0059 | 2026-09-08 | **Captura de imagen móvil con feedback** (ADR 0057). Logo, sello y producto indican que analizan la imagen y, en móvil, ofrecen galería más cámara trasera explícita | **implementada** (2026-09-15). Verificado por render REAL antes de marcar: el feedback cuelga de `isAnalyzing` y el `capture="environment"` vive **dentro** del guard `isTouch`. **EL LIMITE «no hay ningun test» QUEDO CERRADO EL MISMO DIA**: `catalog/image-capture.test.ts`, 6 tests con `renderToStaticMarkup` + `node-html-parser` (**bundleado en next**, cero paquetes), y **DOS mutaciones EJECUTADAS y transcriptas** — anular el guard `isTouch` y colgar el cartel de `true` dan rojo 1/6 cada una, con la asercion hablando de la propiedad. Sigue sin oraculo el guard de las OTRAS dos superficies (sello: pendiente; logo: exige `vi.mock("react")` porque su estado carga en un `useEffect` que no corre en SSR) | **implementada** (2026-09-15, por decision del owner: «ya estan corriendo»). Verificado por lectura antes de marcar: el feedback cuelga de `isAnalyzing` en las tres superficies y el `capture="environment"` vive **dentro** del guard `isTouch`, asi que escritorio no la recibe. **LIMITE DECLARADO: no hay NINGUN test que pinnee esto** — se puede romper «Tomar foto» o sacarle el guard con los 5 gates en verde | `specs/0059-captura-de-imagen-movil-con-feedback.md` |
| 0060 | 2026-09-09 | El micro-portal `/wallet` se actualiza sin cerrarlo y reabrirlo (hoy el saldo es una foto del render del servidor). **Ojo:** el service worker no sirve como disparador — por el ADR 0040 la acreditación de quien tiene el pase NO sale por Web Push | borrador | sí | `specs/0060-el-portal-del-consumidor-se-actualiza-solo.md` |
| 0061 | 2026-09-09 | **Locales en el backoffice** — ver, crear, editar y **archivar** locales; Geoapify da texto **y** coordenadas, y una direccion tipeada sin coincidencia se guarda como **solo texto, sin georreferencia** (nunca se inventan coordenadas). Tope por plan: free 1, plus 3, contando ACTIVOS. Reemplaza el mock de la spec 0015 y cierra la parte abierta de la 0023. Migracion `0029`: columna de estado + coordenadas nulables. **El filtro `status='active'` en `assertLocationInBusiness` es load-bearing**: sin el, un `?location=<uuid>` guardado en favoritos sigue acreditando contra un local archivado | **implementada** (PASS de revisor independiente en 2 pasadas) | sí | `specs/0061-locales-en-el-backoffice.md` |
| 0062 | 2026-09-10 | **La integracion Neon corre en CI** — los 27 `.neon.integration` contra una rama persistente `ci-integration`, migrada en cada corrida, serializada por `concurrency`, y con un paso que **FALLA si el secret falta** para que un skip no vuelva a parecer un pass. Antes se podia borrar un guard de produccion con los 5 gates verdes (probado por mutacion en la 0061) | **implementada** (verificado en CI: `Tests 678 passed (678)`, cero skipped, vs 521\|157 en local) | sí | `specs/0062-integracion-neon-en-ci.md` |
| 0063 | 2026-09-10 | **Cambio de plan y cancelacion de suscripcion** (implementa el ADR 0058, cierra la tarea 53). Seccion propia `/backoffice/subscription`: upgrade `free`→`plus` por Stripe Checkout aplicado cuando el pago se confirma; **downgrade = cancelacion** (con los planes de hoy son la MISMA operacion) con **bloqueo duro** — el servidor rechaza la baja mientras haya mas locales activos que el tope destino; **tope efectivo = min(plan vigente, plan pendiente)**, asi que una baja programada baja el tope a 1 y no se puede desarchivar para volver a 3 (decision 6 del owner). El webhook deja de escribir `plan:"plus"` para cualquier evento. **La primera version paso por DOS revisores independientes y por verificacion empirica del orquestador (SQL contra prod, `EXPLAIN`, los `.d.ts` de la version instalada), y cambio en cinco puntos.** Los dos mas caros: **(1) la spec repitio el patron del ADR 0054 sobre si misma** — afirmo «verificado empiricamente» que el periodo se lee de `items.data[0].current_period_end` citando los tipos de `stripe@22.5.0` (dahlia), pero **la forma del payload la fija la api_version del endpoint, y en prod es `2020-08-27`** (verificado: `select payload_version from core.stripe_webhook_event`), anterior al movimiento de ese campo a los items → habria dado `undefined` en runtime con `typecheck` en verde; el diseño pasa a **leer el estado real de Stripe** (`subscriptions.retrieve`) y usar el payload solo como disparador, lo que ademas mata el desorden de eventos; **(2) el webhook escribia el plan SIN `lockBusiness`**, asi que un desarchivado concurrente podia dejar `free` con 2 activos — el invariante central violado por el camino mas probable (hallado por los dos revisores por separado, uno lo ejecuto). Ademas: `subscription_live` definido como `status != 'canceled'` **bloqueaba a los 11 negocios de prod** (todos `active`) incluidos los 9 `free` que la spec existe para desbloquear; `deleted` llega con `cancel_at_period_end: true` (lo dice el campo) asi que la regla vieja dejaba `pending_plan` puesto **estando ya en free**; la derivacion regalaba `plus` permanente a `unpaid`/`paused`; y el observable de «el efecto ocurre una vez» **no existia** (el `UPDATE` es idempotente, la mutacion quedaba verde) — se redefinio a «una sola entrega gana el claim». La QA manual estaba escrita sobre un estado de prod **viejo** (A1 tiene 1 local activo y ninguna suscripcion de Stripe; el unico con suscripcion real es Negocio B) **Las 4 decisiones abiertas las cerro el owner el 2026-09-10** (ADR 0058 §8-11): modal en vez de boton gris; A1 se arregla creando la suscripcion en Stripe; el impago bloquea el acceso en vez de bajar el plan (ADR 0059, tarea 54); y **el cambio de intervalo mensual↔anual entra**. De ese ultimo salio un dato de Stripe que invierte la intuicion y esta verificado en la doc del SDK: `proration_behavior: 'none'` al cambiar de intervalo **no** es «al final del periodo» — «we don't generate any credits for the old subscription's unused time. We still reset the billing date and **bill immediately**» (`Subscriptions.d.ts:50`), o sea que el cliente pierde lo pagado y se le cobra otra vez; «al final del periodo» exige `subscription_schedules`. De ahi el diseño asimetrico (mensual→anual inmediato con `always_invoice`; anual→mensual agendado). **Y una correccion conceptual del owner que cambio el arreglo del bug 3:** «`deleted` → `free`» **no** es el fix — `free` es una suscripcion y un `deleted` no es ninguna, asi que va a **`plan='none'`** (ADR 0058 §12). De implementarlo salio un hallazgo propio: el `deleted` de **fin de periodo** es el final normal del flujo de cancelacion, asi que mandarlo a `none` **bloquearia a quien cancelo bien** — el discriminante es `pending_plan`, sin columna nueva. La spec entrega ademas la **salida** desde `none` (`settle_to_free`, la unica operacion de plan que no toca Stripe, con la MISMA funcion pura y el mismo modal que el downgrade voluntario para que no existan dos reglas que puedan divergir). **Recorte final del owner:** el cambio de intervalo se entrega **solo mensual → anual** (inmediato, con `always_invoice`); el sentido inverso es la **tarea 55** porque no tiene forma barata —reembolso, o quedarse con la plata del cliente, o `subscription_schedules`— y su rechazo (`interval_downgrade_unsupported`) vive en la funcion pura para que haya un solo lugar donde habilitarlo. Con eso salio `pending_interval` del modelo: la migracion 0030 queda en 3 columnas + el unique de `business_id` + `ignored_reason` **SEGUNDA RONDA de revision independiente (2 revisores mas) — encontro 7 bloqueantes y la spec se reescribio otra vez.** El peor: **el discriminante de «esta baja la pedimos nosotros» era FALSIFICABLE por el actor del que habia que defenderse.** La version anterior usaba `pending_plan` y afirmaba «no hace falta ninguna columna nueva»; pero la jerarquia de la propia spec escribe `pending_plan='free'` ante cualquier `cancel_at_period_end`, que es **justo lo que setea el boton del dashboard de Stripe** — asi que cancelar desde el dashboard con 3 locales activos clasificaba el `deleted` como «esperado» y aterrizaba en **`free` con 3 activos**, el estado que toda la spec existe para prohibir. Se agrega `downgrade_requested_at`, escrita SOLO por nuestras rutas. Los otros seis: **`PlanChangeInput` no podia expresar un cambio de intervalo** (y la tabla de casos exigia 3 codigos de intervalo) ni declaraba **orden de evaluacion** —con entradas que matcheaban dos filas, incluida la salida de `none`, que leida literal era un 409—, resuelto con un `PlanIntent` discriminado y guardas **ordenadas**; **`invoice.paid` LLEGA a este endpoint** (verificado en la base) y `subscriptions.retrieve` con un `in_…` entra en **loop de reintentos hasta que Stripe desactiva el endpoint**, resuelto con allow-list de tipos y el claim **antes** del retrieve; **el `itemId` que la llamada de intervalo necesitaba no existia en ninguna parte del sistema**; el `SET` de `settle_to_free` no estaba escrito y la eleccion natural (`status='active'` con el id viejo) dejaba al negocio **sin poder pagar nunca mas**; la reconciliacion era un **no-op en su propio caso motivador** (el `stripe_customer_id` lo escribe solo el webhook, asi que si el evento se perdio el customer tambien) y `list` sin `status` **no ve las canceladas**; y **el orden de despliegue estaba invertido** — pushear antes de migrar deja `planLocationLimit` pidiendo una columna inexistente y **los 11 negocios pierden el modulo Locales**. Ademas: un negocio `free` con la primera tarjeta rechazada terminaba en `plan='none'`; `Subscription.Status` **no tiene 8 valores, termina en `OtherString`**, asi que el guard anti-doble-cobro necesitaba allow-list de **muertos**, no de vivos; los «comandos exactos» usaban `DATABASE_URL` y **skipeaban la integracion en silencio** (el falso verde de la 0062, reintroducido); y la mutacion del `lockBusiness` del webhook estaba justificada con un motivo **falso** (el lock serializa, no ordena) | **IMPLEMENTADA Y EN PROD** (todas las fases con PASS; migracion `0030` aplicada y verificada por SQL; dos tandas de QA del owner). **SUPERADA EN PARTE POR LA SPEC 0064 — el documento lleva el aviso al inicio:** `POST /api/billing/resume` y el boton «Reanudar» **ya no existen en el arbol**, y la baja **ya no se programa** (es inmediata, ADR 0063). Todo lo que este doc dice sobre `resume` y sobre «baja programada con fecha» es **historia, no el estado actual**; sigue vigente el bloqueo duro por locales, `min(vigente, pendiente)` **solo** para la baja que llega del dashboard de Stripe, `plan='none'` y el intervalo mensual → anual. Cerrada originalmente el 2026-09-10 tras 2 rondas de revision. Seis decisiones del orquestador quedan etiquetadas como tales, no como del owner (entre ellas `resume`, y el `price.id` desconocido que se registra en `ignored_reason` sin aviso porque no hay infra de alertas). **EN IMPLEMENTACION POR FASES: la FASE A (fundacion) tiene PASS de revisor independiente el 2026-09-11** — migracion `0030` (5 columnas + `core_subscription_business_unique`, verificada por SQL), `effectiveLocationLimit`, `decidePlanChange`, `planFromSubscription`, `assessEventApplicability` y el DTO, con 779 tests en verde y **las 9 mutaciones de la fase ejecutadas y transcriptas**. La 1a pasada de revision dio **FAIL** por un bloqueante que vale registrar: `limitReached` tenia la rama correcta y **ningun oraculo** — anularla dejaba la suite en 620/620 verde. **Dos hipotesis de la tabla de mutaciones resultaron mal atribuidas al ejecutarlas (M1 y M11), y M15 revelo que el guard de pertenencia no tenia oraculo ninguno.** **La FASE B (webhook + `store.ts` + integracion) tiene PASS de revisor independiente el 2026-09-11, en 2 pasadas**; falta la fase C (rutas + UI + D8/D10). **Nada desplegado a prod.** La 1a pasada dio **FAIL** con 2 bloqueantes que vale registrar porque **los dos eran afirmaciones escritas que el arbol no sostenia, no bugs de codigo**: (B1) el docblock del claim afirmaba en mayusculas «UNA SOLA ENTREGA GANA EL CLAIM» y es **falso para el solape real** — el claim commitea antes del `retrieve` por [R1-M1], asi que dos entregas solapadas ganan las dos; vale solo para la reentrega SECUENCIAL, que es el reintento de Stripe; y (B2) **el costo declarado para cerrarlo era falso** («un lock explicito o una columna `processing_at`»): el lease entra en la columna **`received_at` que ya existe**, como un predicado mas en el mismo `setWhere`, sin migracion ni lock, y el `EXPLAIN` muestra **los dos predicados en el mismo nodo post-lock**, asi que no rompe la premisa del ADR 0054. B2 es el ADR 0054 en su version mas dificil de ver —una **imposibilidad** afirmada de mas, no un invariante— y **habria torcido la decision del owner**; el orquestador lo habia relatado sin verificarlo, que es exactamente lo que `CLAUDE.md` prohibe. **Lo mejor de la revision fue una mutacion que no estaba en la tabla (MUT-B): anular la ASIMETRIA del guard de adopcion** (que corriera tambien cuando el id coincide), que demostro que las **dos** mitades de m1 tienen oraculo —no solo la benigna— con 2 rojos unit + 2 de integracion por SQL. Queda **UN item del DoD abierto a decision del owner**: «dos entregas simultaneas, una sola gana el claim» — reescribirlo o implementar el lease. **Al cerrar la fase A la spec se edito una vez mas para saldar el hallazgo m1, como decision del ORQUESTADOR (n.º 7), no del owner:** el guard de pertenencia de D5.h no frenaba nada sobre una fila **adoptable** (`stripe_subscription_id IS NULL` o status muerto — el caso de A1 en prod), asi que compuesto con la precedencia «lo terminal gana sobre el price desconocido» **un `deleted` de una suscripcion AJENA con price AJENO apagaba un negocio vivo** (`plan='none'`). Se agrega la regla de ADOPCION —una fila adoptable se adopta solo si la suscripcion **recuperada** tiene price nuestro y status no muerto, nuevo `ignored_reason='not_adoptable'`— mas su espejo en el binding de `checkout.session.completed`, con dos filas de DoD y las mutaciones **M18/M19**. El atajo «adoptable solo por `created`» esta explicitamente prohibido en la spec: el tipo de evento es un disparador, no un hecho **[2026-09-11] El unico item del DoD que quedaba abierto lo cerro el owner: se IMPLEMENTA, como FASE C (D12, ADR 0061), y la vieja fase C pasa a FASE D.** Y al diseñarlo se cazo que el lease propuesto por la fase B, **implementado tal como estaba escrito, habria sido una regresion**: el rechazo por lease contestaba 200 y Stripe deja de reintentar ante cualquier 2xx, asi que un evento cuya primera entrega muriera no se habria procesado nunca. El claim pasa a tri-estado con respuesta no-2xx para `in_flight`. **El parrafo que declaraba el precio («una linea de SQL mas su test») habia nacido corrigiendo OTRO limite inflado: sub-corregir dejo el mismo agujero mas chico.** **LA FASE C TIENE PASS DE REVISOR INDEPENDIENTE el 2026-09-11, en 3 pasadas** (PASS con 5 menores → **FAIL del delta** por 1 bloqueante → PASS final sin hallazgos abiertos), commiteada en `71499f5`; **falta la fase D** (rutas + UI + D8/D10). **Lo que mas valio de esa revision fueron 4 mutaciones FUERA de la tabla, y dos quedaron VERDES** —o sea dos afirmaciones de docblock sin oraculo—: una se cerro (la forma del statement se pinnea con `toSQL()`, no era un limite) y la otra se **declara** como no pinneada en el codigo, con el renglon a re-mirar. **El bloqueante del delta es el mas instructivo: el archivo de test reportado como «299/300 — queda margen» tenia 309 y ya violaba el limite** — el revisor lo cazo corriendo el hook `file-size.sh` (`EXIT=2`) con un control, no con un `wc`, y los 5 gates no lo cazan porque ese hook es PostToolUse, no Stop. De ahi salieron **tres entradas mistake→rule en `CLAUDE.md`**, las tres de errores reales del orquestador en esta fase: un ROJO puede ser por el motivo equivocado (un test que moria por `DATABASE_URL` se leia como el guard mordiendo); una mutacion se corre contra **todos** los archivos que pueden verla (M21/M22 se transcribieron con 2 y 1 rojos, eran 3 y 2, y una fila razonaba sobre el numero equivocado); y un numero que va a un doc se re-mide en el momento, preguntandole **al hook** y no a un `wc` viejo. **FASE D1 (servidor) CON PASS DE REVISOR el 2026-09-12, commiteada en `8effb1b`; falta la fase D2 (UI + D8 + render del HTML).** Lo que definio la D1 fue el METODO, no el codigo: **cuatro rondas de revision encontraron un bloqueante cada una, los cuatro de la MISMA familia** —un docblock que afirma un invariante que ningun test pinnea—, y despues **un BARRIDO sistematico de los docblocks encontro NUEVE en una sola pasada**, incluido el cableado del gate que evita **cobrar dos veces**. De ahi sale la regla de `CLAUDE.md`: la tabla de mutaciones se escribe desde el diseño, asi que no ve lo que el codigo termino afirmando. Cerro en **121 archivos / 872 tests / 0 skipped** con los 10 oraculos discriminando (cada mutacion pone roja 1 de 47). **Residuales para la D2:** un `it` que carga CUATRO oraculos y cuyo titulo nombra uno (la atribucion existe en el archivo, pero en CI solo se ve el titulo), y `billing-store.neon.integration.test.ts` de la fase B con **24 lineas de literales FIJOS** — el mismo modo de falla que en la D1 envenveno la rama efimera y dejo la suite muriendo **en el seed**, indistinguible de un bug de codigo. **Ojo al revisar la D2:** la salida del estado «plus con la suscripcion muerta» cuelga de «D8 **o** el boton de D10», y la particion las mando a fases distintas — **ningun revisor las vio juntas.** | si | `specs/0063-cambio-de-plan-y-cancelacion-de-suscripcion.md` |
| 0064 | 2026-09-13 | **Baja inmediata y los datos del cobro** (implementa el ADR 0063 y cierra los huecos del QA de la 0063). La baja se cancela en el acto sin devolucion, el modal lo dice antes de confirmar, y aparece el aviso de cuando conviene bajar. Ademas los **cuatro huecos de UI que el QA encontro y que la spec 0063 NUNCA PIDIO** (verificado leyendola, no son fallas del implementador): la seccion no dice si el plan es mensual o anual, el cambio de intervalo **no pide confirmacion y cobra inmediato**, no se muestra la fecha de renovacion, y no hay forma de ver el importe cobrado ni el recibo. Hace falta sumar `subscriptions.cancel` e `invoices` al gateway y al fake. **Riesgo escrito antes de implementar: el defecto que la origino no lo caza una mutacion** —no hay invariante roto que mutar—, asi que el plan de pruebas exige un caso que recorra la secuencia completa y asevere que lo que se cobra y lo que se puede usar coinciden | **cerrada** (2026-09-13). **NO EXISTE la baja diferida: se BORRA la ruta `resume`** y con ella el bug D4 del QA —no se arregla codigo que no va a existir—; el recibo es el de la **ultima factura pagada**; y el aviso vive **en el modal**, no como cartel. **Pedido del owner que entra al DoD: que borrar la ruta no deje un ERROR FANTASMA** — `.next/types/validator.ts` es generado, tiene un bloque por ruta que importa su `route.js` y `tsc` lo mira, asi que al borrar `resume` el `typecheck` falla **nombrando el archivo que borramos a proposito**. Lo que el build regenera es el VALIDATOR (sin la ruta), no la ruta; y **solo pasa en una maquina con `.next` tibio**, porque en CI/Vercel se buildea de cero. Va con **hook propio** que lo detecte y lo borre, probado que muerde **y que discrimina**. Una decision queda etiquetada como del ORQUESTADOR y no del owner: que hace la app si la cancelacion diferida llega **desde el dashboard de Stripe** (la registra y la informa, sin ofrecer reanudar). **IMPLEMENTADA (2026-09-14) EN 3 FASES, SIN REVISION INDEPENDIENTE FINAL — el owner CORTO el ciclo de verificacion** («no podemos quedarnos eternamente gastando tokens en cosas que no acaban… probar un QA que nos de la verdad»), y de ahi sale la regla de `CLAUDE.md` sobre presupuesto y condicion de corte. **QA DEL OWNER EN PROD: TODO EN VERDE (2026-09-15)** — el oraculo que define. `Vercel: success` verificado para el sha exacto `ca2d746` ANTES de pedirle el QA. Gates al cerrar: 721 unit + 240 de integracion de billing, typecheck/lint/format/build verdes, cero archivos sobre el limite de 300. **El hallazgo que mas costo NO fue de producto sino de infraestructura de test, y habria roto el CI entero:** un `import` de VALOR al barrel `./billing` agregado a `billing-integration-support.ts` cerraba un ciclo con la factory de `vi.mock("./stripe-config")` —que importa ese mismo support— y **los dos archivos `billing-pages*.neon.integration.test.ts` COLGABAN para siempre** (ni `vitest list` terminaba, 0% de CPU). Apuntarlo a `./billing/store` lo bajo de infinito a **1.06 s**. Es el mismo deadlock que ya documentaba `billing-pages.neon.integration.test.ts`, reintroducido por otra puerta: **un `import type` es gratis, uno de valor al barrel no**. **Cinco agentes se cortaron a mitad en esta spec**; el cierre lo hizo el orquestador a mano. Tres decisiones del ORQUESTADOR —**O-2** el link del recibo cruza como prop; **O-3** NO hay migracion de datos de los diferidos; **O-4** la baja conserva la marca de intencion del paso 2 (borrarla pierde la idempotencia ante un 503 y manda al negocio a `none` en vez de `free` si el webhook gana la carrera)— **quedaron ACEPTADAS el 2026-09-15**, en bloque y sobre el producto funcionando, con O-3 presentada como contraria a un pedido literal del owner | no | `specs/0064-baja-inmediata-y-los-datos-del-cobro.md` |
| 0065 | 2026-09-15 | **Campaña de proximidad por wallet** (implementa los ADR 0064/0065; primer tipo de campaña real y journey base de los demas). Compositor en bloques cerrados: «clientes de [locales] que no vienen hace [N] dias → se les muestra tu local en su Wallet al pasar a ~100 m → mensaje ≤60 → sin cupon | cupon (etiqueta, costo estimado, tope) → vigencia/revision con audiencia real y costo maximo». Tick cada 6 h por GitHub Actions (Hobby: 2 crons ya usados) con `planConsumerPlacement` PURO + aplicador SQL bajo lock por consumidor: encola (unico parcial `(business, consumer) where vivo` — por NEGOCIO, no por campaña: el ADR dice «1 turno activo por negocio» y dos campañas del mismo negocio se colaban), vence con outcome (`purchase`/`coupon_redeemed`/`none`), cancela con razon, coloca ≤3 utilidad + ≤5 turnos (≥400 m, 1/negocio, cuota 50, holdout 10 % que NO cuenta), escribe `pass_placement`, sube `message_updated_at` SIN tocar `latest_message` y encola `pass_refresh`. Cupon canjeado en el mostrador con `for update` de la campaña + turno, unique `turn_id` y `(business, client_request_id)`; mas la lectura de idempotencia por `client_request_id` BAJO el lock y ANTES de las guardas de negocio (sin ella el reintento salia 409, no 200); se cierra con la **carrera real**, no con el `EXPLAIN` (son dos statements: ningun plan muestra el lock). Resultados SOLO observados salvo «estimacion del efecto» con ≥30 retenidos; «estas en el pase de K de C clientes». Opt-out por negocio desde una **seccion de Configuracion** del portal del consumidor (`marketing_opt_out_at`, escrita solo por esa accion; lo transaccional y el saldo en el pase no cambian) y freno por plan como **BLOQUEO DURO**, calcado del de locales: guarda nueva `downgrade_blocked_campaigns` con `deactivateCount` en `decidePlanChange` (orden declarado: locales → campañas → `already_on_plan`), mas una **pausa defensiva** en el webhook para el plan que aterriza en `free`/`none` sin pasar por nuestra ruta (dashboard de Stripe / impago — el agujero del ADR 0060) | **cerrada** (2026-09-15, segunda vez, tras la revision adversarial). **FAIL unanime de 3 revisores, 16 bloqueantes + un 17.º que ningun revisor cazo y salio de correr el SQL** contra Postgres 18 real: el `on conflict` contra un indice **parcial** exige repetir el `where` del indice, sin el **el tick reventaba en su primera corrida**. Lo demas que cazo: el unico parcial por campaña en vez de por negocio; `push-worker.ts:60` colapsando `pass_refresh` a `transactional` con typecheck verde; un `PATCH` de Google citado como existente que hay que **escribir**; un barrido estatico **vacuo** (buscaba `marketing_opt_out_at`, el codigo escribe `marketingOptOutAt`); el cooldown quemado por turnos **cancelados** (pausar un dia bloqueaba la audiencia 30); el unico parcial sobre `status='pending'` chocando con el retry del worker (fila clavada en `sending`, `attempts` en 0, reintento infinito — reproducido en SQL); la mutacion del webhook que **no mordia**; y la falta de una tabla para los conteos de exclusion del tick (`campaign_tick_audience`). **Los 17 verificados empiricamente antes de bajarlos a la spec, no por la cita del revisor.** Dos decisiones del owner al cerrar: puerta compartida → **se fusionan** saldo y oferta en un texto (`composeRelevantText`, cap 120 a medir en QA), y **merito desde el dia uno** (ADR 0066). 4 fases (A fundacion, B backoffice, C cupon, D consumidor+plan), cada una con PASS . **IMPLEMENTADA ENTERA al 2026-09-16 (A en prod; B, C y D en el arbol, D sin commitear); falta la COLA DE REVISIONES INDEPENDIENTES, una por fase, que el owner decidio correr al final.** Correccion en vuelo: el opt-out quedo como **tercera pestaña** del portal y no como ruta (**ADR 0068**, decision del owner) | no | `specs/0065-campana-de-proximidad-por-wallet.md` |
| 0066 | 2026-09-16 | **Reparacion del harness** (implementa el ADR 0069; no toca una linea de `apps/`). Poda `CLAUDE.md` de **696 a <=200** lineas con tres destinos y **ninguno es borrar**: lo que cambia una decision en toda sesion se queda, el protocolo de verificacion y las gotchas por dominio van a **skills** (que la doc oficial recomienda literal para lo que solo hace falta a veces), el historico a `docs/LECCIONES.md` y lo chequeable a un hook. **Trampa central: `LECCIONES.md` se referencia como texto plano, NUNCA con `@`** — un import se carga igual que si estuviera pegado y la poda no ahorraria un token. `docs/TASKS.md` queda con solo el arco en ejecucion; lo diferido va a `docs/PARQUEADO.md` y los ~10 encargos/revisiones de fases cerradas a `docs/archivo/` con `git mv`. Crea `.claude/agents/implementador.md` y `revisor.md` con el protocolo de mutaciones adentro (shasum ANTES de mutar, bitacora ANTES de medir, `git status` por el untracked) y **el presupuesto y la condicion de corte como parte del encargo**. Y `tools/worktree-new.sh`: `node_modules` propio por worktree instalado **100% offline** desde el `.pnpm-store` del repo. Presupuesto de verificacion **declarado en la propia spec: 3 mutaciones**, una por guard nuevo, clase de error = los plausibles. El DoD que mas importa y no se puede declarar sin ejecutarlo: un worktree corre `typecheck` en verde **y** el `node_modules` principal queda intacto por `shasum`. **Implementada con PASS de revisor independiente** (2026-09-16, en contexto fresco): las 3 mutaciones del presupuesto ejecutadas y revertidas por el revisor —cobertura 46/46, hook `exit 2` en 201 lineas y `exit 0` en 200, worktree con `shasum` de `node_modules/.modules.yaml` del repo principal identico antes/despues—; 1 hallazgo informativo (el mensaje del script de cobertura reporta el bloque perdido con un preview de baja granularidad, no un falso negativo). No pusheada | implementada | sí | `specs/0066-reparacion-del-harness.md` |
| 0067 | 2026-09-16 | **Identidad sin contraseña** (primera tajada del ADR 0070; el arco del wizard se corta en 4 specs y esta es la que va primera porque **owner y staff comparten hoy la misma pantalla de login**, `login-form.tsx:49`). `emailAndPassword` se apaga: el owner escribe su email y queda logueado, y despues entra por **link magico**. **Requisito tecnico, NO decision del owner: un email YA EXISTENTE nunca abre sesion** — manda link — porque sin contraseña escribir el email de otro merchant le entregaria el negocio. El **email verificado pasa a ser un gate real**: hoy la columna existe con default `false` (`schema/auth.ts:10`) y **no se lee en una linea de produccion** (solo en soporte de tests), asi que el gate es codigo nuevo, no migracion; vive DENTRO de `requireBackofficeSession` (ADR 0044, 8 paginas + mostrador) y se aplica **solo a `role='owner'`** — el staff no tiene email y el mostrador quedaria muerto. El **staff deja de tener email/contraseña** (hoy `signUpEmail` con una clave que el owner elige y pasa por fuera, `staff.ts:119`) y entra con `handle@slug` + **PIN de 6 digitos hasheado**, visible UNA sola vez al crearlo y despues solo regenerable. **El PIN NO pasa por el plugin `username`**: medido que no trae reglas propias, que la global `/sign-in*` (3/10 s) es `(IP, path)` —el staff de un local se bloquea entre si y quien rota IPs no tiene limite por cuenta—, que el storage `memory` se evapora en lambda (no hay donde persistir el escalado) y que `enabled`=`isProduction` la apaga en test. Bloqueo escalado propio persistido por **`(business_id, user_id)`** — `business_membership` tiene PK compuesta y **no existe `membership_id`**. Incluye el **slug** del negocio (unico global, que NO sigue al nombre) y el **borrado del arco de recuperacion** entero. Presupuesto declarado en la spec: **5 mutaciones**, clase = los plausibles; declarado AFUERA la carrera real de dos PINes simultaneos, la entregabilidad del mail y la ejecucion del wipe contra prod. **Entrega API y endpoints, NO interfaz** (ADR 0070 §16): el entregable son DOS piezas, los endpoints **y** `specs/0067-contratos-de-api.md`, el contrato HTTP normativo que consume quien construye la UI por fuera — un endpoint sin su fila en el contrato no esta terminado, y el DoD incluye el chequeo mecanico de que **ningun archivo fuera de `app/api/` aparece en el diff**. **El owner eligio la salida A: la UI vieja se BORRA** (ADR 0070 §17), contra la recomendacion del orquestador (recablear, por «gana la pantalla»). Se eliminan `/login`, `/onboarding` y `/forgot-password` enteras **con sus 10 referencias colgantes en 8 archivos**, medidas: 2 enlaces de la landing, 1 del `not-found`, el `sign-out-button`, 5 CTA de las paginas demo, y **los 3 `redirect` de `requireBackofficeSession` — que son control de acceso**: apuntados a una ruta borrada convierten un rebote en un 404 y el `staff_disabled` del ADR 0055 pierde el canal que explica el rechazo, asi que pasan a `/` conservando el codigo y su allow-list. **Costo aceptado y declarado: el producto queda sin entrada por navegador, o sea SIN QA de pantalla** — es la excepcion explicita a «gana la pantalla» y la verificacion manual baja a `curl` con respuestas transcriptas. Presupuesto 5→**6 mutaciones**, la sexta sumada explicitamente porque el borrado mueve control de acceso. **El owner confirmo el corte del arco en 4 specs el 2026-09-16** (identidad → wizard → entitlements → onboarding derivado) y con eso la spec quedo **`cerrada`**. **Re-medida antes de despachar, el censo de su §7 estaba corto y su chequeo mecanico era IMPOSIBLE de satisfacer** (§7-bis/ter/quater, agregadas): faltaban 3 docblocks que citan `login-notice.ts` y sobreviven al borrado, el barrido de las 3 rutas no puede dar vacio porque `api/onboarding/business/route.ts` **es una ruta que la spec manda editar** y `recovery-routes.test.ts:56` es del consumidor, y 4 tests sin nombrar — `auth-guards.test.ts` **se actualiza, no se borra** (es el oraculo de la mutacion #6) y `middleware.ts` **se borra entero** (existe solo para el gate de `/forgot-password`). **Se implementa en 3 pasos** (base+puros → staff/PIN → owner sin contraseña + el borrado), porque los gates tienen que quedar verdes al final de cada uno. **Paso 1 con `PASS` de revisor independiente** (2026-09-16, contexto fresco): migracion `0032` con **backfill a mano** —descubierto ejecutandola, moria con `23514` contra las 8 filas de staff vivas—, `slug.ts` y `nextLockout` puros. Evidencia y bitacoras en `archivo/spec-0067-implementacion.md`. **Tres cosas que salieron de ahi y que el owner tiene que decidir:** el `DEFAULT` volatil del slug es andamiaje sin oraculo (quitarlo hoy rompe 15 call-sites), **pushear el paso 1 solo pone CI en rojo** (`ci.yml` corre `db:migrate` en cada corrida y el CHECK nuevo rechaza el insert de `createStaff`), y `slugify` colapsa alfabetos no latinos a `"000"` —lo que colisiona handles de staff— y puede devolver reservadas (`"Admin"` → `"admin"`)  **Paso 3 implementado (2026-09-16, sin commitear, sin revisor):** `emailAndPassword` apagado —medido que better-auth **sigue montando** `/sign-in/email` y contesta **400 «not enabled»**, no 404—, plugin `magicLink` con sus **dos** endpoints en `disabledPaths`, `auth/start` con las dos ramas, el consumo del link por ruta propia (que ademas **verifica el email**: better-auth pone `emailVerified: true` al consumir el token y antes revoca lo acumulado sin prueba del buzon), el gate de `emailVerified` dentro del guard **solo para owner** + su gemelo de API 403 `email_not_verified`, el `PATCH` del slug con 409 desde el choque del unico, el slug persistido en el alta, y **el borrado**: `/login`, `/onboarding`, `/forgot-password`, `api/merchant/recovery/`, `server/recovery/`, `middleware.ts` y el flag. **Dos consecuencias que salieron de medir:** `app/page.tsx` deja de rebotar la sesion viva a `/backoffice` porque con los tres `redirect` del guard en `/` eso cerraba un **bucle infinito**, y el barrido `forgot-password` del DoD **no puede dar vacio** porque la §1 manda esa palabra en `RESERVED_SLUGS`. Dos migraciones nuevas, aplicadas SOLO en la rama de integracion: `0033` dropea `password_reset_attempt` (se quedo sin lector) y `0034` crea `auth_start_attempt`, la fuente del rate limit por IP de `start`. Las 4 mutaciones del presupuesto (#3 gate sin `owner`, #4 el email conocido abre sesion, #5 el rename arrastra el slug, #6 el guard apunta a la ruta borrada) salieron **las cuatro rojas por la asercion correcta** y se revirtieron con `diff` vacio. **IMPLEMENTADA (2026-09-17) con `PASS` de revisor independiente en los CUATRO pasos** (el 4º es alcance que el owner agrego el 2026-09-17: **borrar la consola de staff del backoffice**, que posteaba campos ya ignorados y **nunca leia el `pin`**, o sea que tiraba la unica credencial del integrante en cada alta; §7-quinquies). **Hallazgo que destapo ese borrado y queda para la spec siguiente: no existe `GET /api/staff`**, asi que dos de las cuatro rutas de staff son inalcanzables para un integrante preexistente y `listStaff` queda sin dueño — cortado por condicion de corte, no por olvido, cada uno en contexto fresco. El presupuesto de 6 mutaciones se gasto entero y todas salieron rojas por la asercion correcta; **4 extra, declaradas y no escondidas**: una para probar un oraculo nacido de un FAIL en el paso 2, y tres en el paso 3 porque **`expect` corta en la primera asercion que falla** y la mutacion obvia dejaba sin ejercitar las dos que importaban (mas una cuarta del revisor, que cazo que esas tres atacaban un solo eje y ninguna probaba «rechaza de mas»). **Dos FAIL reales, arreglados**: un `code` que el contrato declaraba y dos rutas no emitian, y **`verify-email` que no distinguia una sesion de STAFF** —el sintetico `@staff.invalid` **pasa** el chequeo de forma— y por eso emitia token, gastaba cupo y pedia entregar a un TLD que RFC 2606 reserva para no resolver. **Cuatro criterios del DoD nacieron IMPOSIBLES** (tres barridos `rg` + el destino `/onboarding` que la propia spec borra): corregidos, y el patron quedo en `LECCIONES.md`. **Decisiones ABIERTAS del owner, en `TASKS.md`**: el gate de email verificado alcanza **2 superficies de API de 6** (billing, locations, marketing y catalog no pasan por `requireOwner`), el `503` no cubre la resolucion de sesion en 3 rutas (cortado por condicion de corte), `start` tiene la misma preimagen del FAIL arreglado pero **no entrega sesion** y solo se alcanza a mano, `app/page.tsx` dejo de rebotar al backoffice y `/` quedo estatica, `staff-console.tsx` sigue perdiendo la credencial del integrante, y el `DEFAULT` volatil del slug sigue vivo. Bitacoras y evidencia en `archivo/spec-0067-implementacion.md`. **No commiteada ni pusheada** | implementada | sí | `specs/0067-identidad-sin-contrasena.md` + **`specs/0067-contratos-de-api.md`** (el contrato HTTP normativo, ya con los **8** endpoints —los 4 del staff, `auth/start`, el consumo del link magico, `verify-email` y el `PATCH` del slug— con todos sus `code` estables, si setean cookie, y **la tabla de codigos de rebote** que reemplaza a la allow-list de la pantalla de acceso borrada) |
| 0068 | 2026-09-17 | **Cierre de la API de staff** (deuda de la 0067, un solo dominio). **El owner recorto el alcance el 2026-09-17**, con su motivo: «como estamos en proceso de reconversion todo a API diferentes, mas seguras, yo tocaria solo lo que faltaba de staff para completar lo que faltaba del arco 1». Quedan **cuatro** piezas. **(1)** `GET /api/staff` reusando `listStaff` (`server/staff.ts:100`), que hoy **no tiene un solo consumidor de produccion** —su unico llamador es su test— y sin el **`regenerate` y `status` son inalcanzables** para cualquier integrante creado antes de la sesion actual, porque su `userId` sale solo del 201 del alta. **(2)** **`email` SALE del `StaffDTO`**: es el sintetico `@staff.invalid` y serializarlo devuelve al navegador el mismo contacto falso que motivo borrar la consola de staff (decision del owner; la alternativa era solo documentarlo). El sintetico **se sigue persistiendo** —`user.email` es `NOT NULL` con unico— pero no se serializa nunca, misma regla que `toClientProgram` con las claves de R2. **(3)** `requireStaffOwner` **ADENTRO del `try`** en las 4 rutas donde hoy queda afuera: un fallo de base en la resolucion de sesion sale **500 sin `code`**, y **el contrato ya declara lo contrario** («las cuatro envuelven todo lo que toca la base, incluida la resolucion de la sesion»), o sea que **hoy el documento miente**. **(4)** borrar `emailOTP`, que quedo sin consumidor al borrarse el arco de recuperacion — medido: **una sola** instancia de better-auth en el repo, y sonda ejecutada (`Object.keys(auth.api)` = 44 endpoints, 16 de OTP/password). **La trampa:** sin el plugin sus 9 paths dan 404 **por inexistentes** y `merchant-auth-disabled-paths.test.ts` **se volveria vacuo**, asi que su oraculo pasa a aseverarse sobre la instancia (`not.toContain("signInEmailOTP")`) con su control. **SACADO DE ALCANCE POR EL OWNER y parqueado con sus dos decisiones ya tomadas (fila 56 de `PARQUEADO.md`): el gate de email en las otras 9 superficies de API** — medido: cubre **2 de 11**, asi que un owner sin verificar crea sucursales, sube marca, arma campañas, carga catalogo y **abre un checkout de Stripe**; y los tres `ownerBusiness` (brand/catalog/loyalty) **no filtran `status='active'`**, que `ownerContext` si filtra. **`core.business.status`**, que el owner pidio el mismo dia, **no existe** y va a la 3ª spec (fila 57); la suscripcion `free` de base **ya existe en toda alta**. Presupuesto **5 mutaciones** con su oraculo escrito de antemano, **2 pasos** con revisor independiente cada uno, **sin migraciones**, **sin UI** (ADR 0070 §16) y **sin QA de pantalla** (declarado: el producto no tiene entrada por navegador mientras dure el arco). **Los barridos del DoD se corrieron contra el arbol antes de cerrar** — leccion de la 0067, que cerro con cuatro criterios imposibles; dos nacieron mal aca y se corrigieron | cerrada | sí | `specs/0068-cierre-de-la-api-de-identidad.md` |
| 0069 | 2026-09-17 | **El wizard de alta es API** (2a tajada del ADR 0070): categoria `gcid:` obligatoria con columna y lista curada de 15, Mexico, prellenado de pais/sesgo geografico, creacion del programa desde dos campos, sello placeholder con la inicial en `#1A1A1A`, y el QR descargable SVG/PNG. **Importa porque es el insumo de la UI que el owner construye por fuera**: sin el contrato escrito no hay wizard. Corrige dos afirmaciones del ADR 0070 medidas como falsas — el placeholder NO llega solo con tocar la ruta publica (`client-view.ts:81` anula el path) y Mexico toca UNA lista, no dos. **IMPLEMENTADA con PASS de revisor independiente** (2026-09-17): 1479 tests verdes con Neon, 0 failed. El revisor cazo que el filtro por `(businessId, programId)` de la ruta publica del sello **no tenia oraculo** —quitarlo dejaba 23 tests verdes— y el orquestador agrego el test y probo que muerde. **Sin commitear.** | **implementada** | si | `specs/0069-el-wizard-de-alta-es-api.md` + anexo `specs/0069-contratos-de-api.md` |
| 0072 | 2026-09-17 | **Entitlements y estado del negocio** (3a tajada del ADR 0070, y absorbe las filas 56 y 57 de `PARQUEADO`). Tres agujeros que comparten los mismos archivos: el conocimiento de plan vive en 3 lugares y **ya divergio una vez** (documentado en `plan-gate.ts:48`), el gate de email verificado cubre **2 superficies de API de 11** —un owner sin verificar abre un checkout de Stripe— y `core.business.status` esta en produccion desde la `0036` **sin un solo lector**: hoy un negocio `suspended` esta suspendido en la base y operativo en la app. Entrega la capa `can()`/`limitOf()` con catalogo declarativo, un `requireApiOwner` unico para las 10 superficies, y los guards de `status` en login, API, mostrador y enroll. **Importa porque el guard tiene que estar en cada superficie y no en la puerta**: las sesiones duran 7 dias por default, asi que un staff ya logueado seguiria acreditando una semana en un negocio cerrado. Corrige tres afirmaciones de `TASKS`/`PARQUEADO` medidas como falsas — `billing/plan-change.ts` NO es un call-site divergido (importa el de locations), staff y catalogo **no tienen tope por plan** (nada que unificar), y las superficies sin gate son **10**, no 9 (`loyalty-program/qr` nacio con la 0069). El corte del **alta nueva** (enrolamiento publico) en `suspended` y `closed` lo decidio el owner el 2026-09-17; lo ya emitido —pase, sellos, tarjeta— no se toca. Presupuesto **7 mutaciones**, sin migraciones, sin UI. **IMPLEMENTADA con PASS de revisor independiente** (2026-09-17): **203 archivos / 1590 tests con Neon, 0 failed** (baseline 1479), 5 gates verdes, y las 7 mutaciones rojas por la asercion correcta — la del tope del catalogo tiro **22 tests en 9 archivos**, o sea que la capa nueva **es** la fuente y no una copia muerta. El revisor gasto sus 4 y cazo **un agujero real fuera del alcance de la spec, reproducido por el orquestador y CERRADO por decision del owner** (2026-09-17): `POST /api/onboarding/program` llamaba a **`saveProgram`, el MISMO writer** que la ruta gateada, con solo `getSession`, asi que **reescribia el programa de un negocio `suspended`** que `PUT /api/loyalty-program` rechaza con 403 — y eso contradecia la regla textual del owner («no pueden … cambios en programa»), o sea que era un incumplimiento y no una decision abierta. **El gate se puso en el WRITER y no en la ruta**, porque es un writer con DOS puertas y una defensa en el borde deja la otra abierta; asi ademas lee el `status` de la misma fila que el write toca. Medido, el hallazgo se acoto de tres rutas a UNA: `prefill` no lee una fila del negocio y `business` solo CREA (409 si ya hay membresia, asi que nunca llega a un write). `businessStatusFailure` se mudo a una **hoja sin imports** (`server/business-status.ts`) para no arrastrar better-auth a un modulo de dominio — la leccion del ciclo del paso 1, aplicada de nuevo. Y en la misma tanda el owner cerro el **landing publico del enroll**, con un criterio que queda como regla: eso se toca al editar la pantalla **salvo que sea propiedad de API**, y medido **lo es** (`getEnrollLanding` vive en el servidor) — se resolvio con una condicion en el `where` y **cero `.tsx`**, porque el mensaje que pidio («Este programa no esta disponible») **ya existia** en la rama `!landing` de la pagina. Y se declaro un **limite medido**: un `status` desconocido es INALCANZABLE contra base porque el `CHECK` de la `0036` lo rechaza incluso por SQL crudo. El orquestador corrigio ademas un `limit(1)` **sin `orderBy`** en el corte del link magico (no determinista, y su propio docblock afirmaba el orden) y **una afirmacion de mas de esta spec**: la divergencia `asc`/`desc` NO se cerro del todo (`loyalty-program.ts:66` sigue `desc`), inalcanzable hoy y declarada. **Sin commitear.** | **implementada** | si | `specs/0072-entitlements-y-estado-del-negocio.md` + anexo `specs/0072-contratos-de-api.md` |
| 0074 | 2026-09-18 | **El hueco de lecturas del API** — el arco contrato las ESCRITURAS (0067/0069/0072), porque ahi viven los guards; las lecturas nunca necesitaron HTTP y por eso no existen: 7 paginas de `/backoffice` son server components que consultan drizzle **en proceso**, incluida la principal, que lee `core.subscription` con un `select` directo. **Importa porque en cuanto la UI se va afuera y habla por HTTP —que es lo que el ADR 0070 §16 ordena— cada lectura in-process se vuelve un endpoint faltante**, y por eso los cuatro huecos son todos de lectura: contexto de sesion (rol/negocio/`slug`/`status`, hoy solo `get-session` de better-auth, que devuelve `user` pelado), plan (billing es **POST-only**, cero `GET`), estado del wizard (`business`/`program` son POST-only: un alta cerrada a mitad no se retoma) y metricas. Entrega los tres primeros como `GET` aditivos que reusan el DTO existente —`billingStateResponse` ya compone el de billing— mas el anexo de contrato que consume quien hace la UI. El de sesion es un **reportero, no un guard**: 200 siempre y **sin** los cinco `code` de la 0072, porque un endpoint gateado por el estado que reporta no deja renderizar la pantalla de cuenta suspendida. **Las metricas quedan AFUERA y declaradas**: que numeros muestra el dashboard es producto y el owner no lo dijo. Presupuesto 5 mutaciones, sin migraciones, cero `.tsx`. **IMPLEMENTADA con PASS de revisor independiente** (2026-09-18): **211 archivos / 1634 tests con Neon, 0 failed, 0 skipped**, 5 gates verdes, las 5 mutaciones rojas por la asercion correcta. **El revisor cazo un invariante ESCRITO Y SIN ORACULO** —borrar el filtro de membresia de `/api/onboarding/state` dejaba los 1631 tests en verde, reproducido por el orquestador sobre la suite entera—; se cerro con dos oraculos, uno Neon y uno unitario que no depende de Neon. La mutacion que mas dice es la de la fuga: con `toSubscriptionView` sustituido por `row`, `billing-routes` y `billing-view` quedan VERDES y solo muerde `read-surfaces-leak`, o sea que esa allow-list es **lo unico** entre Stripe y el navegador — y aseverar el **conjunto exacto de claves** hace trabajo que buscar valores-centinela no hace (probado agregando una clave sin valor secreto). Dos errores del ORQUESTADOR corregidos en vuelo: la tabla de Archivos declaraba «diff aditivo puro» y era falsa (un barrido de inventario de `api/billing/**` exige igualdad exacta), y el encargo declaraba un limite FALSO («los tests Neon no se pueden correr local») cuando `.env.integration.local` esta en la raiz — con 4 de 5 mutaciones apuntando a oraculos Neon. El anexo se contradecia a si mismo sobre `suspensionReason` (gana **solo rol**) y declaraba menos estados de los que la ruta emite (faltaban los dos **503**). **Sin commitear.** | **implementada** | si | `specs/0074-el-hueco-de-lecturas-del-api.md` + anexo `specs/0074-contratos-de-api.md` |
| 0075 | 2026-09-18 | **El QR del wizard no lleva el gate de email.** La pantalla del QR es la CUARTA del wizard (ADR 0070 §1) pero `requireApiOwner` le aplica el paso 3, asi que una cuenta nueva —cuyo `email_verified` nace `false`— crea negocio y programa y despues come un 403 al pedir el resultado de su propia alta. **Importa porque no es una decision abierta sino un incumplimiento**: el owner ya habia dictado que la verificacion bloquea «todo lo que venga DESPUES del wizard» (ADR 0070 §11), y la 0072 barrio una superficie del wizard adentro de las 10 gateadas; el precedente ya existe dos veces (`onboarding/program` y `onboarding/state`). Medido y declarado en la spec: **gatear «solo durante el onboarding» no es implementable** sin inventar el `onboarding_step` que el ADR 0070 prohibe, asi que la ruta pierde el paso 3 siempre y **conserva los otros tres** (sesion, owner activo, eje `status`), con una funcion hermana explicita en vez de un flag que apague el gate. Presupuesto 3 mutaciones —las tres atacan que NO se caiga otro paso—, sin migraciones, cero `.tsx`. **IMPLEMENTADA con PASS de revisor independiente** (2026-09-18): **211 archivos / 1637 tests con Neon, 0 failed, 0 skipped**, 5 gates verdes, y `requireApiOwner` verificado intacto por **shasum de su cuerpo** contra `git show HEAD:` (identico) ademas del `85 inserciones / 0 borrados`. La hermana la consume **una sola** ruta. Las 3 mutaciones del implementador + las 3 del revisor dieron rojo **solo en la fila del QR**, lo que prueba que la copia del guard discrimina y no arrastro a las otras 11. **El revisor cazo ademas un SEGUNDO flake de infra de tests con mecanismo IDENTIFICADO** —`counter-integration-support.ts:209` genera `phone_e164` con `Math.random()` sobre una columna UNIQUE en una rama Neon compartida que acumula filas: colision de cumpleaños del seed, que afecta a **25 archivos de test** y produce rojos indistinguibles de una regresion—; queda **reportado y sin tocar**, fuera del alcance. **El error estuvo otra vez en la SPEC, no en el codigo**: §D1 pedia «cuerpo comun» entre las dos funciones y el Alcance/DoD exigian `requireApiOwner` byte por byte igual — incompatibles; se resolvio por el criterio binario, que es el verificable. **Sin commitear.** | **implementada** | si | `specs/0075-el-qr-del-wizard-no-lleva-el-gate-de-email.md` |
| 0076 | 2026-09-18 | **El permiso de alta vive en la sesion, y el gate de email baja al writer** (ADR). **Que es:** el gate deja la puerta HTTP y pasa a `saveProgram`, que aprende que crear no es editar; en su lugar, un permiso que el servidor escribe en la fila de la SESION al crear la cuenta —no viaja en ningun request, el cliente no lo puede falsificar— acotado a las escrituras del alta y con tres cortes (email verificado · 5 min tras el alta completa · 60 min desde la cuenta). Trae ademas UNA sola ruta de escritura del programa en vez de dos, `kind` aceptado por la API, y el TOS por pais con personalizacion libre en el panel. **Por que importa:** el gate en la puerta es EVADIBLE y esta reproducido contra Neon —la misma sesion sin verificar reescribe el programa por `onboarding/program` (200) y come 403 por `PUT /api/loyalty-program`—, y es la SEGUNDA vez que la misma grieta se abre (la 0072 ya la tapo para el eje `status`); ademas el ADR 0070 §11 registra que el motivo del gate era de NEGOCIO, no de seguridad. **Estado:** aceptada; habilita tres specs disjuntas (A: TOS por pais · B: permiso + invariante en el writer · C: una ruta y `kind`), ninguna escrita todavia. | **aceptada** | — | `adr/0076-el-permiso-de-alta-vive-en-la-sesion-y-el-gate-baja-al-writer.md` |
| 0077 | 2026-09-18 | **El permiso de alta y el invariante crear/editar** (spec B del ADR 0076). **Que es:** el gate de email baja de la puerta HTTP al writer —`saveProgram` aprende que crear no es editar— y en la puerta lo reemplaza un permiso que el servidor escribe en la fila de la SESION al crear la cuenta, con `input: false` para que no sea seteable desde la API y tres cortes (email verificado · 5 min tras el alta completa · 60 min desde la cuenta). **Por que importa:** cierra un bypass REPRODUCIDO —misma sesion sin verificar: `POST onboarding/program` 200 y la fila reescrita a `target:50`, `PUT loyalty-program` 403— que es la SEGUNDA vez que se abre la misma grieta. 5 mutaciones, migracion de una columna, cero `.tsx`. **Estado: IMPLEMENTADA con PASS de revisor independiente** (2026-09-18): **215 archivos / 1682 tests con Neon, 0 failed, 0 skipped**, 5 gates verdes, reproducido por el orquestador. El revisor cazo **un invariante SIN ORACULO** —borrar el `isNotNull` del acortado dejaba los 44 tests en VERDE, y `least(NULL, now()+5min)` devuelve la FECHA, no NULL, o sea que le regalaba 5 min de permiso a una sesion que nunca lo tuvo—; cerrado con un caso de dos sesiones que **muerde** esa mutacion (verificado por el orquestador: mismo ataque, ahora 1 failed). **2 de los 3 hallazgos fueron errores de la SPEC, no del codigo**: un oraculo que pedia 403 donde corresponde 200, y una causa FALSA sobre `overrideAll` que el implementador copio al docblock — las dos corregidas en la spec. | **implementada** | no (journal de migraciones) | `specs/0077-el-permiso-de-alta-y-el-invariante-crear-editar.md` |
| 0078 | 2026-09-18 | **El TOS es por pais** (spec A del ADR 0076). **Que es:** los terminos del programa se eligen por `business.countryCode` con caida a un scope `default`, empezando por EC, en vez del unico `global-draft` hardcodeado; entran ademas los dos defectos medidos del texto legal que ve el consumidor (el «Los sello se acumulan…» por usar el singular de `unitName`, y que `country_code` no esta en ningun `variables_allowlist`, asi que un TOS por pais no podria nombrar a su pais). **Por que importa:** es decision del owner y **no cuesta esquema** — `terms_template` ya tiene `jurisdiction_scope` y el texto libre por clausula ya se acepta; lo que falta son semillas y seleccion. 4 mutaciones, migracion de semillas, cero `.tsx`. **Estado:** cerrada; **implementada por el implementador y SIN PASS de revisor** (2026-09-18): migracion `0038` de semillas aplicada a integracion y verificada por SQL, 5 gates verdes, **219 archivos / 1715 tests con Neon, 0 failed, 0 skipped**, 6 mutaciones rojas por la asercion correcta (4 de la spec + 2 de los hallazgos de TEST del revisor, que devolvio **PASS**). Queda **un hallazgo de PRODUCTO para el owner**: `renderedTerms` acepta cualquier `templateId` `published` sin validar scope y `GET /api/loyalty-terms/templates` no devuelve `jurisdictionScope`, asi que el panel puede escribirle a un comercio EC la plantilla deprecada de `global-draft`. **IMPLEMENTADA con PASS de revisor independiente** (2026-09-19): **219 archivos / 1715 tests con Neon, 0 failed, 0 skipped**, 5 gates verdes, reproducido por el orquestador. Los 2 hallazgos del revisor eran de ORACULO y no de codigo, y se cerraron: «el pais no puede venir del cuerpo» era una **convencion sin test** (su mutacion la rompio con 65/65 en verde; ahora muerde — verificado por el orquestador con su propia mutacion), y el 503 tenia la mitad de contrato cerrable con `vi.mock` en 4 ms. El `0038_snapshot.json` se hizo A MANO y el revisor lo valido con la prueba fuerte: **`drizzle-kit generate` lo leyo como estado previo y dijo «No schema changes» sin escribir nada**. | **implementada** | no | `specs/0078-el-tos-es-por-pais.md` + anexo `specs/0078-contratos-de-api.md` |
| 0079 | 2026-09-18 | **Una sola ruta de escritura del programa, y acepta `kind`** (spec C del ADR 0076). **Que es:** `POST /api/onboarding/program` se BORRA y queda `PUT /api/loyalty-program` como unica escritura, con el compositor adentro (todo lo que el servidor puede completar es opcional; el bloque de dinero de Puntos es obligatorio porque no tiene default seguro), `code` estables en sus 8 errores y `points` aceptado. **Por que importa:** dos puertas sobre un writer es exactamente lo que produjo el bypass de la 0077 y antes el del eje `status`; y el owner decidio que la API no niegue una modalidad que el dominio ya soporta. Modifica a proposito un DoD de la 0075 (`SinGateDeEmail` pasa de una ruta a DOS, aseverado como conjunto exacto). **Estado: IMPLEMENTADA con PASS de revisor independiente** (2026-09-19), reproducido por el orquestador: 5 gates verdes y **220 archivos / 1726 tests con Neon**. `test:e2e` corrido, y su unico rojo (`loyalty.spec.ts:27`) demostrado PREEXISTENTE con una corrida baseline en worktree limpio a `20531b2` —contadores identicos—, no declarado ajeno de palabra. **Los 3 defectos estaban en la SPEC, no en el arbol** (mismo patron que 0077 y 0078): el oraculo de M5 (`business_suspended`) estaba VENCIDO —lo corta el paso 4 del guard sin pasar por `codeForStatus`, y el revisor lo reprodujo dejando ese caso VERDE bajo la mutacion—, la tabla «Archivos» omitia 13 archivos y listaba `validation.ts` como «editar» cuando no tocarlo era lo correcto. Quedan **3 hallazgos menores abiertos** en `TASKS.md` (un docblock sobre-afirmado, un invariante del contrato sin oraculo permanente, y el costo de 5 round-trips por `PUT`). | **implementada** | **no** — orden obligatorio 0077 → 0078 → 0079 | `specs/0079-una-sola-ruta-de-escritura-del-programa.md` + anexo `specs/0079-contratos-de-api.md` |
| 0080 | 2026-09-19 | **Los pendientes del cierre del ADR 0076** (spec CHICA). **Que es:** los tres hallazgos del cierre del arco, pedidos por el owner textual («completa las que quedan en tasks.md»): el invariante de `clauses` del contrato 0079 que no tenia oraculo, un docblock que afirmaba lo que su caso no mide, y el test ajeno de `consumer-recovery` que leia `.at(-1)` de un `select` **sin `ORDER BY`**. **Por que importa:** los tres son defectos de ORACULO, cero produccion — `program-defaults.ts` quedo **byte-identico a HEAD**, verificado contra el blob. **Estado: IMPLEMENTADA con PASS de revisor independiente** (2026-09-19), gates reproducidos por el orquestador: **221 archivos / 1730 tests, 0 failed, 0 skipped** (antes 220/1726 con 1 rojo). **LO QUE ENCONTRO VALE MAS QUE SU ALCANCE: la premisa de la propia spec era FALSA — `[]` es TRUTHY**, asi que la mutacion que la spec proponia media **21/21 en VERDE**. El invariante real es un `clauses` **falsy pero presente** (`null`), y con ese caso la mutacion muerde con **1 test en 1250**. La frase «verificado con una sonda ejecutada» venia de un PASS de la 0079 y se copio **sin ejecutarla**. El revisor confirmo ademas que un `vi.mock` de `wizardClauseTemplateIds` seria un **oraculo MUERTO** (binding local del mismo modulo: el doble no intercepta), y dejo **F1 abierto** — un hueco real del contrato 0079, sin oraculo para «un `clauses` no vacio sobrevive», **absorbido por la 0081**. | **implementada** | **no** — va antes de la 0081 | `specs/0080-los-pendientes-del-cierre-del-adr-0076.md` |
| 0081 | 2026-09-19 | **Las variables del TOS y el monto por sello**. **Que es:** el TOS pasa a nombrar al negocio —sus locales, su direccion, su moneda y el dinero que hace falta para ganar una unidad— con las **12** variables del diccionario; el monto por unidad va en una **SEGUNDA plantilla** (`earning_per_amount`) elegida por el `accrual.mode`; se archiva `global-draft`; `GET /api/loyalty-terms/templates` filtra por el scope del negocio. Entrega el **contrato HTTP escrito** para quien construya la pantalla 3 **por fuera** (decision textual del owner: «TU NO TOCAS UI»). Cero `.tsx`, migracion de **solo datos**. **Por que importa:** `renderTermsText` tira 422 **cuando el valor es VACIO**, asi que una variable «que a veces no aplica» impide guardar el programa. Y el costo de la decision del owner quedo medido: el dominio **ya aceptaba** «un sello cada $X», asi que la spec no cambio el dominio. **Estado: IMPLEMENTADA con PASS de revisor independiente** (2026-09-19), gates reproducidos por el orquestador con exit code explicito: **226 archivos / 1757 tests, 0 failed, 0 skipped**. **CUATRO claims de la spec resultaron FALSOS al medirlos**: «no emitir» y «emitir vacio» son **indistinguibles** — los dos dan el mismo 422, asi que el oraculo que la spec asignaba a su caso trampa **no podia morder**; `transition` de `global-draft` **ya estaba `archived` desde la 0011** y copiar su texto habria reintroducido un 422; el `UPDATE` de allowlist pedido era un **no-op**; y la prosa decia «once» variables donde su tabla listaba doce. El revisor sumo **R4**, que nadie habia medido: el **aislamiento por negocio** de la consulta nueva de locales, hoy pinneado. **Quedan 2 acciones: aplicar la `0039` ANTES del deploy** (sin ella un `per_amount` da 503) y la divergencia `asc`/`desc` entre el resolvedor del guard y el del writer, **preexistente y sin oraculo** (0072 §D3). | **implementada** | **no** — ultima del arco | `specs/0081-las-variables-del-tos-y-el-monto-por-sello.md` + anexo `specs/0081-contratos-de-api.md` |
| 0082 | 2026-09-19 | **El gate de email no bloquea la puerta, bloquea las acciones** (spec CHICA). **Que es:** `requireBackofficeSession` deja de rebotar al owner sin verificar a `/?e=email_not_verified` — entra y **ve** el panel—, y a cambio el **mostrador** (4 rutas, hoy sin gate en ninguna linea) pasa a exigir email verificado **solo al owner**; `operatorBusiness` devuelve el `role` al lado del negocio, no adentro (ADR 0055: no mezclar ejes). **Por que importa:** el rebote contradecia las palabras textuales del owner en ADR 0070 §11 —*«Entra a su cuenta directamente al terminar el wizard y ve el onboarding»*, y el primer paso del onboarding **es** verificar el email, que no se puede ver desde afuera—; entro inadvertido porque la spec 0067 lo implemento cuando el backoffice era inalcanzable a proposito y **no habia QA de pantalla**. Lo reprodujo el owner en prod el 2026-09-19. **Sacar el rebote no abre un agujero, medido:** las 11 superficies owner-only ya devuelven 403 `email_not_verified` (pinneado por `api-owner-surfaces.test.ts`, 13 entradas, 2 excepciones cerradas) y el backoffice tiene **cero server actions** — las 8 paginas son lecturas y toda escritura sale por la API gateada. | **implementada** — PASS de revisor independiente (5 mutaciones, las 5 rojas) | **si** | `specs/0082-el-gate-de-email-no-bloquea-la-puerta.md` |
| 0077 | 2026-09-20 | **El onboarding son DOS recursos: el checklist y el tutorial** (ADR). **Que es:** el checklist («que hay que hacer y en que estado esta», derivado de hechos de la base) y el tutorial paso-a-paso de cada item («toca aca, abri la camara») se sirven por endpoints distintos; la API dicta `position`, `required` y `blocking` (**dos ejes separados**, no uno: «hay que hacerlo» no es «frena a los que siguen»), el texto viaja en la respuesta, y el `anchor` es una CLAVE estable, nunca un selector ni una coordenada. **Por que importa:** el item sobrevive a cualquier rediseño y el tutorial ES la descripcion de una pantalla, asi que juntarlos ata el recurso estable a la cadencia del volatil — y un `anchor` con coordenadas dejaria hotspots apuntando a botones borrados **sin poner rojo a nada en CI**. Cierra las seis decisiones que la 0074 dejo abiertas. **Estado: aceptada**, sin implementar (la implementa la spec 0083). | **aceptada** | — | `adr/0077-el-onboarding-son-dos-recursos-el-checklist-y-el-tutorial.md` |
| 0083 | 2026-09-20 | **El checklist del onboarding** (spec CHICA, implementa el ADR 0077). **Que es:** `GET /api/onboarding/checklist` con UN item (`verify-email`), cuyo hecho ya viaja en la sesion y cuya accion ya existe; catalogo de items en codigo tipado (como `ENTITLEMENTS`), sin migracion y sin tocar un solo `.tsx`. **Por que importa:** **NO lleva gate de email y ese es el punto** — un endpoint que dice «verifica tu email» no puede estar bloqueado por no haberlo verificado, asi que no usa `requireApiOwner` (su escalera evalua el email en el paso 3 y no admite saltarlo, medido) sino las piezas compartidas `ownerContext` + `businessStatusFailure`, salteando SOLO el paso 3 del ADR 0073 §1. Es la pieza que le faltaba a la 0082, que saco el rebote para que el owner pudiera VER este paso. **IMPLEMENTADA con PASS de revisor independiente** (2026-09-20, 6 mutaciones, las 6 rojas y por la propiedad atacada). **Dos correcciones de la SPEC durante su implementacion, las dos del orquestador:** (1) decia «no se escribe un resolvedor nuevo» sin ver que `requireApiOwnerSinGateDeEmail` **ya existia** (`api-owner.ts:174`, pasos 1-2-4) — el implementador armo la escalera a mano y eso creaba una exencion del gate de email **invisible** a los dos mecanismos que el repo usa para contarlas; el owner decidio usar la pieza y **ampliar el inventario cerrado de 2 a 3**; (2) su §D4 afirmaba «cero consultas extra» y la propia enmienda lo volvio falso (el guard devuelve `business`/`userId`, **no la sesion**). La **M6** prueba que ese inventario **gobierna** y no es un numero escrito a mano. | **implementada** | **si** | `specs/0083-el-checklist-del-onboarding.md` + anexo `specs/0083-contratos-de-api.md` |
| 0078 | 2026-09-20 | **El onboarding son CINCO items, y cuatro son TOURS** (ADR, enmienda el 0077 §4-5). **Que es:** el checklist posterior al wizard queda en cinco items y solo UNO deriva de un hecho de dominio (`verify-email`, ya implementado); los otros cuatro son tours de pantalla —staff, catalogo, programa, marca— y los cuatro comparten el mismo `done`: «completo **o SALTEO** este tour». El progreso se guarda **POR NEGOCIO** (decision del owner) y `completed`/`skipped` se persisten DISTINTOS aunque los dos proyecten `done: true`. **Por que importa:** **MATA el segundo recurso `GET /api/onboarding/guide/{item}`** que el 0077 §5 habia diferido — el contenido del tour son componentes de `driver.js` en el cliente, no JSON del servidor, asi que la API sirve **estado y no contenido**; y como «vi un tour» no es derivable de ninguna tabla (medido: cero columnas de «visto» en el schema, y `loyalty_program.updated_at` es `defaultNow()` **sin `$onUpdate`**, o sea que revisar-sin-tocar no deja rastro), **el checklist deja de ser lectura pura y gana su primera tabla y su primera escritura**. **La libreria se eligio MIDIENDO tres candidatas en movil** (iPhone 13, build de produccion de Next 16): gana `driver.js` porque **no declara ni un peer ni una dependencia** —asi no puede acoplarse a una version de Next— y porque fue la que mejor recorto el spotlight sobre un CTA `position: fixed`. Se descarto `@tour-kit/react`, cuyo `useNextAppRouter()` **rompe el build** bajo Turbopack (*dynamic usage of require is not supported*) — **el typecheck no lo distingue, solo lo caza `build`, que no esta en el Stop hook**. Costo aceptado textualmente por el owner: un merchant puede terminar el onboarding salteando los cuatro tours. **Estado: aceptada**; la implementan las specs 0084 y 0085. | **aceptada** | — | `adr/0078-el-onboarding-son-cinco-items-y-cuatro-son-tours.md` |
| 0084 | 2026-09-20 | **El progreso de los tours del onboarding** (implementa el ADR 0078 §2-3). **Que es:** tabla nueva `core.business_onboarding_tour` con PK `(business_id, tour_id)` —**sin `user_id`: el progreso es por NEGOCIO**— y `POST /api/onboarding/tours/{tourId}` con `status: "completed" | "skipped"`, idempotente por upsert, mas el catalogo `ONBOARDING_TOURS` que la 0085 va a consumir. **Por que importa:** es la **PRIMERA escritura del onboarding** (el checklist era lectura pura) y la primera vez que el `blocking` de `verify-email` se **HACE CUMPLIR** en vez de solo reportarse: a diferencia del checklist, esta ruta **SI lleva el gate de email** —el checklist se exime porque se gatearia a si mismo, esta no tiene ese problema— asi que **el inventario de exenciones sigue en 3** y `SURFACES` pasa de 14 a 15. El invariante propio: **`completed` NUNCA se degrada a `skipped`** (el `where` del `do update`), porque los dos proyectan `done: true` y sin eso el dato que el owner pidio conservar se destruiria en silencio. **Serializada DESPUES de si misma y ANTES de la 0085**, que toca los mismos archivos de inventario. | **implementada** | **si** (colisiona con la 0085: serializar) | `specs/0084-el-progreso-de-los-tours-del-onboarding.md` + anexo `specs/0084-contratos-de-api.md` |
| 0085 | 2026-09-20 | **El checklist pasa a CINCO items, y pierde un campo** (spec CHICA, implementa el ADR 0078 §1). **Que es:** `CHECKLIST_ITEMS` va de 1 a 5 —`verify-email` mas los cuatro TOURS (staff, catalogo, programa, marca), cuyos ids salen de `ONBOARDING_TOURS` y no de una segunda lista— y su `done` sale de `core.business_onboarding_tour`: **`completed` Y `skipped` cuentan los dos como `done: true`**, y el JSON no dice cual fue. **Y `blocking` SE BORRA** del tipo, del JSON y del contrato. **Por que importa:** el owner pregunto por que habia dos campos y la respuesta medida es que no hay dos — su definicion de `required` (*«sin eso no se puede hacer nada mas»*) **ES** la de `blocking` del ADR 0077 §2, y con los cinco items reales los dos ejes **nunca divergen** (8 lineas en `checklist.ts` y 14 en `checklist.test.ts` existian solo para sostener la distincion: andamiaje sin su tarea). Se hace AHORA porque el endpoint ya esta pusheado pero **ninguna UI lo consume**, asi que sacar un campo cuesta cero hoy. Corrige ademas el contrato 0083 §5, que le anuncia a quien construya la UI un `GET /api/onboarding/guide/{item}` **que no existe ni va a existir**. **Trampa MEDIDA que trae:** el doble de `./db` de `api-owner-surfaces.test.ts` es una cadena fija con `.limit()`, y la consulta de tours no lo usa — contra ese doble la ruta cae a **503** y se lleva toda la bateria; hay que extenderlo, y el archivo esta en **299** de **300**, asi que toca DIVIDIRLO. | **implementada** | **no** (serializada DESPUES de la 0084) | `specs/0085-el-checklist-pasa-a-cinco-items.md` |
| 0079 | 2026-09-20 | **Los permisos del staff son alcances POR OBJETO, y toda accion de configuracion deja rastro** (ADR; escribe lo que los ADR 0008 y 0010 dieron por sentado y el 0044 difirio). **Que es:** siete toggles —staff, locales, fidelizacion, marketing, catalogo, marca, mostrador—, **uno por objeto y nunca una matriz CRUD** (palabras del owner); leer/crear/editar si, lo IRREVERSIBLE no se delega nunca y DESACTIVAR no cuenta como destructivo; el permiso `staff` es el perfil **ADMINISTRADOR** y solo el owner lo otorga, porque el alta devuelve el PIN en claro y por eso crear a un tercero con el permiso X **equivale a tener X**; los permisos viven en `business_membership.permissions text[]` (el guard ya lee esa fila: cero consultas nuevas), viajan en `GET /api/merchant/session` y se evaluan en UN guard con el orden del ADR 0073 intacto; y toda accion de configuracion —tambien las del owner— escribe en `core.activity_log` **en la misma transaccion**, con los nombres en TEXTO PLANO. **Por que importa:** hoy el rol ES el permiso y esta medido que el staff llega a **una sola superficie** (`/api/counter/*`) mientras las otras once contestan `403 not_owner`, asi que delegar cualquier cosa obliga hoy a entregar la cuenta del owner; y el eje PLAN y el eje ESTADO alcanzan al staff **por construccion** —`EntitlementContext` no tiene nocion de usuario y el login por PIN ya corta con `businessStatusFailure`—, que era la condicion que el owner puso. La escalada residual del perfil administrador (cuenta interpuesta ⇒ los seis permisos no-`staff`) esta **aceptada por escrito** y acotada; el PIN fuera de banda queda en `PARQUEADO`. **Estado:** aceptada; la implementan tres specs (A permisos y guard · B log de auditoria · C archivado del catalogo), ninguna escrita todavia. Dos hallazgos a decidir quedan marcados en el ADR. | **aceptada** | — | `adr/0079-los-permisos-del-staff-son-alcances-por-objeto.md` |
| 0080 | 2026-09-21 | **El identificador del staff SIGUE AL NOMBRE, y editar va en DOS rutas separadas** (ADR). **Que es:** renombrar a un integrante **re-deriva su handle**, asi que el `identifier` nunca queda desincronizado del nombre —a cambio de que **cada renombre cambia con que string entra esa persona**—; no rompe un solo dato (nada interno referencia el handle: FK, auditoria y **sesion activa** van por `userId`, medido) y entregar un identificador nuevo **no es una operacion nueva** para el merchant, que ya lo hace al regenerar un PIN. La condicion que lo vuelve sano, y es parte de la decision: la respuesta **siempre devuelve el `identifier` nuevo**. Editar va en `PATCH /api/staff/{userId}` (solo el nombre) + `PATCH …/permissions` **intacta**, nunca fusionadas: **sus politicas de autorizacion son distintas** —al nombre se lo edita uno mismo, a los permisos no— y en una ruta unica conviven en el mismo camino de codigo, donde un handler que autorice por *request* en vez de por *campo* es una escalada esperando un refactor; la ruta del nombre **RECHAZA** `permissions` con 400 en vez de ignorarlo, porque el descarte silencioso es la version peligrosa del mismo bug. **Por que importa:** el precedente es propio y de esta misma semana — en la 0086 `requireStaffOwner` era compartida y cambiarla en el lugar habria **delegado el slug publico con el permiso `staff` sin que una linea de esa ruta cambiara**. | **aceptada** | — | `adr/0080-el-identificador-del-staff-sigue-al-nombre.md` |
| 0081 | 2026-09-21 | **El checklist suma Locales, y la pantalla se delega** (ADR; enmienda el 0078 §1 y la decision 4 de la spec 0061). **Que es:** el checklist pasa de CINCO a SEIS items —entra `locations` en la posicion 2, como TOUR y no como hecho de dominio— y `/backoffice/locations` deja de ser `requireOwner()` para gatearse por el permiso `locations`. **Por que importa:** la API de `/api/locations/*` ya estaba delegada desde la 0086, asi que la pantalla cerrada dejaba el permiso **vivo en la API y muerto en el producto** — mismo patron que la enmienda §10 de la 0086, y no es decision nueva sino cumplir la que el owner ya dio. Que el item sea un tour esta **medido**: el wizard del alta YA crea el primer local, asi que un item «tenes un local» naceria `done: true` para todos. La nav se generaliza con un criterio explicito —se le muestra un link a un integrante solo si la PANTALLA ya esta gateada por permiso, hoy Staff y Locales— porque ofrecer una puerta que rebota es peor que no ofrecerla. | **aceptada** | — | `adr/0081-el-checklist-suma-locales-y-la-pantalla-se-delega.md` |
| 0082 | 2026-09-21 | **Importacion de catalogo con IA agnostica y borrador revisable** (ADR). **Que es:** la IA nunca escribe el catalogo — archivos privados temporales → analisis → **borrador versionado que el merchant revisa** → aceptacion idempotente y transaccional; el dominio consume un contrato propio (`CatalogExtractionProvider`) y no los formatos de OpenAI/Claude/Kimi, y proveedor y modelo son **configuracion evaluable**; duplicados y precios ambiguos exigen una decision del merchant, y los originales se borran al aceptar, cancelar o vencer. **Por que importa:** una extraccion probabilistica que escribiera directo confunde `$3,50` con `$35,00` en el catalogo real, y hablar en `response_id` en las rutas seria lock-in de un proveedor cuyo rendimiento relativo cambia cada mes. **Y la enmienda del 2026-09-21 (§8-§12) es la mitad del valor del ADR, porque la escribio una revision contra el arbol:** el «worker» de la §4 **no era implementable en Hobby** —los 2 crons de `vercel.json` son el maximo y un tercero hace que Vercel rechace el deploy ENTERO—, asi que el trabajo largo **se delega en el proveedor** (`background: true` + callback firmado con Standard Webhooks, verificado en la doc y no de memoria) y el scheduler externo pasa de worker a **reconciliador**; el precio queda en **dos** estados y **ninguno bloquea** (un `ambiguous` nace con `unit_price` **null**, nunca `0` — el bloqueo de la §6 solo tenia sentido mientras el valor fuera cero, que es un precio falso con cara de valido); el cupo de analisis es un **valor centralizado con ventana** (1/dia, y **los fallos no lo consumen**); el PDF se cuenta con **`node:zlib`, sin dependencias** (el escaneo naive devuelve **0** en un PDF con `/ObjStm`, o sea que uno de 400 paginas pasaria un check de `<=10`); y se avisa por email al terminar (**el aviso por email lo ELIMINO el ADR 0085 el 2026-09-23**). | **aceptada** | — | `adr/0082-importacion-de-catalogo-con-ia-agnostica-y-borrador-revisable.md` |
| 0083 | 2026-09-22 | **El cupo de importacion cuenta resultados y submits aceptados.** El analisis diario se consume solo con borrador; el fusible operativo observa `provider IS NOT NULL`, no `attempt_count > 0`. Configuracion, preparacion y submits rechazados no bloquean al merchant; el presupuesto se valida antes de firmar uploads y otra vez antes de invocar al proveedor. | **aceptada** | — | `adr/0083-el-cupo-de-importacion-cuenta-resultados-y-submits-aceptados.md` |
| 0084 | 2026-09-23 | **La importacion de catalogo escribe directo y solo agrega** (ADR; supersede el invariante «la IA nunca escribe» del ADR 0082 §1, y con el su §5, §6 y §13). **Que es:** subir → extraer → **el servidor escribe el catalogo** en una transaccion, conciliando contra el catalogo ACTUAL: reusa categorias, omite productos que ya existen dentro de su categoria, crea lo que falta y **nunca actualiza ni borra nada**. La primera importacion y la quinta son la MISMA operacion, asi que el LLM **no** recibe el catalogo existente: lo sabe el servidor, que es quien escribe. Un precio que no parsea crea el producto **sin precio** (filtro «sin precio» en la pantalla, y el mostrador se lo pide al operador); un item ilegible **se descarta** y se lista para cargarlo a mano. **Por que importa:** el borrador revisable del 0082 le devolvia al merchant la misma carga a mano que la feature venia a ahorrar —90 productos campo por campo en un modal, en el telefono—, y **toda** la maquinaria (borrador, resoluciones, `draft_version`, `PUT /draft`, `POST /accept`, estado `ready`) existia solo para sostener ese invariante: cambiarlo **borra codigo**. Se descarta explicitamente la version del 0084 del 2026-09-22, que resolvia lo mismo **agregando** maquinaria (contexto en el prompt, flag de autoaceptacion, `ready` como escape hatch, trazabilidad y lock por negocio) encima de la que iba a borrarse. Riesgo aceptado por el owner: un precio mal leido con confianza alta entra al catalogo y se corrige editando el producto. | **aceptada** | — | `adr/0084-la-importacion-de-catalogo-escribe-directo-y-solo-agrega.md` |
| 0085 | 2026-09-23 | **La importacion de catalogo no manda email** (ADR; supersede el aviso del ADR 0082 §12 y el invariante §7 de la spec 0091). **Que es:** se ELIMINA el aviso por email al terminar una importacion —modulo, copys, tests, las dos llamadas de `finish.ts` y la columna `notified_at`—, no se apaga con un flag. Decision del owner el 2026-09-23, textual: *«jamas pedi que el cliente reciba un email»*. **Por que importa:** el aviso existia para un flujo que ya no existe — en el ADR 0082 el analisis terminaba en un borrador que alguien tenia que ir a revisar, y el mail traia a esa persona a hacer trabajo; desde el ADR 0084 termina con **el catalogo escrito** y no hay nada que ir a hacer. Cierra ademas un hueco de verificacion **borrandolo**: la mitad «una sola vez» de `notified_at` no tenia oraculo posible en unidad (el doble de `./db` ignora el `where`) y deja de tener que probarse. **Consecuencia operativa que no es opcional:** el codigo que corre en prod ESCRIBE esa columna, asi que el `DROP COLUMN` va **DESPUES** del deploy. No se toca `server/email/**`, que lo usan recuperacion de contraseña y alta de staff. | **aceptada** | — | `adr/0085-la-importacion-no-manda-email.md` |
| 0086 | 2026-09-23 | **El alta manual de catalogo se bloquea durante una importacion** (ADR). **Que es:** mientras el negocio tiene un import ABIERTO, crear categoria o producto a mano devuelve **409**; editar, renombrar y borrar siguen permitidos, porque el writer es estrictamente aditivo y no compite con ellos. El bloqueo es del **SERVIDOR**; la pantalla solo lo refleja. **Por que importa:** en vez de resolver las dos carreras contra el writer, las vuelve improbables — la del **23505 de categoria**, cuyo *interleaving* real no tenia oraculo, y la del **producto duplicado**, que la spec 0091 habia declarado afuera porque evitarla costaba serializar las escrituras. **El owner propuso bloquear PRODUCTOS y la carrera del 23505 es de CATEGORIAS** (crear un producto a mano no crea ninguna categoria): se bloquean las dos altas, y asi se cierran las dos carreras. La recuperacion del 23505 **no se borra**: sigue siendo la red de lo que el guard no ve, porque dos requests simultaneas todavia pueden cruzarse. | **aceptada** | — | `adr/0086-el-alta-manual-se-bloquea-durante-una-importacion.md` |
| 0086 | 2026-09-20 | **Los permisos del staff y el guard unico** (spec A del ADR 0079; su contrato es `specs/0086-contratos-de-api.md`). **Que es:** `business_membership` gana `permissions text[]` con **tres CHECK** que vuelven imposibles los tres estados invalidos —permiso desconocido, owner con permisos, staff con cero— y **SIN backfill: la migracion BORRA las membresias de staff existentes** (decision del owner — estamos en prueba, las recrea a mano), porque `ADD CONSTRAINT CHECK` valida las filas viejas; se borra la membresia y **no** el `user`, que la FK sin `onDelete` de `order.created_by_user_id` protege; nace `requireApiPermission` (la escalera del ADR 0073 con un paso nuevo entre membresia y email, y el paso de email salteando al staff porque su email es sintetico) y **diez** superficies migran a el mientras las **cuatro de la CUENTA** —billing, slug, onboarding, estado— se quedan en `requireApiOwner`; `POST /api/staff` exige `permissions` con al menos uno, nace `PATCH /api/staff/{userId}/permissions` como **reemplazo total** con las cuatro reglas anti-escalada, el mostrador pasa a exigir `counter` y `GET /api/merchant/session` devuelve `permissions` —los **siete** para un owner, aunque su fila este vacia—. **Por que importa:** hoy el rol ES el permiso y delegar cualquier tarea obliga a entregar la cuenta del owner; y trae un **cambio de contrato declarado** — `not_owner` desaparece de las diez delegables y lo reemplazan `not_member` y `missing_permission`. El eje PLAN **no se toca**: sigue en el writer, y por eso el staff hereda los topes sin una linea nueva (medido: `EntitlementContext` no tiene nocion de usuario). Presupuesto **6 mutaciones**; `test:e2e` **no aplica** y se demuestra con `git diff`. **Estado: IMPLEMENTADA** (2026-09-21) con **PASS** de un revisor independiente. Se ejecutaron **8 mutaciones + 9 del revisor**; la enmienda **§10** cerro un bloqueo que el guard no veia —`brand.ts` y `loyalty-program/owner.ts` re-resolvian el negocio con `role='owner'` e ignoraban el `businessId` del guard, asi que las dos superficies quedaban delegadas pero muertas— con un `businessId` **opcional** que deja el camino del owner intacto. | **implementada** | **no** (serializa con la spec B; colisiona en las diez superficies y en `staff-permissions.ts`) | `specs/0086-los-permisos-del-staff-y-el-guard-unico.md` |
| 0087 | 2026-09-21 | **Editar el nombre del integrante** (implementa el ADR 0080; su contrato es `specs/0087-contratos-de-api.md`). **Que es:** nace `PATCH /api/staff/{userId}` y edita **solo el nombre**, re-derivando el handle y devolviendo **siempre** el `identifier` nuevo; la derivacion pasa por `nextSuggestion` —asi hereda gratis las reservadas y el sufijo de colision, que es la razon tecnica de que el handle **no** sea un campo libre— y **excluye el handle actual del target**, sin lo cual renombrar «Carla» a «Carla» devolveria `carla-2` y bumpearia el sufijo en cada renombre; la ruta **rechaza con 400 `permissions_not_here`** un cuerpo con `permissions` **mirando la PRESENCIA de la clave y no su valor** (porque «es el mismo valor, es un no-op» es el bypass), reusa los `code` del alta (`invalid_body`/`name_required`/`name_too_long`) sin inventar ninguno, y mantiene R4 (`409 target_is_owner`) mientras R3 **no** aplica: uno mismo **si** edita su propio nombre. **Por que importa:** hoy un nombre mal escrito en el alta **no se arregla** —no hay `PATCH` ni `DELETE /api/staff`— y el error se fosiliza en el string que esa persona tipea para entrar. Sin migracion. Presupuesto **5 mutaciones**, las cinco verificadas contra el arbol antes de cerrar; `test:e2e` **no aplica** y se demuestra con `git diff`. **Estado: IMPLEMENTADA** (2026-09-21) con **PASS** de un revisor independiente que **no repitio** las 5 mutaciones y gasto su presupuesto en cinco propias, apuntadas a lo que la tabla no cubria — y encontro que `rejectionFor` estaba scopeada por negocio **sin oraculo**: el caso de aislamiento usaba un target `role='staff'`, que con el scope roto **sigue dando 404**, asi que el unico vector que lo distingue es el **owner de otro negocio** (409 `target_is_owner`, que filtra existencia). Cerrado con una 6ª mutacion **medida**. | **implementada** | **si** | `specs/0087-editar-el-nombre-del-integrante.md` |

| 0088 | 2026-09-21 | **Tour interactivo de Staff** (la primera pantalla del backoffice gateada por PERMISO y no por `requireOwner`). **Que es:** la UI de Staff mas cinco recorridos de `driver.js` — una orientacion breve que el checklist lanza con `?tour=onboarding` (la unica que persiste progreso, con la API de la 0084) y cuatro ayudas operativas independientes que ejecutan acciones REALES. Copy y selectores en archivos; sin migracion y sin endpoint nuevo. **Por que importa:** es el primer item del checklist de la 0085 que deja de ser una promesa, y el molde para los otros tres tours. **Y lo que ensenio son DOS rondas de revision independiente con FAIL** (nueve hallazgos, los nueve cerrados): el defecto caro fue un paso que pedia el clic que APAGA el unico permiso del alta —y su gemelo en el recorrido de baja, donde el anchor es un TOGGLE—, dos veces la misma familia; el gate de permiso de la pantalla **no tenia oraculo** (borrarlo dejaba 1.526 tests verdes); y un cambio de CSS **compartido** metio un efecto lateral en SEIS pantallas ajenas a la spec, primero oscureciendo el dialogo y despues —al revertirlo a medias— dejandolo con texto blanco sobre blanco en modo oscuro. **La condicion de corte del ADR 0062 quedo ACTIVADA** por el revisor de la segunda vuelta: el resto va a QA de pantalla, no a una tercera ronda. 5 mutaciones de cierre, las cinco rojas, en `archivo/spec-0088-bitacora-de-mutaciones.md`. | **cerrada** — codigo completo, gates verdes y las dos revisiones cerradas; falta el QA del owner de los 3 puntos del handoff y el `test:e2e` de la CI | **no** | `specs/0088-tour-de-staff.md` + bitacora en `archivo/spec-0088-bitacora-de-mutaciones.md` |
| 0089 | 2026-09-21 | **Contrato de API de LOCALES** (implementa el ADR 0081; es un contrato, no una feature nueva). **Que es:** las cuatro rutas —listar, crear, editar, archivar/reactivar— escritas para quien construya la pantalla por fuera: la escalera de 5 rechazos con su `code`, las **dos formas** del `address` (sugerencia de Geoapify re-verificada en el server vs. texto tipeado **sin** coordenadas, porque un punto inventado seria indistinguible de uno verificado), y las dos reglas que sorprenden — **no se archiva el ultimo local activo** y **reactivar consume cupo del plan** (sin eso «archivar → crear → reactivar» dejaria a un Free con dos activos). **Por que importa:** el codigo estaba entero y sin contrato escrito, que es lo unico que el arco del alta entrega ademas de la API (ADR 0070). Deja dicho ademas que la entidad que cruza al navegador tiene **cuatro claves** y ninguna interna, y que el tope lo decide el server **bajo el lock del negocio**: esconder el boton es cortesia, el 409 es la regla. | **cerrada** | **no** | `specs/0089-contratos-de-api.md` |
| 0090 | 2026-09-21 | **Importar catalogo desde imagen o PDF** (implementa el ADR 0082 **y su enmienda**; su contrato es `specs/0090-contratos-de-api.md`). **Que es:** un PDF o 1-10 fotos suben firmadas a R2 privado, `analyze` responde **202 en el orden de un segundo** y el analisis corre **del lado del proveedor**; un **callback firmado** lo retoma —verifica la firma antes de tocar la base, toma solo el `id` y **va a buscar el resultado a la API porque el cuerpo del webhook no se cree**— y un reconciliador cada 5 min cubre el webhook perdido, el `after()` muerto y el desarrollo local. Despues: borrador versionado, duplicados con **la misma normalizacion que el indice unico real**, y `accept` todo-o-nada idempotente por key. **Por que importa:** es la unica forma de que cargar el menu inicial no sea tipear 60 productos a mano — y es la primera superficie del repo que **gasta plata por request**, asi que el cupo (1/dia, centralizado en entitlements, **los fallos no lo consumen**) es parte de la feature y no un extra. **Esta es la SEGUNDA version de la spec: la primera se reviso contra el arbol y tenia siete bloqueantes y TRES afirmaciones de mecanismo falsas** —el «rate limit comun de escrituras» que no existe, la firma de R2 que «ata el tamano» (ata `Key` y `ContentType`, y rechaza todo lo que pase de 5 MB) y el limite de paginas de PDF sin mecanismo—, mas un `analyzing` sin salida que dejaba al negocio sin poder importar nunca mas. **SIN UI: cero `.tsx`** (ADR 0070) — y la API se ajusto a la pantalla que YA existe, que manda **un** archivo, no edita el borrador y cierra el modal sin cancelar: de ahi que `POST` se **apropie** de un import abandonado en `pending_upload` (si no, el segundo intento quedaba trabado en 409) y que **no** se apropie de uno en `ready` (descartarlo quema el analisis del dia), por lo que `test:e2e` **no aplica** y se demuestra con `git diff --stat`. Presupuesto **8 mutaciones** con su condicion de corte. | **implementada (2026-09-22)** — PASS de revisor independiente en DOS vueltas: la primera dio **FAIL** con **cinco mutaciones sobrevivientes** (ninguna era un defecto vivo: eran lineas borrables **con los seis gates en verde** — el filtro por negocio del `accept`, el `lte(expires_at)` del cleanup diario, el `toImportDTO` de la ruta que abre la pantalla, y un **falso oraculo** que doblaba `where()` ignorando el predicado); cerrados los cuatro hallazgos, la vuelta 2 re-midio las cinco en **ROJO** a alcance completo. Un **TERCER hermano** del aislamiento (`activeImport`) aparecio ahi y lo cerro el orquestador, medido en dos tiempos. Migracion **aplicada en Neon `main`** y verificada por MCP. **LIMITE DECLARADO: las 6 suites `.neon.integration` NUNCA CORRIERON** (583 tests en `skipped`, y un `skipped` se lee igual que un `passed`) — quedan sin oraculo ejecutado M4, M8, media M9, RM1 y la atomicidad real del `accept`. **Las corre la CI al pushear.** Marcada por instruccion explicita del owner, con el estado real escrito en la propia spec | **no** — toca catalogo, schema, entitlements, `r2.ts` y cleanup | `specs/0090-importacion-de-catalogo-desde-imagen-o-pdf.md` |
| 0090-c | 2026-09-21 (reescrita el 2026-09-23 por la spec 0091) | **Contrato de API de la IMPORTACION DE CATALOGO** (es un contrato, no una feature: lo consume quien construye la pantalla). **Que es:** las rutas de `/api/catalog/imports/*` escritas para el owner que arma la UI por fuera — la escalera de 5 rechazos con su `code`, el **modelo mental** (el analisis es asincrono **a favor**: se sube, se cierra la pantalla y se vuelve; **el aviso por email lo ELIMINO el ADR 0085**, asi que la pantalla que vuelve encuentra el `result` del ultimo import) y las reglas que sorprenden. **Actualizado por la 0091 / ADR 0084:** la importacion **escribe el catalogo sola y es siempre aditiva**, asi que **no hay borrador, ni revision, ni aceptacion**; `PUT /{id}/draft` y `POST /{id}/accept` **ya no existen** (404 por inexistencia de la ruta), el estado `ready` desaparecio y con el cuatro `code` de la lista cerrada. El DTO perdio `draft` y gano **`result`** —creadas, reusadas, omitidas, sin precio y la lista de descartados—, y `GET /api/catalog/imports` devuelve **el ULTIMO import, terminal incluido**, para que un reload no pierda el resultado. **El precio lo decide el SERVIDOR** a partir del texto impreso: lo que no parsea sin ambiguedad nace `null`, **nunca `0`**. La plata sigue viajando como **string decimal**. **Por que importa:** es lo unico que el arco del alta entrega ademas de la API (ADR 0070), y sin el la pantalla se disenaria leyendo codigo — o peor, programando contra rutas muertas. Deja dicho ademas que la respuesta **no** incluye claves de R2, el id del trabajo del proveedor, tokens, costo ni **la extraccion cruda del modelo**. | **cerrada** | **si** | `specs/0090-contratos-de-api.md` |
| 0091 | 2026-09-23 | **La importacion de catalogo escribe directo** (implementa el ADR 0084; plantilla **chica**: un dominio, **cero migraciones**, decisiones cerradas). **Que es:** nace una clave canonica unica y un **plan aditivo puro** (categoria por clave, con empate resuelto por la mas vieja; producto por `(categoria, clave)`), y un writer transaccional que **relee el catalogo adentro de la transaccion** y escribe; el 23505 de categoria **se reusa**, no falla. El precio lo parsea **el servidor** desde el texto impreso —el proveedor deja de decidirlo— y lo que no parsea nace `null`, nunca `0`. Se **BORRAN** `draft.ts`, `draft-save.ts`, `PUT /{id}/draft`, `POST /{id}/accept`, el estado `ready` y cuatro codigos de error; `GET /api/catalog/imports` pasa a devolver el **ultimo** import para que un reload no pierda el resultado. **Por que importa:** es la spec que hace la feature usable, y su firma es que **quita mas codigo del que agrega**. **Cero `.tsx`** (ADR 0070): la pantalla la hace el owner y por eso `test:e2e` **no aplica**, demostrado con `git diff --stat`. Presupuesto **6 mutaciones**; declarado afuera: la calidad de la extraccion (la mide el corpus), el precio mal leido con confianza alta y la carrera del alta manual. | **implementada** (PASS del revisor independiente + 2 oraculos agregados por el orquestador) | **si** | `specs/0091-la-importacion-de-catalogo-escribe-directo.md` |
| 0092 | 2026-09-23 | **Sin email, y el alta manual se bloquea durante una importacion** (implementa los ADR 0085 y 0086; plantilla **grande** porque lleva migracion). **Que es:** (1) se borra el aviso por email entero, incluida la columna `notified_at` y su migracion generada por `drizzle-kit`; (2) nace `assertNoOpenImport`, que `createCategory` y `createProduct` llaman para devolver **409 `catalog_import_in_progress`** mientras hay un import abierto, `CatalogError` gana un `code` **opcional** —el dominio catalogo no tiene lista cerrada de codigos, a diferencia de catalog-import— y `GET /api/catalog` suma `importInProgress` para que la pantalla lo refleje sin una segunda llamada. **Por que importa:** cierra dos de los tres huecos que dejo la revision de la 0091, y uno de ellos lo cierra **borrando la funcion** en vez de escribirle un test. **El orden de despliegue no es opcional:** deploy primero, `DROP COLUMN` despues, porque el codigo que corre en prod escribe esa columna. Presupuesto **4 mutaciones**, dos de ellas de **cableado** (el guard puede existir y no estar llamado). Declarado afuera: dos requests exactamente simultaneas siguen pudiendo cruzarse, y por eso la recuperacion del 23505 se conserva. | **implementada** — PASS de revisor independiente (4+2 mutaciones, y **las dos de CABLEADO sobreviven la suite de unidad entera**: solo Neon las caza) | **si** | `specs/0092-sin-email-y-el-alta-manual-se-bloquea.md` |
| 0093 | 2026-09-24 | **El modal de importacion no se cuelga** (plantilla chica; pedido del owner en QA). **Que es:** fuera el copy de email → mensaje rotativo; el modal no se cierra mientras sube/analiza (queda «Cancelar»); un resultado ya visto no traba el modal al reabrir. **Por que importa:** hoy no se puede importar un segundo menu sin recargar. | **implementada** (6 gates verdes + PASS del revisor, 3+1 mutaciones) | **si** | `specs/0093-el-modal-de-importacion-no-se-cuelga.md` |
| 0094 | 2026-09-25 | **El modal muestra que esta trabajando** (plantilla chica; pedido del owner tras el QA de la 0093). **Que es:** mientras procesa, el icono late, cada mensaje entra con fundido y corre una barra indeterminada (apagado con `prefers-reduced-motion`); «Cancelar importación» a ancho completo. **Por que importa:** que el merchant no crea que se colgo. | **implementada** (6 gates + PASS del revisor; 0 mutaciones, sin oraculo visual) | **si** | `specs/0094-el-modal-muestra-que-esta-trabajando.md` |
| 0095 | 2026-09-25 | **Catalogo: boton de IA y filtros del sistema de diseño** (plantilla chica; pedido del owner). **Que es:** «Probar importador» → «Importar con IA»; los filtros de categoria y local pasan de `<select>` nativo a `SelectField` (gana `hideLabel`); se borra el CSS `.catalog-filter`. **Por que importa:** la pantalla usa un solo lenguaje de inputs, el del wizard. | **implementada** (6 gates + PASS del revisor) | **si** | `specs/0095-catalogo-boton-ia-y-filtros-del-sistema.md` |

**"Disjunta?"** = si el trabajo no comparte archivos con otra spec abierta. Es lo que
habilita paralelizar. Lo decide la spec, no el orquestador en runtime.

## Tours de catálogo — alcance acordado el 2026-09-25

- **ADR 0087:** [Los tours de catálogo son UI y consumen estado del API](adr/0087-los-tours-de-catalogo-son-ui-y-consumen-estado-del-api.md).
  Fija orientación/ayudas, avance confirmado y responsabilidades Codex UI / Claude Code API.
  Estado: **aceptada**; API existente suficiente, sin desarrollo backend requerido.
- **Spec 0096:** [Tours de onboarding y ayuda del catálogo](specs/0096-tours-de-onboarding-y-ayuda-del-catalogo.md).
  Cuatro pasos de orientación y ocho ayudas, con permisos, errores y validación móvil.
  Estado: **cerrada**, UI implementada localmente con PASS independiente acotado; QA real pendiente; no disjunta.
  [Handoff y evidencia](archivo/spec-0096-handoff-ui.md), [revisión independiente](archivo/spec-0096-revision-ui.md); sin cambios de API.

- **ADR 0088:** [Los tours de Marca acompañan el borrador y el guardado](adr/0088-los-tours-de-marca-acompanan-el-borrador-y-el-guardado.md).
  Cinco pasos, seis ayudas, guardado completo y acceso por permiso brand; afiche separado.
  Estado: **aceptada**; API existente suficiente, sin cambio backend identificado.
- **Spec 0097:** [Tours de onboarding y ayuda de Marca](specs/0097-tours-de-onboarding-y-ayuda-de-marca.md).
  Recorridos aprobados, contratos, recuperación del borrador, permisos y verificación móvil.
  Estado: **cerrada**, UI pusheada con PASS independiente, seis gates y aprobación general del owner; QA específico pendiente; no disjunta.
  [Handoff](archivo/spec-0097-handoff-ui.md), [revisión independiente](archivo/spec-0097-revision-ui.md), [mutaciones](archivo/spec-0097-bitacora-de-mutaciones.md).

## Sistema de diseño de Loyalty — aprobado el 2026-09-26

- **ADR 0090 / Spec 0100:** [Tours de onboarding y ayuda de Loyalty](specs/0100-tours-de-onboarding-y-ayuda-de-loyalty.md).
  Orientación general y ayudas para crear, editar, cerrar y políticas sobre el editor existente.
  Estado: **parqueada por owner el 2026-09-26**, propuesta/borrador en [PARQUEADO](PARQUEADO.md); sin código.

- **ADR 0089:** [Loyalty reutiliza el sistema del wizard merchant](adr/0089-loyalty-reutiliza-el-sistema-del-wizard-merchant.md).
  Unifica consulta, formularios, progreso y cierre con el catálogo compartido, conservando reglas/API.
  Estado: **aceptada** por el owner; UI implementada localmente el 2026-09-26.
- **Spec 0098:** [Loyalty: sistema de diseño del backoffice](specs/0098-loyalty-sistema-de-diseno-del-backoffice.md).
  Comparación por superficie/campo y verificación; revisión 2026-09-26 incorpora PUT 0079 y permisos/codes 0086, con acceso staff por loyalty.
  Estado: **cerrada con UI implementada y QA pendiente**, no disjunta; [entrega](archivo/spec-0098-handoff-ui.md) y [PASS independiente](archivo/spec-0098-revision-ui.md).

## API de Loyalty — auditoria del 2026-09-25

- **Spec 0099:** [El DTO del programa a lista blanca, y el contrato vigente consolidado](specs/0099-el-dto-del-programa-y-el-contrato-vigente.md).
  Endurece `toClientProgram` (hoy filtra 6 columnas internas via spread) y consolida en un
  anexo el contrato vigente de las 4 rutas de `/api/loyalty-program`, corrigiendo el `code`
  403 que el anexo 0079 dejo desactualizado tras la 0086 (`not_owner` → `not_member` /
  `missing_permission` en `GET`/`PUT`).
  Estado: **implementada**, con PASS de revisor independiente (2 mutaciones del
  presupuesto, ambas ROJAS bajo mutacion y revertidas con `diff` vacio; anexo verificado
  linea por linea contra `api-permission.ts`/`api-owner.ts`/`route.ts`, sin aceptar
  ninguna cita como prueba de si misma). No toca el contrato de escritura ni habilita
  `tiers`/`cashback`.
  `specs/0099-el-dto-del-programa-y-el-contrato-vigente.md` + anexo
  `specs/0099-contratos-de-api.md`.

## Convenciones

- **ADR** = una decision y su motivo. Se escribe cuando la decision se toma, no despues.
  Inmutable: si cambia, se escribe uno nuevo que supersede al viejo.
- **Spec** = que se va a construir, cerrada **antes** de tocar codigo. Ver
  `specs/TEMPLATE.md`.
- Numeracion correlativa de 4 digitos. No se reusan numeros.
- Frontmatter obligatorio con `fecha` y `resumen` de una linea: es lo que se lee sin
  abrir el archivo.

## Proceso con agentes

El protocolo obligatorio de orquestación, implementación, handoff y revisión independiente
está en [AGENT-WORKFLOW.md](AGENT-WORKFLOW.md). La plantilla de spec exige especificación
técnica, Definition of Done y plan de pruebas antes de que su estado pase a `cerrada`.
